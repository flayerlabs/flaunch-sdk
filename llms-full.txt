# Flaunch SDK

[![npm version](https://img.shields.io/npm/v/@flaunch/sdk.svg)](https://npmjs.com/package/@flaunch/sdk)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

A TypeScript SDK for seamless interaction with the Flaunch protocol and Uniswap V4.

> This protected-swap branch is release-blocked. Its planner requires new router capabilities not supplied by the current legacy address maps. Follow [the coordinated release gates](./PROTECTED_SWAP_RELEASE.md); do not publish or deploy a dependency-only upgrade.

![Flaunch Header](https://raw.githubusercontent.com/flayerlabs/flaunch-sdk/refs/heads/master/.github/flaunch-header.png)

_Note: Add this `llms-full.txt` file into Cursor IDE / LLMs to provide context about using the Flaunch SDK: [llms-full.txt](https://raw.githubusercontent.com/flayerlabs/flaunch-sdk/refs/heads/master/llms-full.txt)_

## Features

- 🚀 Flaunch new memecoins
- 💱 Buy and sell memecoins via Uniswap V4 hooks
- 🏗️ Build your own token launchpads on top of the flaunch protocol
- 📊 Read functions for token and pool data
- 🔒 Built-in Permit2 support for gasless approvals
- 🌐 Works on Base, Base Sepolia, Ethereum, Unichain, and Robinhood

## Network support

Base and Base Sepolia retain their generation-specific launch paths. Ethereum, Unichain, and Robinhood support standard direct launches, existing-manager launches, static and dynamic address fee split launches, the IPFS metadata helper, `PoolCreated` receipt decoding, and creator fee claims through the legacy multichain zap. This does not add v1.3 paired-token manager launches or general multichain liquidity support. Robinhood also supports native ETH swaps and paired-token launches through their separate capability-gated paths.

The v1.3.1 multi-asset manager generation has address mappings for Base, Base Sepolia, and Robinhood. Gate these separate `*V1_3` APIs with `doesChainSupportMultiAssetManagers()`; legacy manager helpers do not select that generation automatically.

Static-split recipients independently divide 100% of the pool remaining after creator/owner allocation. This corrects previous Base behavior; callers must not pre-scale recipient percentages by the residual pool size. See the unreleased changelog and static-split tests before upgrading.

## Table of Contents

- [Installation](#installation)
- [Quick Start](#quick-start)
- [Usage](#usage)
  - [Read Operations (with Viem)](#read-operations-with-viem)
  - [Write Operations (with Viem + Wagmi)](#write-operations-with-viem--wagmi)
  - [Flaunching a Memecoin](#flaunching-a-memecoin)
  - [Flaunching with a Paired Token](#flaunching-with-a-paired-token)
    - [How to generate `base64Image` from User uploaded file](#how-to-generate-base64image-from-user-uploaded-file)
  - [Flaunch with Address Fee Splits](#flaunch-with-address-fee-splits)
  - [Buying a Flaunch coin](#buying-a-flaunch-coin)
  - [Selling with Permit2](#selling-with-permit2)
  - [Swap using USDC or other tokens by passing `intermediatePoolKey`](#swap-using-usdc-or-other-tokens-by-passing-intermediatepoolkey)
  - [Claiming creator revenue](#claiming-creator-revenue)
  - [Advanced Integration: Revenue Sharing with RevenueManager](#advanced-integration-revenue-sharing-with-revenuemanager)
    - [v1.3.1 multi-asset RevenueManager (Base)](#v131-multi-asset-revenuemanager-base)
  - [Bot Protection during Fair Launch via TrustedSigner](#bot-protection-during-fair-launch-via-trustedsigner)
  - [Groups](#groups)
    - [v1.3.1 multi-asset Groups (Base)](#v131-multi-asset-groups-base)
  - [Importing External Coins into Flaunch](#importing-external-coins-into-flaunch)
  - [Adding Liquidity to Imported (or flaunch) coins](#adding-liquidity-to-imported-or-flaunch-coins)
  - [Import AND Add Liquidity calls in a single batch](#import-and-add-liquidity-calls-in-a-single-batch)
  - [Import AND Add Single-Sided Coin Liquidity calls in a single batch](#import-and-add-single-sided-coin-liquidity-calls-in-a-single-batch)
  - [`createFlaunchCalldata`: alternative to `createFlaunch`](#createflaunchcalldata-alternative-to-createflaunch-that-returns-the-tx-call-object-with-to-data-and-value-instead-of-directly-broadcasting-the-tx-from-walletclient)
  - [All SDK functions](#all-sdk-functions)
  - [React Hooks](#react-hooks)
- [Flaunch Docs](#flaunch-reference)

## Installation

Using pnpm (recommended):

```bash
pnpm add @flaunch/sdk
```

Using npm:

```bash
npm install @flaunch/sdk
```

Using yarn:

```bash
yarn add @flaunch/sdk
```

## Quick Start

Here's a simple example to get started with reading token metadata:

```ts
import { createFlaunch } from "@flaunch/sdk";
import { createPublicClient, http } from "viem";
import { base } from "viem/chains";

const publicClient = createPublicClient({
  chain: base,
  transport: http(),
});

const flaunchRead = createFlaunch({ publicClient });
const { symbol, name, image } = await flaunchRead.getCoinMetadata(coinAddress);
// returns: {symbol: "TEST", name: "Test", image: "https://<IMAGE_URL>"}
```

## Usage

The SDK provides two types of operations:

1. Read operations: Only require a `publicClient`
2. Write operations: Require both `publicClient` and `walletClient`

### Read Operations (with Viem)

```ts
import { createFlaunch } from "@flaunch/sdk";
import { createPublicClient, http } from "viem";
import { base } from "viem/chains";

const publicClient = createPublicClient({
  chain: base,
  transport: http("<RPC_URL>"), // "<RPC_URL>" is optional, defaults to public RPC
});

const flaunchRead = createFlaunch({ publicClient });

// Read token metadata
const { symbol, name, image } = await flaunchRead.getCoinMetadata(coinAddress);
// returns: {symbol: "TEST", name: "Test", image: "https://<IMAGE_URL>"}
```

### Write Operations (with Viem + Wagmi)

For write operations, you'll need both Viem and Wagmi. Here's how to set it up in a React component:

```ts
import { createFlaunch, ReadWriteFlaunchSDK } from "@flaunch/sdk";
import { base } from "viem/chains";
import { useWalletClient } from "wagmi";
import { useMemo } from "react";

// ... your React component ...

const publicClient = createPublicClient({
  chain: base,
  transport: http("<RPC_URL>"), // "<RPC_URL>" is optional, defaults to public RPC
});
const { data: walletClient } = useWalletClient();

const flaunchWrite = useMemo(() => {
  if (!publicClient && !walletClient) return null;

  return createFlaunch({
    publicClient,
    walletClient,
  }) as ReadWriteFlaunchSDK;
}, [publicClient, walletClient]);
```

### Flaunching a Memecoin

Flaunch your own memecoin with default parameters:

_**Flaunches below $10k market caps don't incur any protocol fees!**_

```ts
const hash = await flaunchWrite.flaunchIPFS({
  name: "Test",
  symbol: "TEST",
  fairLaunchPercent: 0, // 0%
  fairLaunchDuration: 30 * 60, // 30 mins
  initialMarketCapUSD: 4_000, // $4k
  creator: address,
  creatorFeeAllocationPercent: 80, // 80% to creator, 20% to community
  metadata: {
    base64Image: imageData, // refer to the code below, on how to generate this base64Image
    description: "Your memecoin description",
    // optional:
    websiteUrl: "https://example.com/",
    discordUrl: "https://discord.gg/example",
    twitterUrl: "https://x.com/example",
    telegramUrl: "https://t.me/example",
  },
});

// parse the logs from the tx hash to get memecoin address and Flaunch NFT tokenId

import { PoolCreatedEventData } from "@flaunch/sdk";

// Parse the flaunch transaction hash
const poolCreatedData: PoolCreatedEventData | null =
  await flaunchRead.getPoolCreatedFromTx(hash);

if (poolCreatedData) {
  console.log("Memecoin Address:", poolCreatedData.memecoin);
  console.log("Token ID:", poolCreatedData.tokenId);
  // ... other params
}
```

### Flaunching with a Paired Token

Base, Base Sepolia, and Robinhood support the V1.3 paired-token launch path.
The paired token must be approved by that chain's `PairedTokenRegistry`; use
`zeroAddress` to select native ETH.

```ts
import { doesChainSupportPairedTokenLaunch } from "@flaunch/sdk";
import { zeroAddress } from "viem";

if (!doesChainSupportPairedTokenLaunch(publicClient.chain.id)) {
  throw new Error("Paired-token launches are not supported on this chain");
}

const flaunchParams = {
  name: "Paired Coin",
  symbol: "PAIR",
  tokenUri: "ipfs://...",
  premineAmount: 0n,
  creator: address,
  creatorFeeAllocation: 8_000,
  flaunchAt: 0n,
  initialPriceParams,
  feeCalculatorParams: "0x" as const,
  pairedToken,
};

if (!(await flaunchRead.isPairedTokenApproved(pairedToken))) {
  throw new Error("Paired token is not approved");
}

const quote = await flaunchRead.calculatePairedTokenFlaunchFee({
  flaunchParams,
  slippageBps: 100n,
});

const hash = await flaunchWrite.flaunchPairedToken({
  flaunchParams,
  trustedFeeSigner: zeroAddress,
  value: quote.ethRequired,
  maxPremineCost: quote.pairedPremineCost,
});
```

The SDK does not auto-approve ERC20 spending or enforce a sponsorship policy.
Applications that promise a gas-sponsored launch should reject unexpected
non-zero quote values before signing, as the API service does.

#### How to generate `base64Image` from User uploaded file

```tsx
// handle image file input and convert into base64
const handleImageChange = useCallback(
  (e: ChangeEvent<HTMLInputElement>) => {
    if (e.target.files && e.target.files[0]) {
      const file = e.target.files[0];

      // read the file as base64
      const reader = new FileReader();
      reader.onloadend = () => {
        const base64String = reader.result as string;

        // this can be passed to the flaunch function call
        handleFlaunch({ imageData: base64String });

        // use `storedImagePreview` to display the uploaded image with <image src={storedImagePreview} />
        setStoredImagePreview(base64String);
      };
      reader.readAsDataURL(file);
    }
  },
  [setStoredImagePreview]
);

// File upload input
<input
  type="file"
  accept="image/*"
  onchange="handleImageChange(event)"
  id="image-upload"
/>;
```

### Flaunch with Address Fee Splits

You can flaunch a coin and share the revenue with the creator and an array of recipients, each with a different percentage share.\
Example below of a creator splitting 50% of their revenue with 2 addresses, with 30% and 70% share respectively:

```ts
await flaunchWrite.flaunchIPFSWithSplitManager({
  name: "...",
  symbol: "...",
  metadata: {
    base64Image: "...",
  },
  fairLaunchPercent: 0,
  fairLaunchDuration: 30 * 60, // 30 mins
  initialMarketCapUSD: 1_000,
  creator: "0x...",
  creatorFeeAllocationPercent: 100,
  // **Note:** Split related params
  creatorSplitPercent: 50,
  splitReceivers: [
    {
      address: "0x123...",
      percent: 30,
    },
    {
      address: "0xabc...",
      percent: 70,
    },
  ],
});
```

### Flaunch with Dynamic Address Fee Splits

Use the dynamic split manager when recipient weights need to be updated after deployment.
Unlike the static split manager, recipient shares are mutable and do not need to be modeled as a fixed 100% distribution in SDK inputs.
`creatorShare + managerOwnerShare` must be less than or equal to `100_00000` (`VALID_SHARE_TOTAL`).

```ts
await flaunchWrite.flaunchIPFSWithDynamicSplitManager({
  name: "...",
  symbol: "...",
  metadata: {
    base64Image: "...",
  },
  fairLaunchPercent: 0,
  fairLaunchDuration: 30 * 60, // 30 mins
  initialMarketCapUSD: 1_000,
  creator: "0x...",
  creatorFeeAllocationPercent: 100,
  // Dynamic split manager params (raw share weights)
  creatorShare: 10_00000n,  // 10%
  managerOwnerShare: 20_00000n,  // 20%
  moderator: "0xmoderator...",
  splitReceivers: [
    { address: "0x123...", share: 2_000_000n },
    { address: "0xabc...", share: 1_000_000n },
  ],
});
```

You can update recipient distribution after deployment:

```ts
import { ReadWriteDynamicAddressFeeSplitManager } from "@flaunch/sdk";

const manager = new ReadWriteDynamicAddressFeeSplitManager(
  "0xmanagerAddress...",
  flaunchWrite.drift
);

// share = 0 removes recipient; non-zero adds/updates recipient share
await manager.updateRecipients([
  { recipient: "0x123...", share: 1_500_000n },
  { recipient: "0xdef...", share: 500_000n },
  { recipient: "0xabc...", share: 0n },
]);
```

### Buying a Flaunch coin

```ts
// Execute the buy transaction
const buyTokens = async () => {
  const hash = await flaunchWrite.buyCoin({
    coinAddress,
    slippagePercent: 5,
    swapType: "EXACT_IN",
    amountIn: parseEther("0.1"),
  });

  // Wait for confirmation
  const receipt = await flaunchWrite.drift.waitForTransaction({ hash });
  console.log(receipt.status === "success" ? "Success" : "Failed");
};
```

### Selling with Permit2

Permit2 enables gasless token approvals. Here's how to implement token selling with Permit2:

```ts
import { useSignTypedData } from "wagmi";
import { parseEther, Hex } from "viem";

const {
  signTypedData,
  status: sigStatus,
  error: sigError,
  data: signature,
} = useSignTypedData();

// Check allowance and permit if needed
const { allowance } = await flaunchWrite.getPermit2AllowanceAndNonce(
  coinAddress
);

if (allowance < parseEther(coinAmount)) {
  const { typedData, permitSingle } = await flaunchWrite.getPermit2TypedData(
    coinAddress
  );
  signTypedData(typedData);

  // then call this when signature is not undefined
  const hash = await flaunchWrite.sellCoin({
    coinAddress,
    amountIn: parseEther(coinAmount),
    slippagePercent: 5,
    permitSingle,
    signature,
  });
} else {
  // if already approved
  const hash = await flaunchWrite.sellCoin({
    coinAddress,
    amountIn: parseEther(coinAmount),
    slippagePercent: 5,
  });
}
```

### Swap using USDC or other tokens by passing `intermediatePoolKey`

An `intermediatePoolKey` can be specified for the swap, where one currency of the poolKey is required to be `ETH` and the other currency can be `USDC` or some other token for which you want to swap from or into.
With the addition of this intermediate pool key, the swap gets routed as: `USDC -> ETH -> flETH -> Coin`

1. Buy Coin with USDC

```ts
import { useSignTypedData } from "wagmi";
const {
  signTypedData,
  status: sigStatus,
  error: sigError,
  data: signature,
} = useSignTypedData();

// flaunch coins by default have infinite approval for Permit2
// this is required for external tokens like USDC
const usdcAllowanceToPermit2 = await flaunchWrite.getERC20AllowanceToPermit2(
  usdcAddress
);
if (usdcAllowanceToPermit2 < amount) {
  // 1. max approve tx for Permit2 address to spend USDC
  await flaunchWrite.setERC20AllowanceToPermit2(usdcAddress, maxUint256);
}

const { allowance: usdcPermit2Allowance } =
  await flaunchWrite.getPermit2AllowanceAndNonce(usdcAddress);
if (usdcPermit2Allowance < amount) {
  // 2. sign permit2 signature to spend USDC for swap
  const { typedData, permitSingle } = await flaunchWrite.getPermit2TypedData(
    usdcAddress
  );
  signTypedData(typedData);
}

// 3. Buy flaunch coin with USDC
await flaunchWrite.buyCoin({
  coinAddress: coin,
  slippagePercent: 5,
  swapType: "EXACT_IN",
  amountIn: amount, // USDC amount
  // poolKey to route USDC to ETH swap
  intermediatePoolKey: {
    currency0: zeroAddress,
    currency1: "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913", // USDC
    fee: 500,
    tickSpacing: 10,
    hooks: zeroAddress,
    hookData: "0x",
  },
  permitSingle,
  signature,
  referrer: referrer,
});
```

2. Sell coin for USDC

```ts
import { useSignTypedData } from "wagmi";
import { parseEther, Hex } from "viem";

const {
  signTypedData,
  status: sigStatus,
  error: sigError,
  data: signature,
} = useSignTypedData();

// Check allowance and permit if needed
const { allowance } = await flaunchWrite.getPermit2AllowanceAndNonce(
  coinAddress
);

if (allowance < parseEther(coinAmount)) {
  const { typedData, permitSingle } = await flaunchWrite.getPermit2TypedData(
    coinAddress
  );
  signTypedData(typedData);

  // then call this when signature is not undefined
  const hash = await flaunchWrite.sellCoin({
    coinAddress,
    amountIn: parseEther(coinAmount),
    slippagePercent: 5,
    permitSingle,
    signature,
    // poolKey to route ETH to USDC swap
    intermediatePoolKey: {
      currency0: zeroAddress,
      currency1: "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913", // USDC
      fee: 500,
      tickSpacing: 10,
      hooks: zeroAddress,
      hookData: "0x",
    },
  });
} else {
  // if already approved
  const hash = await flaunchWrite.sellCoin({
    coinAddress,
    amountIn: parseEther(coinAmount),
    slippagePercent: 5,
    // poolKey to route ETH to USDC swap
    intermediatePoolKey: {
      currency0: zeroAddress,
      currency1: "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913", // USDC
      fee: 500,
      tickSpacing: 10,
      hooks: zeroAddress,
      hookData: "0x",
    },
  });
}
```

3. The quote functions `getSellQuoteExactInput`, `getBuyQuoteExactInput` and `getBuyQuoteExactOutput` also support passing optional `intermediatePoolKey`.

### Claiming creator revenue

Creator fees accrue in a fee escrow and are claimed by the creator (the current holder of the coin's Flaunch NFT). Which escrow depends on the coin's pairing:

- **flETH-paired coins** (the default) use the single-token `FeeEscrow`: `creatorRevenue()` returns the claimable ETH and `withdrawCreatorRevenue()` claims it.
- **Coins paired with anything else** — native ETH, or a registry token such as the B20 equities on Base (AAPLc, GOOGLc, …) — use the v1.3.1 **multi-token `FeeEscrow`**: one contract per chain, balances keyed `(recipient, token)` and denominated in the paired token. The contract cannot enumerate a recipient's keys, so you name the tokens to look at. A flETH key pays out as ETH; a stock key is delivered as the stock itself.

```ts
import { zeroAddress } from "viem";
import { doesChainSupportMultiTokenFeeEscrow } from "@flaunch/sdk";

const AAPLC = "0xb200000000000000000000C2e324d24d7eEcd1fb";

if (doesChainSupportMultiTokenFeeEscrow(base.id)) {
  // One entry per token, in that token's raw units (AAPLc has 8 decimals)
  const balances = await flaunchRead.creatorRevenueByToken({
    creator: creatorAddress,
    tokens: [zeroAddress, AAPLC],
  });

  const tokens = balances.filter((b) => b.amount > 0n).map((b) => b.token);
  if (tokens.length > 0) {
    // One transaction sweeps every key
    await flaunchWrite.withdrawCreatorRevenueByToken({ tokens });
  }
}
```

### Advanced Integration: Revenue Sharing with RevenueManager

For platforms building on top of Flaunch, the `RevenueManager` contract enables sophisticated revenue-sharing models. It allows platforms to automatically take a protocol fee from the trading fees generated by memecoins launched through their integration.

**Key Concepts:**

1.  **Deployment & Initialization**: Before flaunching tokens via your platform, deploy an instance of your `RevenueManager` with your protocol fee and recipient address.

```ts
const revenueManagerInstanceAddress = await flaunchWrite.deployRevenueManager({
  protocolRecipient: "0xabc...",
  protocolFeePercent: 20, // 20% revenue share between protocol and 80% to coin creators (of the 1% swap fees)
});
```

2.  **Flaunching Directly to Manager**: Instead of the creator receiving the Flaunch NFT, you'd flaunch the token directly into the `RevenueManager`. This automatically sets up the fee split.

```ts
await flaunchWrite.flaunchIPFSWithRevenueManager({
  name: "...",
  symbol: "...",
  metadata: {
    base64Image: "...",
  },
  fairLaunchPercent: 0,
  fairLaunchDuration: 30 * 60, // 30 mins
  initialMarketCapUSD: 1_000,
  creator: "0x...",
  creatorFeeAllocationPercent: 100,
  // **Note:** Specify your instance of the RevenueManager here
  revenueManagerInstanceAddress: "...",
});
```

3.  **Claiming Fees**: Creators can claim their share of earned fees, and the protocol can claim its share via the `RevenueManager` contract.

```ts
// check address' balance
const balance = await flaunchRead.revenueManagerBalance({
  revenueManagerAddress: revenueManagerInstanceAddress,
  recipient: "0xabc...",
});

// protocol claim: the connected wallet must be the protocol recipient for the revenue manager
await flaunchWrite.revenueManagerProtocolClaim({
  revenueManagerAddress: revenueManagerInstanceAddress,
});

// creator claim: the connected wallet must be the coin's creator for them to claim their share
await flaunchWrite.revenueManagerCreatorClaim({
  revenueManagerAddress: revenueManagerInstanceAddress,
});

// creator claim: for specific flaunch token ids:
await flaunchWrite.revenueManagerCreatorClaimForTokens({
  revenueManagerAddress: revenueManagerInstanceAddress,
  flaunchTokens: [
    {
      flaunch: FlaunchAddress[base.id],
      tokenId: 5,
    },
    {
      flaunch: FlaunchV1_1Address[base.id],
      tokenId: 10,
    },
  ],
});
```

Refer to the [RevenueManager Docs](https://docs.flaunch.gg/manager-types/revenuemanager) for detailed implementation guides and function references.

#### v1.3.1 multi-asset RevenueManager (Base)

Base mainnet also runs a second manager generation (flaunch-managers release `v1.3.1-base`): its own `TreasuryManagerFactory` (`TreasuryManagerFactoryV1_3Address`), implementations (`RevenueManagerV1_3Address`, …) and a `FlaunchManagerZap` (`FlaunchManagerZapV1_3Address`) that deploys them. Managers of this generation keep a balance **per payout asset** — native ETH (`zeroAddress`) for flETH / native pools, otherwise the coin's paired token (a wrapper's underlying, or a B20 stock as itself) — so they are the ones to use for coins paired with a stock or native ETH. The two generations do not mix: a v1.3.1 coin cannot be deposited into a manager from the old factory, and `deployRevenueManager()` / `revenueManagerBalance()` / `revenueManagerCreatorClaim()` keep talking to the old one. Everything for the new generation carries a `V1_3` suffix and is gated by `doesChainSupportMultiAssetManagers()`.

```ts
import { zeroAddress } from "viem";
import {
  doesChainSupportMultiAssetManagers,
  FlaunchV1_3Address,
  FlaunchVersion,
} from "@flaunch/sdk";

if (doesChainSupportMultiAssetManagers(base.id)) {
  // 1. deploy through the v1.3.1 FlaunchManagerZap; resolved from the v1.3.1 factory's ManagerDeployed event
  const managerV1_3 = await flaunchWrite.deployRevenueManagerV1_3({
    protocolRecipient: "0xabc...",
    protocolFeePercent: 20,
    // WHITELISTED resolves to WhitelistedPermissionsV1_3Address (the old instance is bound to the old factory)
  });

  // 2. move an existing v1.3.1 coin in (approve the Flaunch NFT first, as in the Groups section)
  await flaunchWrite.addToTreasuryManager(managerV1_3, FlaunchVersion.V1_3, tokenId);

  // 3. balances are per payout asset — find out which ones the manager has been paid in
  const assets = await flaunchRead.revenueManagerPayoutAssets(managerV1_3); // e.g. [zeroAddress, AAPLc]
  const aaplBalance = await flaunchRead.revenueManagerBalanceV1_3({
    revenueManagerAddress: managerV1_3,
    recipient: "0xabc...",
    asset: assets[1], // omit for the native ETH bucket
  });
  const protocolBalances = await flaunchRead.treasuryManagerBalancesV1_3({
    treasuryManagerAddress: managerV1_3,
    recipient: "0xabc...", // every payout asset when `assets` is omitted
  });

  // 4. claims settle every payout asset by default …
  await flaunchWrite.revenueManagerProtocolClaimV1_3({ revenueManagerAddress: managerV1_3 });
  await flaunchWrite.revenueManagerCreatorClaimV1_3({
    revenueManagerAddress: managerV1_3,
    flaunchTokens: [{ flaunch: FlaunchV1_3Address[base.id], tokenId }], // optional
  });

  // … or a subset, so a recipient a transfer-restricted stock refuses can still collect the rest
  await flaunchWrite.revenueManagerCreatorClaimV1_3({
    revenueManagerAddress: managerV1_3,
    assets: [zeroAddress],
  });
}
```

`treasuryManagerBalancesV1_3()` and `treasuryManagerClaimAssetsV1_3()` work on any v1.3.1 manager (the fee-split managers and the staking manager too), and the `ReadRevenueManagerV1_3` / `ReadDynamicAddressFeeSplitManagerV1_3` / `ReadStakingManagerV1_3` clients (and their `ReadWrite*` variants) expose the full per-asset surface. Launching a coin straight into a v1.3.1 manager is not yet wired through the SDK — `flaunchWithRevenueManager()` and the other launch helpers still go through the previous-generation zap.

### Bot Protection during Fair Launch via TrustedSigner

1. The `flaunch` call can be modified to enable TrustedSigner. For coins where it's enabled in their fair launch period, the buy transactions revert if they don't include the hookData with the signature (unique to the coin & sender address).
   It's upto your UI integration to use some captcha service or other sort of gating to allow the signature to be generated only for the selected users that are trying to buy the coin during it's fair launch.

You can also limit the amount of coins that can be bought in a single swap tx, or even set per wallet caps.

**Note:** If your have provided your custom `trustedFeeSigner`, then the signatures are expected to be signed by your address. The Flaunch UI won't be able to execute the swaps during fair launch in that case, as our UI won't have access to your keys.
But you may leave the `trustedFeeSigner` param `undefined`, then the swaps can go through our UI. But you won't be able to generate valid signatures on end, as you won't access to our signer address.

1.1. Without Premine

```ts
const hash = await flaunchWrite.flaunchIPFS({
  name: "Test",
  ...
  trustedSignerSettings: {
    enabled: true,
    trustedFeeSigner: "0x..",
    walletCap: parseEther("1000000"), // optional
    txCap: parseEther("10000"),       // optional
  }
});
```

1.2. With Premine
Refer to '2.' below to understand how to generate the deadline and signature, that needs to be passed here.

```ts
const hash = await flaunchWrite.flaunchIPFS({
  name: "Test",
  ...
  premineAmount: 123..,
  trustedSignerSettings: {
    enabled: true,
    trustedFeeSigner: "0x...",
    walletCap: parseEther("1000000"), // optional
    txCap: parseEther("10000"),       // optional
    premineSignedMessage: {
    	deadline: 1234..,
    	signature: "0xabc...",
    }
  }
});
```

2. Backend SDK to sign & generate hookData
   As the signing happens via your secure privateKey, we have a separate `FlaunchBackend` exported from our SDK package that's intended to be run on your nodejs backend or API. The API can verify if the request is legit (post captcha verification), and then return the signed hookData to be used for the `buyCoin` transaction.

```ts
import { FlaunchBackend } from "@flaunch/sdk";

const flaunchBackend = new FlaunchBackend(privateKeyHex, chain.id);

const { hookData, deadline, signature } =
  await flaunchBackend.generateHookDataWithSignature({
    userWallet,
    coinAddress,
    referrer: referrer || undefined, // Optional parameter
    deadlineFromNowInSeconds: 10 * 60, // 10 minutes in seconds
  });

// hookData would be passed to buyQuote and buyCoin functions
// signature and deadline useful for passing to the flaunch call with premine, via `trustedSignerSettings.premineSignedMessage` parameter
```

3. Check TrustedSigner status for a coin

```ts
const {
  isCurrentlyEnabled,
  trustedSignerEnabled,
  signer,
  fairLaunchStartsAt,
  fairLaunchEndsAt,
  isFairLaunchActive,
} = await flaunchRead.trustedPoolKeySignerStatus(coinAddress);

// if `isCurrentlyEnabled` is true, then we need to sign & pass in the hookData for the swaps with the given `signer` address
```

4. Buy quotes with TrustedSigner

```ts
const quote = await flaunchRead.getBuyQuoteExactInput({
  coinAddress: coin,
  amountIn: amount,
  // when trusted signer is currently enabled:
  hookData, // from the backend SDK
  userWallet: address,
});
```

5. Buy coin transaction with TrustedSigner

```ts
await flaunchWrite.buyCoin({
  coinAddress: coin,
  slippagePercent: 5,
  swapType: "EXACT_IN",
  referrer: referrer,
  // when trusted signer is currently enabled:
  hookData, // from the backend SDK
});
```

### Groups

The technical side of Groups is handled by the StakingManager. It allows the creator to deposit their flaunch memestream into the manager, and then a defined ERC20 token can be staked by the user to earn their share of the ETH rewards from the memestream.

1. Deploy your instance of the staking manager

```ts
import { Permissions } from "@flaunch/sdk";

const stakingManagerInstance = await flaunchWrite.deployStakingManager({
  managerOwner: ...,
  stakingToken: ..., // The ERC20 token to stake
  minEscrowDuration: ..., // The minimum duration (in seconds) that the creator's memestream NFT would be locked for
  minStakeDuration: ..., // The minimum duration (in seconds) that the user's tokens would be locked for
  creatorSharePercent: ..., // The % share that a creator will earn from their token
  ownerSharePercent: ..., // The % share that the manager owner will earn from their token
  // The Permissions can be OPEN, CLOSED, or WHITELISTED
  // OPEN = anyone can stake
  // CLOSED = no one except the managerOwner can stake
  // WHITELISTED = only whitelisted addresses can stake
  permissions: Permissions.OPEN,
});
```

2. Setting permissions after a Group is deployed (can only be called by the manager owner)

```ts
await flaunchWrite.treasuryManagerSetPermissions(
	treasuryManagerAddress: stakingManagerInstance,
	permissions: Permissions.CLOSED
);
```

3. Flaunch a new coin into your Group
   3.1. Flaunching into your staking manager instance

```ts
const hash = await flaunchWrite.flaunchIPFS({
  name: "Test",
  ...
  treasuryManagerParams: {
  	manager: stakingManagerInstance
  }
});
```

3.2. Deploying a new staking manager instance, and flaunching a new coin into it in a single transaction

```ts
import { StakingManagerAddress } from "@flaunch/sdk";

const hash = await flaunchWrite.flaunchIPFS({
  name: "Test",
  ...
  treasuryManagerParams: {
  	manager: StakingManagerAddress[chain.id],
    permissions: Permissions.CLOSED
  }
});
```

Note: the `treasuryManagerParams.permissions` value is ignored if the `treasuryManagerParams.manager` is your manager instance instead.

4. Add an already flaunched coin to a group

```ts
import { FlaunchVersion } from "@flaunch/sdk";

// first approve then deposit
const tokenId = 123;
const isApproved = await flaunchRead.isFlaunchTokenApprovedForAll(
  FlaunchVersion.V1_1_1,
  owner,
  treasuryManagerAddress
);

if (!isApproved) {
  await flaunchWrite.setFlaunchTokenApprovalForAll(
    FlaunchVersion.V1_1_1,
    treasuryManagerAddress,
    true
  );
}

await flaunchWrite.addToTreasuryManager(
  treasuryManagerAddress,
  FlaunchVersion.V1_1_1,
  tokenId
);
```

#### v1.3.1 multi-asset Groups (Base)

The v1.3.1 manager generation on Base (see [the RevenueManager note](#v131-multi-asset-revenuemanager-base)) has its own `StakingManager` implementation, `StakingManagerV1_3Address`, deployed through the v1.3.1 `FlaunchManagerZap` rather than `deployStakingManager()`. Staking positions are in the staking token as before; rewards accrue **per payout asset** (native ETH as `zeroAddress`, or a deposited coin's paired token), and a v1.3.1 coin can only be deposited into a v1.3.1 group.

```ts
import { zeroAddress } from "viem";
import { Permissions, ReadStakingManagerV1_3 } from "@flaunch/sdk";

// 1. deploy through the v1.3.1 zap (same params as deployStakingManager), then resolve the
//    address from the v1.3.1 factory's ManagerDeployed event
const hash = await flaunchWrite.readWriteFlaunchManagerZapV1_3.deployStakingManager({
  managerOwner,
  stakingToken,
  minEscrowDuration,
  minStakeDuration,
  creatorSharePercent: 10,
  ownerSharePercent: 5,
  permissions: Permissions.OPEN,
});
const groupV1_3 = await flaunchRead.readTreasuryManagerFactoryV1_3.getManagerDeployedAddressFromTx(hash);

// 2. read per-asset rewards
const staking = new ReadStakingManagerV1_3(groupV1_3, flaunchRead.drift);
const position = await staking.userPositions(user); // { amount, timelockedUntil }
const assets = await staking.payoutAssets();
const ethRewards = await staking.pendingStakeRewards(user, zeroAddress);
const stockRewards = await staking.pendingStakeRewards(user, assets[1]);

// 3. claim everything, or only the assets that will not block the call
await flaunchWrite.treasuryManagerClaimAssetsV1_3({ treasuryManagerAddress: groupV1_3, assets: [zeroAddress] });
```

Any other v1.3.1 implementation deploys the same way through `readWriteFlaunchManagerZapV1_3.deployAndInitializeManager({ managerImplementation, owner, data, permissions })`, with `data` being the implementation's ABI-encoded `InitializeParams` (see the `initialize` entry of its `*V1_3Abi`). `GroupMapperV1_3Address` and `ERC721OwnerFeeSplitManagerV1_3Address` are exported as addresses and ABIs only.

### Importing External Coins into Flaunch

You can import external ERC20 coins launched on other platforms like Zora, Virtuals, etc. into Flaunch to utilize the power of our Uniswap V4 hooks!

1.1. Import with coin's current price

```ts
await flaunchWrite.importMemecoin({
  coinAddress: "0x...", // ERC20 token contract address
  creatorFeeAllocationPercent: 100, // Fee allocation to creator (0-100%)
  initialPriceUSD: 1, // Starting price in USD
});
```

1.2. Import with coin's current market cap. For memecoins, market caps are more human readable so you can pass it as follows

```ts
await flaunchWrite.importMemecoin({
  coinAddress: "0x...", // ERC20 token contract address
  creatorFeeAllocationPercent: 100, // Fee allocation to creator (0-100%)
  initialMarketCapUSD: 10_000, // Starting market cap in USD
});
```

**Note:** The Flaunch Version for imported coins is: `FlaunchVersion.ANY` where `import {FlaunchVersion} from "@flaunch/sdk"`

**⚠️ Note:** The term "MarketCap" used throughout simply means `current coin price * ERC20.totalSupply()`. Some coins might show it as FDV, make sure to verify so that the Pool is initialized at the right price, and the liquidity gets provided at the expected range.

1.3. Verify if a memecoin is imported into Flaunch

```ts
const isImported = await flaunchRead.isMemecoinImported(coinAddress);
```

### Adding Liquidity to Imported (or flaunch) coins

When an external coin is imported into Flaunch's Uniswap V4 hooks, it has no liquidity in the pool. We can add liquidity by using the functions below.

Note: for existing flaunch coins you can pass `version` param as `FlaunchVersion.V1_2`, if left blank then the SDK can determine it automatically, defaulting to `FlaunchVersion.ANY` for external coins

1. Read call to calculate the Coin & ETH amounts required for providing liquidity, based on the liquidity range specified. The return values are useful for displaying on the UI, before sending the liquidity transaction.

1.1. Full Range liquidity

```ts
const result = await flaunchRead.calculateAddLiquidityAmounts({
  coinAddress: "0x...", // Token contract address
  // version = FlaunchVersion.ANY for imported coins as they use our {AnyPositionManager}
  version: FlaunchVersion.ANY, // optional (auto determines Flaunch version if not provided, else defaults to ANY if not found)
  liquidityMode: LiquidityMode.FULL_RANGE,
  coinOrEthInputAmount: parseEther("1"), // Input amount in wei
  inputToken: "coin", // "coin" or "eth" - which token amount is specified
  minMarketCap: "0", // not needed for FULL_RANGE
  maxMarketCap: "0", // not needed for FULL_RANGE
});
// Returns: { coinAmount, ethAmount, tickLower, tickUpper, currentTick }
```

1.2. Concentrated Range liquidity
1.2.1. With Market Cap inputs

```ts
const result = await flaunchRead.calculateAddLiquidityAmounts({
  coinAddress: "0x...", // Token contract address
  version: FlaunchVersion.ANY,
  liquidityMode: LiquidityMode.CONCENTRATED,
  coinOrEthInputAmount: parseEther("1"), // Input amount in wei
  inputToken: "coin", // "coin" or "eth" - which token amount is specified
  minMarketCap: "1000000", // $1M
  maxMarketCap: "5000000", // $5M
});
// Returns: { coinAmount, ethAmount, tickLower, tickUpper, currentTick }
```

1.2.2. With Coin Price inputs

```ts
const result = await flaunchRead.calculateAddLiquidityAmounts({
  coinAddress: "0x...", // Token contract address
  version: FlaunchVersion.ANY,
  liquidityMode: LiquidityMode.CONCENTRATED,
  coinOrEthInputAmount: parseEther("1"), // Input amount in wei
  inputToken: "coin", // "coin" or "eth" - which token amount is specified
  minPriceUSD: "1",
  maxPriceUSD: "5",
});
// Returns: { coinAmount, ethAmount, tickLower, tickUpper, currentTick }
```

1.2.3. `checkSingleSidedAddLiquidity`: helper check function to detect which token input to hide during single-sided concentrated liquidity addition.

Based on the specified concentrated range, we can determine which token (memecoin or ETH) to hide on the UI, as the liquidity would be single-sided

```ts
const { isSingleSided, shouldHideCoinInput, shouldHideETHInput } = await flaunchRead.checkSingleSidedAddLiquidity({
  coinAddress,
  liquidityMode: LiquidityMode.CONCENTRATED,
  // Option 1: can pass market caps
  minMarketCap: "1000000", // $1M
  maxMarketCap: "5000000", // $5M
  currentMarketCap: "1000000" // (optional) can be queried from the pool if not specified
  // OR Option 2: or can pass price
  minPriceUSD: "1",
  maxPriceUSD: "5",
  currentPriceUSD: 0.001, // (optional) can be queried from the pool if not specified
});
```

2. Add Liquidity Transactions
   Adding liquidity is a multi-step process, but we have abstracted away all the complexities. With wallets supporting ERCs 5792 & 7702, users would only get one wallet popup to confirm the transactions. See code snippet below at 2.3. for the same

2.1. With Market Cap inputs

```ts
const addLiqCalls = await flaunchWrite.getAddLiquidityCalls({
  coinAddress: "0x...", // Same parameters as calculateAddLiquidityAmounts
  version: FlaunchVersion.ANY, // optional
  liquidityMode: LiquidityMode.CONCENTRATED,
  coinOrEthInputAmount: parseEther("1"),
  inputToken: "coin",
  minMarketCap: "1000000", // $1M
  maxMarketCap: "5000000", // $5M
});
// Returns: CallWithDescription[] - Array of transaction calls with descriptions
```

2.2. With Coin Price inputs

```ts
const addLiqCalls = await flaunchWrite.getAddLiquidityCalls({
  coinAddress: "0x...", // Same parameters as calculateAddLiquidityAmounts
  version: FlaunchVersion.ANY, // optional
  liquidityMode: LiquidityMode.CONCENTRATED,
  coinOrEthInputAmount: parseEther("1"),
  inputToken: "coin",
  minPriceUSD: "1",
  maxPriceUSD: "5",
});
```

2.3. Sending the transactions as a single batch to the Wallet via ERC-5792 and wagmi

**Note:** if the user's connected wallet doesn't support ERC-5792 (can be checked via `useCapabilities`), then you can simply loop through the `addLiqCalls` array and use `useSendTransaction` to send the transactions individually. The calls have a description to show on the UI if they'd be sent sequentially.

```ts
import { useSendCalls } from "wagmi";

const { sendCalls } = useSendCalls();

const calls = addLiqCalls.map((call) => ({
  to: call.to as `0x${string}`,
  value: call.value,
  data: call.data as `0x${string}`,
}));

await sendCalls({ calls });
```

3. **Single-Sided Coin Liquidity (Current Price to Infinity)**

You can easily provide single-sided coin liquidity from the current price to infinity.

```ts
const singleSidedCalls = await flaunchWrite.getSingleSidedCoinAddLiquidityCalls(
  {
    coinAddress: "0x...", // Token contract address
    coinAmount: parseEther("1000000"), // Amount of coins to provide as liquidity
    version: FlaunchVersion.V1_2, // optional (auto-determines if not provided)
  }
);
```

### Import AND Add Liquidity calls in a single batch

1. This allows an external coin to be live on flaunch, and be tradeable instantly with the liquidity being provided in the same transaction batch.

💡 Tip: To mimic the traditional flaunch liquidity curve, the coin creator can provide liquidity from current market cap to a higher market cap. This makes the liquidity addition to be single-sided, entirely being in their coin with no ETH required. As people buy into the pool and the price goes up, the liquidity position accumulates ETH while selling the coins from the position.

Note: As a flaunch liquidity provider, you won't accumulate the trading fees. The fees are only accumulated to the Creator or split based on the TreasuryManager used.

1.1. With Market Cap inputs

```ts
const importAndAddLiqCalls = await flaunchWrite.getImportAndAddLiquidityCalls({
  coinAddress: "0x...", // ERC20 token contract address
  creatorFeeAllocationPercent: 100, // Fee allocation to creator (0-100%)
  initialMarketCapUSD: 10_000, // Starting market cap in USD
  liquidityMode: LiquidityMode.CONCENTRATED,
  coinOrEthInputAmount: parseEther("1"), // Input amount in wei
  inputToken: "coin", // "coin" or "eth" - which token amount is specified
  minMarketCap: "1000000", // $1M
  maxMarketCap: "5000000", // $5M
});
// Returns: CallWithDescription[] - Array of transaction calls with descriptions

// === send these calls to user's wallet as a batch ===
import { useSendCalls } from "wagmi";

const { sendCalls } = useSendCalls();

const calls = importAndAddLiqCalls.map((call) => ({
  to: call.to as `0x${string}`,
  value: call.value,
  data: call.data as `0x${string}`,
}));

await sendCalls({ calls });
```

1.2. With Coin Price inputs

```ts
const importAndAddLiqCalls = await flaunchWrite.getImportAndAddLiquidityCalls({
  coinAddress: "0x...", // ERC20 token contract address
  creatorFeeAllocationPercent: 100, // Fee allocation to creator (0-100%)
  initialPriceUSD: 1, // Starting price in USD
  liquidityMode: LiquidityMode.CONCENTRATED,
  coinOrEthInputAmount: parseEther("1"), // Input amount in wei
  inputToken: "coin", // "coin" or "eth" - which token amount is specified
  minPriceUSD: "1",
  maxPriceUSD: "5",
});
```

### Import AND Add Single-Sided Coin Liquidity calls in a single batch

This allows importing an external coin and providing single-sided coin liquidity (from current price to infinity) in one transaction batch. This is perfect for coin creators who want their imported tokens to be instantly tradeable with single-sided liquidity that doesn't require ETH.

💡 **Key Benefits:**

- No ETH required for liquidity provision
- Liquidity is provided from current price to infinity
- As price increases, the position automatically sells coins and accumulates ETH
- Instant tradeability after import

  2.1. With Market Cap inputs

```ts
const importAndSingleSidedCalls =
  await flaunchWrite.getImportAndSingleSidedCoinAddLiquidityCalls({
    coinAddress: "0x...", // ERC20 token contract address
    verifier: Verifier.CLANKER, // Optional verifier
    creatorFeeAllocationPercent: 100, // Fee allocation to creator (0-100%)
    coinAmount: parseEther("1000000"), // Amount of coins to provide as liquidity
    initialMarketCapUSD: 10_000, // Starting market cap in USD
  });
// Returns: CallWithDescription[] - Array of transaction calls with descriptions

// === send these calls to user's wallet as a batch ===
import { useSendCalls } from "wagmi";

const { sendCalls } = useSendCalls();

const calls = importAndSingleSidedCalls.map((call) => ({
  to: call.to as `0x${string}`,
  value: call.value,
  data: call.data as `0x${string}`,
}));

await sendCalls({ calls });
```

2.2. With Coin Price inputs

```ts
const importAndSingleSidedCalls =
  await flaunchWrite.getImportAndSingleSidedCoinAddLiquidityCalls({
    coinAddress: "0x...", // ERC20 token contract address
    verifier: Verifier.CLANKER, // Optional verifier
    creatorFeeAllocationPercent: 100, // Fee allocation to creator (0-100%)
    coinAmount: parseEther("1000000"), // Amount of coins to provide as liquidity
    initialPriceUSD: 0.001, // Starting price in USD
  });
```

### `createFlaunchCalldata`: alternative to `createFlaunch` that returns the tx call object with to, data, and value instead of directly broadcasting the tx from walletClient

To support custom signers that might not be compatible with our `walletClient` approach, or to enable account abstraction and batching on your wallet signer's end we have `createFlaunchCalldata` that returns the tx object, leaving the tx broadcasting to be flexible & handled on your end.

All the write calls must go via `parseCall` as shown below, read calls are made normally as before

```ts
import { createFlaunchCalldata, parseCall } from "@flaunch/sdk";

// 1. Can create read-only SDK (similar to createFlaunch without walletClient)
const flaunchRead = createFlaunchCalldata({
  publicClient,
});

// 2. Create read-write SDK for call generation
const flaunchCalldata = createFlaunchCalldata({
  publicClient,
  walletAddress,
});

const call = await parseCall(() =>
  flaunchCalldata.setERC20AllowanceToPermit2(usdcAddress, maxUint256)
);

console.log("Call:", {
  to: call.to,
  value: call.value.toString(),
  data: call.data,
});
```

### All SDK functions

For a list of all the functions in the SDK, refer to: [FlaunchSDK.ts](./src/sdk/FlaunchSDK.ts)

### React Hooks

The package also has hooks to listen for new Flaunches and new Coin Swaps. Refer to: [hooks](./src/hooks/FlaunchPositionManagerHooks.ts)

```tsx
import { usePoolCreatedEvents, usePoolSwapEvents } from "@flaunch/sdk/hooks";

const { logs: poolCreatedLogs } = usePoolCreatedEvents(flaunchRead);
const { logs: poolSwapLogs } = usePoolSwapEvents(flaunchRead, coinAddress);

/**
 * The `poolSwapLogs` calculates & returns the net swapped amount:
 *
 * type BuySwapLog = {
 *  ...eventLog,
 *  timestamp: number;
 *  type: "BUY";
 *   delta: {
 *     coinsBought: bigint;
 *     flETHSold: bigint;
 *     fees: {
 *       isInFLETH: boolean;
 *       amount: bigint;
 *     };
 *   };
 * };
 *
 * type SellSwapLog = {
 *  ...eventLog,
 *  timestamp: number;
 *  type: "SELL";
 *   delta: {
 *     coinsSold: bigint;
 *     flETHBought: bigint;
 *     fees: {
 *       isInFLETH: boolean;
 *       amount: bigint;
 *     };
 *   };
 * };
 */
```

## Flaunch Reference

For detailed protocol documentation, visit our [Docs](https://docs.flaunch.gg/).


# API Documentation

# Protected-swap release gate

## Verified status — 7 September 2026

- Integrated SDK #23's merged master baseline without changing production router mappings.
- Companion contract commits `fee736b` and `c8bfd26` are pushed for review in draft contracts #301. The deployment script is Base-Sepolia-only; production routers remain a separate release gate.
- Dedicated router tests: 15 passed, including 256 fuzz cases, real pool settlement, hook-adjusted output, spend-cap rollback, native refunds and atomic acquisition rollback. Another 75 spend-gate tests passed. The historical Robinhood fork test is unverified because the public RPC returned missing historical state at block 27563803.
- The deployment script restricts the initial rollout to Base Sepolia and its pinned PoolManager. Its Sepolia simulation passed (2,111,872 estimated gas). No broadcast or gate approval occurred; the simulator's predicted address is not a real deployment.
- `scripts/rehearse-protected-sepolia.mjs` passed on a disposable Anvil fork at `127.0.0.1:18547`: deployed the actual router artifact, patched its sole PoolManager immutable to independently verify the entire runtime, impersonated the recorded owner locally, approved both gates and passed the three-hook readiness checker. Runtime hash: `0xc301509b6265a17928e37b58f0ed24b42496a71a41ee909cf11c4be17f52ffb3`. The script rejects non-Anvil endpoints and has a fixed loopback RPC. All transactions stayed on the local fork; no testnet or production state changed.
- At Sepolia block 46503112, both mapped routers lacked `exactInputVersion`. Both spend gates are owned by `0xB8A70b4d1547bf6193bd67A73F4F98ea9FD0A973`. Gate addresses are `0x2c9127654ded3b6b2ba017e84f44b02cafdf9f55` (older hooks) and `0x54cdcf0bcbc3a33f470e07134c10582f93058a32` (current hook). Read evidence is in `release-evidence/sepolia-router-readiness-2026-09-07.json`.

Run `node scripts/check-protected-sepolia.mjs` after building. It checks chain ID, bytecode hash, router API, PoolManager, dispatcher registration and gate approvals at one block; it exits nonzero when readiness is unproven. To validate a newly deployed candidate before editing SDK maps, set `PROTECTED_ROUTER` and `PROTECTED_ROUTER_CODEHASH` to the address and runtime hash independently verified against the reviewed build. It emits approval calldata but never signs or sends transactions.

## Authorized Sepolia-only deployment — 2026-09-07

Router `0xb32a99502f433f78454a4d20304e654cdda75c5c` was deployed by
`0xB8A70b4d1547bf6193bd67A73F4F98ea9FD0A973` on chain 84532, block 46504001.
Deployment transaction: `0x134ce59a452da68a434244b55d7dd13414538bd5b40cfcb42942d53d764e1e15`.
The entire runtime matches the reviewed compiled artifact after resolving its sole
PoolManager immutable. Runtime hash:
`0xc301509b6265a17928e37b58f0ed24b42496a71a41ee909cf11c4be17f52ffb3`.
`manager()` and `exactInputVersion() == 1` were read back successfully.

Both existing Sepolia gates approved this router, preserving previous approvals:

- `0x2c9127654ded3b6b2ba017e84f44b02cafdf9f55`: transaction
  `0xf212b8673e7f2c2923d2da59a485f204caba90527e6807b827c321eae33b4ddd`, block 46504020.
- `0x54cdcf0bcbc3a33f470e07134c10582f93058a32`: transaction
  `0x6d4db09350a4265940e59cd4265a68fb49a8237c28faa7c01fc57a1ffe01b8cd`, block 46504023.

At the time of the Sepolia rollout only Sepolia router mappings had changed; the production
routers below were deployed on 2026-09-08. No npm publication has occurred.
The rebuilt SDK's default mappings passed the on-chain readiness checker for all
three hooks at block 46504128. Frontend native-ETH and flETH integration runs both
passed against this router: launch, signed Game Mode flow, two buys, ERC20 approvals
where required, and matching database/on-chain spend. These runs use the preview's
existing pinned SDK artifact with an explicit router override, not a new npm release.
Updated SDK artifacts pass all 115 tests. The local Node 24 Rollup process again
emitted every bundle but did not exit; it was interrupted after final bundle output.
The initial test attempt while the final bundle was still building failed its
package export check; rerunning after all bundles existed passed all 115 tests.
The frontend's earlier preview override `0x6aF705b1b82f0A74C19D1c468794287AdceA94Ee`
was an earlier deployment of identical runtime; the earlier legacy-mapping checker
did not inspect it. Today's deployment is an additional instance, not a core upgrade.

Remaining external steps: approve the companion contracts review, validate the
combined wallet/browser feature journey, and complete the
production-chain matrix before any 0.13.0 npm release. Production deployments
are not authorized by this Sepolia-only rollout.

## Production routers (2026-09-08, flaunch-contracts v1.3.4, PR #302)

| Chain | Router | Deployment | Verification |
| --- | --- | --- | --- |
| Base (8453) | `0x1B8065a099AdcD7aa7c5e241e3596B56ec98bA5a` | tx `0xf00c94da334553b4c14a7788e74f94140c51f232141ac6a66343f88fa930ba83`, block 51035669 | Basescan-verified; `exactInputVersion() == 1`; `manager()` = `0x498581fF…2b2b` |
| Robinhood (4663) | `0xD33dD3B3Aea607F2cC38cdd154eF5d48847Aa764` | tx `0xb1974d698d9b9bf1a3b3fda66b39732dacc6ed77245da6417b115f20ff542fad`, block 57595126 | Sourcify exact match; `exactInputVersion() == 1`; `manager()` = `0x8366a39C…0951` |

Both runtimes are byte-identical to each other and to the Base Sepolia router outside the CBOR
metadata trailer (the metadata hash differs because comment-only lines were added to the source
before the production compile). The address maps in this candidate now point every hook
generation on Base and Robinhood at these routers; the legacy routers `0xafD627…`, `0x92D2dF…`
and `0x8476ED…` are no longer mapped.

**Publish gate (met 2026-09-08).** Each spend gate approves its chain's new router (read back as
`approvedRouters(router) == true`) and a buy and sell have settled through it on a fork of each chain:

| Gate | Chain | Owner | Approval |
| --- | --- | --- | --- |
| `0xBdbF379f9EdFB5993FC00b41AAEfeE8475eAC0Ac` (v1.3.1 PM `0x588c…`) | Base | `0xB8A70b4d…A973` | tx `0x0dff82d2…e7aa`, block 51037249 |
| `0x120a2e0f8f431136897dc78c24b069146a65d79a` (v1.3.3 PM `0x8d34…`) | Robinhood | `0xB8A70b4d…A973` | tx `0x018de7d4…4b43`, block 57624911 |
| `0xB246b270bB05d9Fa76c4456408ce3e8600d916bf` (v1.3.1 PM `0x588c…` / AnyPM `0x6ea0…`) | Robinhood | `0xB8A70b4d…A973` | tx `0xb7aabb94…c88d`, block 57627582 |

Legacy approvals stay in place during migration.

**Canary evidence (2026-09-08, Anvil forks of the live chains, deployer impersonated, no broadcast).**
Against the deployed routers and real v1.3 pools, the full round trip settled on both chains:

| Chain (fork block) | Pool | Buy | Protection checks | Sell |
| --- | --- | --- | --- | --- |
| Robinhood 4663 (57638009) | V133C `0x411bE1f7…` on v1.3.3 PM, flETH-paired | 0.001 flETH consumed exactly; `ExactInputSwap(sender=deployer, amountIn=1e15)` emitted | tight price limit → `PartialFill` (`0x20aae256`); minimum above delivered → `InsufficientOutput` (`0x2c19b8b8`); past deadline → `Expired` (`0x203d82d8`) | all coins sold back; router holds 0 flETH / 0 coins |
| Base 8453 (51037962) | VBVF `0xe0fe1FAA…` on v1.3.1 PM, flETH-paired | 0.0005 flETH consumed exactly; `ExactInputSwap` emitted | `InsufficientOutput`, `Expired` as above | all coins sold back; router holds 0 / 0 |

Both pools were ungated (no trusted signer) so `hookData` was empty; the gated path through these
routers is covered by the Robinhood fork suite in flaunch-contracts (`SpendGatedRobinhoodFork.t.sol`,
`test_ForkProtectedRouter*`). Live-chain canary transactions were deliberately not sent.

Release in this order:

1. Review the companion contract changes and link their PR. Verify full input consumption, quote-derived minimum net output, expiry, buyer binding and refunds on fork/testnet.
2. Deploy approved routers and verify bytecode and hook-to-router mappings on each supported chain, including superseded hooks. Update gate approvals before enabling consumers.
3. Update this SDK's address maps; run the packaged-artifact suite and supported-chain fork matrix. CI typecheck is necessary but not execution evidence.
4. Upgrade the ordinary frontend swap implementation and Game Mode host together. Main's raw V2 implementation does not become protected merely by installing a newer SDK.
5. Execute the same quote-backed plan presented to the user. Acquire-and-swap compositions must be atomic wherever the product promises one purchase. Report actual fills; unobserved receipts must not invite duplicate spending or be represented as confirmed fills.

Do not publish a package claiming compatibility with legacy routers, do not change only the frontend version, and do not roll back to an unprotected route. Disable affected routes when protection cannot be established.

SDK #23 is independent legacy launch maintenance and does not provide v1.3 manager-launch parity. Merge/release it separately before rebasing this release candidate.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchBackend

# Class: FlaunchBackend

Defined in: sdk/FlaunchBackend.ts:24

Backend class for interacting with Flaunch protocol for generating hookData with signature for swaps.
Doesn't require any publicClient or walletClient to be initialized.

## Constructors

### Constructor

> **new FlaunchBackend**(`signerPrivateKey`, `chainId`): `FlaunchBackend`

Defined in: sdk/FlaunchBackend.ts:28

Parameters

#signerPrivateKey

`` `0x${string}` ``

#chainId

`number`

Returns

`FlaunchBackend`

## Properties

### chainId

> `readonly` **chainId**: `number`

Defined in: sdk/FlaunchBackend.ts:26

***

### signerPrivateKey

> **signerPrivateKey**: `` `0x${string}` ``

Defined in: sdk/FlaunchBackend.ts:25

## Methods

### generateHookDataWithSignature()

> **generateHookDataWithSignature**(`userWallet`): `Promise`\<\{ `deadline`: `number`; `hookData`: `` `0x${string}` ``; `signature`: `` `0x${string}` ``; \}\>

Defined in: sdk/FlaunchBackend.ts:43

Generates the hookData for swaps when TrustedSigner is enabled for a coin during fair launch

Parameters

#userWallet

The wallet address to generate a signature for

##coinAddress

`` `0x${string}` ``

##deadlineFromNowInSeconds

`number`

##referrer?

`` `0x${string}` ``

##userWallet

`` `0x${string}` ``

##version?

`FlaunchVersion`

Returns

`Promise`\<\{ `deadline`: `number`; `hookData`: `` `0x${string}` ``; `signature`: `` `0x${string}` ``; \}\>

The hookData, deadline and signature

***

### getPositionManagerAddress()

> **getPositionManagerAddress**(`version?`): `` `0x${string}` ``

Defined in: sdk/FlaunchBackend.ts:119

Gets the position manager address for a given version

Parameters

#version?

`FlaunchVersion`

The version to get the position manager address for, defaults to the latest version if undefined

Returns

`` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapPartialFillError

# Class: PairedSwapPartialFillError

Defined in: sdk/errors.ts:21

## Extends

- `Error`

## Constructors

### Constructor

> **new PairedSwapPartialFillError**(`amountIn`, `consumedIn`): `PairedSwapPartialFillError`

Defined in: sdk/errors.ts:22

Parameters

#amountIn

`bigint`

#consumedIn

`bigint`

Returns

`PairedSwapPartialFillError`

Overrides

`Error.constructor`

## Properties

### amountIn

> `readonly` **amountIn**: `bigint`

Defined in: sdk/errors.ts:23

***

### consumedIn

> `readonly` **consumedIn**: `bigint`

Defined in: sdk/errors.ts:24


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapSlippageExceededError

# Class: PairedSwapSlippageExceededError

Defined in: sdk/errors.ts:10

## Extends

- `Error`

## Constructors

### Constructor

> **new PairedSwapSlippageExceededError**(`amountOutMin`, `actualAmountOut`): `PairedSwapSlippageExceededError`

Defined in: sdk/errors.ts:11

Parameters

#amountOutMin

`bigint`

#actualAmountOut

`bigint`

Returns

`PairedSwapSlippageExceededError`

Overrides

`Error.constructor`

## Properties

### actualAmountOut

> `readonly` **actualAmountOut**: `bigint`

Defined in: sdk/errors.ts:13

***

### amountOutMin

> `readonly` **amountOutMin**: `bigint`

Defined in: sdk/errors.ts:12


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapUnsupportedRouterError

# Class: PairedSwapUnsupportedRouterError

Defined in: sdk/errors.ts:1

## Extends

- `Error`

## Constructors

### Constructor

> **new PairedSwapUnsupportedRouterError**(`router`, `cause?`): `PairedSwapUnsupportedRouterError`

Defined in: sdk/errors.ts:2

Parameters

#router

`string`

#cause?

`unknown`

Returns

`PairedSwapUnsupportedRouterError`

Overrides

`Error.constructor`

## Properties

### cause?

> `readonly` `optional` **cause?**: `unknown`

Defined in: sdk/errors.ts:4

***

### router

> `readonly` **router**: `string`

Defined in: sdk/errors.ts:3


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadDynamicAddressFeeSplitManager

# Class: ReadDynamicAddressFeeSplitManager

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:28

## Extended by

- `ReadWriteDynamicAddressFeeSplitManager`

## Constructors

### Constructor

> **new ReadDynamicAddressFeeSplitManager**(`address`, `drift?`): `ReadDynamicAddressFeeSplitManager`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:31

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift` = `...`

Returns

`ReadDynamicAddressFeeSplitManager`

## Properties

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:29

## Methods

### accumulatorPerShare()

> **accumulatorPerShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:66

Returns

`Promise`\<`bigint`\>

***

### allRecipients()

> **allRecipients**(`includeInactive?`): `Promise`\<`` `0x${string}` ``[]\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:117

Parameters

#includeInactive?

`boolean` = `false`

Returns

`Promise`\<`` `0x${string}` ``[]\>

***

### balances()

> **balances**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:90

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### claimableOwnerFees()

> **claimableOwnerFees**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:113

Returns

`Promise`\<`bigint`\>

***

### creatorShare()

> **creatorShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:54

Returns

`Promise`\<`bigint`\>

***

### lastProcessedManagerFees()

> **lastProcessedManagerFees**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:70

Returns

`Promise`\<`bigint`\>

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:46

Returns

`Promise`\<`` `0x${string}` ``\>

***

### moderator()

> **moderator**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:50

Returns

`Promise`\<`` `0x${string}` ``\>

***

### ownerShare()

> **ownerShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:58

Returns

`Promise`\<`bigint`\>

***

### pendingCreatorFees()

> **pendingCreatorFees**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:103

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### pendingOwnerFees()

> **pendingOwnerFees**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:109

Returns

`Promise`\<`bigint`\>

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:42

Returns

`Promise`\<`` `0x${string}` ``\>

***

### recipientAt()

> **recipientAt**(`index`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:78

Parameters

#index

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### recipientCount()

> **recipientCount**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:74

Returns

`Promise`\<`bigint`\>

***

### recipients()

> **recipients**(`recipient`): `Promise`\<`DynamicRecipientInfo`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:84

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`DynamicRecipientInfo`\>

***

### recipientShare()

> **recipientShare**(`recipient`, `data?`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:96

Parameters

#recipient

`` `0x${string}` ``

#data?

`` `0x${string}` `` = `"0x"`

Returns

`Promise`\<`bigint`\>

***

### totalActiveShares()

> **totalActiveShares**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:62

Returns

`Promise`\<`bigint`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadDynamicAddressFeeSplitManagerV1\_3

# Class: ReadDynamicAddressFeeSplitManagerV1\_3

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:33

Client for a v1.3.1 multi-asset DynamicAddressFeeSplitManager instance in read-only mode.

Shares are asset-agnostic; fee accounting (accumulators, pending and claimable fees) is kept
per payout asset: `zeroAddress` is native ETH, otherwise the coin's paired token.

## Extended by

- `ReadWriteDynamicAddressFeeSplitManagerV1_3`

## Constructors

### Constructor

> **new ReadDynamicAddressFeeSplitManagerV1\_3**(`address`, `drift?`): `ReadDynamicAddressFeeSplitManagerV1_3`

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:36

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift` = `...`

Returns

`ReadDynamicAddressFeeSplitManagerV1_3`

## Properties

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:34

## Methods

### accumulatorPerShare()

> **accumulatorPerShare**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:148

Gets the accumulated fees per share in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### allRecipients()

> **allRecipients**(`includeInactive?`): `Promise`\<`` `0x${string}` ``[]\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:173

Lists the recipients, active ones only unless `includeInactive` is set

Parameters

#includeInactive?

`boolean` = `false`

Returns

`Promise`\<`` `0x${string}` ``[]\>

***

### balances()

> **balances**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:114

Gets the claimable balance of one payout asset for a recipient

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

***

### balancesByAsset()

> **balancesByAsset**(`recipient`, `assets?`): `Promise`\<`AssetBalance`[]\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:131

Gets the claimable balances across payout assets (zero balances kept)

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

Returns

`Promise`\<`AssetBalance`[]\>

***

### balancesETH()

> **balancesETH**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:122

Gets the claimable native ETH balance for a recipient (the `address(0)` bucket)

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### claimableOwnerFees()

> **claimableOwnerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:166

Gets the owner fees claimable in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### creatorShare()

> **creatorShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:59

Returns

`Promise`\<`bigint`\>

***

### managerFees()

> **managerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:143

Gets the manager-wide fees received in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:51

Returns

`Promise`\<`` `0x${string}` ``\>

***

### moderator()

> **moderator**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:55

Returns

`Promise`\<`` `0x${string}` ``\>

***

### ownerShare()

> **ownerShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:63

Returns

`Promise`\<`bigint`\>

***

### payoutAssets()

> **payoutAssets**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:84

Gets every payout asset the manager has ever been credited in (`zeroAddress` = native ETH)

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

***

### pendingCreatorFees()

> **pendingCreatorFees**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:153

Gets the creator fees pending for a recipient in one payout asset

Parameters

#recipient

`` `0x${string}` ``

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### pendingOwnerFees()

> **pendingOwnerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:161

Gets the owner fees pending in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:47

Returns

`Promise`\<`` `0x${string}` ``\>

***

### recipientAt()

> **recipientAt**(`index`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:75

Parameters

#index

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### recipientCount()

> **recipientCount**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:71

Returns

`Promise`\<`bigint`\>

***

### recipientInfo()

> **recipientInfo**(`recipient`, `asset`): `Promise`\<`DynamicRecipientInfoV1_3`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:93

Gets a recipient's split position in one payout asset. `share` is the same in every asset.

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`DynamicRecipientInfoV1_3`\>

***

### totalActiveShares()

> **totalActiveShares**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:67

Returns

`Promise`\<`bigint`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadFeeEscrowV1\_3

# Class: ReadFeeEscrowV1\_3

Defined in: clients/FeeEscrowV1\_3Client.ts:28

Client for the v1.3.1 multi-token FeeEscrow in read-only mode.

One escrow per chain serves every paired token — flETH, native ETH (`address(0)`) and the
registry's ERC20 pairings such as the B20 equities. Balances are keyed (recipient, token) and
denominated in that token, so callers name the tokens they are interested in: the contract
cannot enumerate a recipient's keys.

## Extended by

- `ReadWriteFeeEscrowV1_3`

## Constructors

### Constructor

> **new ReadFeeEscrowV1\_3**(`address`, `drift?`): `ReadFeeEscrowV1_3`

Defined in: clients/FeeEscrowV1\_3Client.ts:37

Creates a new ReadFeeEscrowV1_3 instance

Parameters

#address

`` `0x${string}` ``

The address of the multi-token FeeEscrow contract

#drift?

`Drift` = `...`

Optional drift instance for contract interactions (creates new instance if not provided)

Returns

`ReadFeeEscrowV1_3`

Throws

Error if address is not provided

## Properties

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/FeeEscrowV1\_3Client.ts:29

## Methods

### balances()

> **balances**(`recipient`, `token`): `Promise`\<`bigint`\>

Defined in: clients/FeeEscrowV1\_3Client.ts:54

Gets the claimable balance of one escrow token for a recipient

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#token

`` `0x${string}` ``

The escrow token (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the token's raw units

***

### balancesForTokens()

> **balancesForTokens**(`recipient`, `tokens`): `Promise`\<`EscrowTokenBalance`[]\>

Defined in: clients/FeeEscrowV1\_3Client.ts:68

Gets the claimable balances across several escrow tokens. Zero balances are kept so callers
can decide what to sweep.

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#tokens

`` `0x${string}` ``[]

The escrow tokens to read

Returns

`Promise`\<`EscrowTokenBalance`[]\>

Promise<EscrowTokenBalance[]> - One entry per token, in the order given

***

### pairedTokenRegistry()

> **pairedTokenRegistry**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FeeEscrowV1\_3Client.ts:95

Gets the PairedTokenRegistry that decides each token's unwrap policy on withdrawal

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The registry address

***

### totalFeesAllocated()

> **totalFeesAllocated**(`poolId`, `token`): `Promise`\<`bigint`\>

Defined in: clients/FeeEscrowV1\_3Client.ts:84

Gets the total fees a pool has accrued in one escrow token

Parameters

#poolId

`` `0x${string}` ``

The pool to check

#token

`` `0x${string}` ``

The escrow token

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total allocated, in the token's raw units


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadFlaunchManagerZapV1\_3

# Class: ReadFlaunchManagerZapV1\_3

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:43

Client for the v1.3.1 FlaunchManagerZap in read-only mode.

The zap deploys and initializes a manager through the v1.3.1 TreasuryManagerFactory in one
call, sets its permissions and hands ownership to the chosen owner. Flaunching a coin straight
into a manager stays with the core FlaunchZap.

## Extended by

- `ReadWriteFlaunchManagerZapV1_3`

## Constructors

### Constructor

> **new ReadFlaunchManagerZapV1\_3**(`chainId`, `address`, `drift?`): `ReadFlaunchManagerZapV1_3`

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:54

Creates a new ReadFlaunchManagerZapV1_3 instance

Parameters

#chainId

`number`

The chain ID of the contract

#address

`` `0x${string}` ``

The address of the FlaunchManagerZap contract

#drift?

`Drift` = `...`

Optional drift instance for contract interactions (creates new instance if not provided)

Returns

`ReadFlaunchManagerZapV1_3`

Throws

Error if address is not provided

## Properties

### chainId

> **chainId**: `number`

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:44

***

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}\]\>

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:45

## Methods

### treasuryManagerFactory()

> **treasuryManagerFactory**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:69

Gets the v1.3.1 TreasuryManagerFactory the zap deploys through

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The factory address


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadFlaunchSDK

# Class: ReadFlaunchSDK

Defined in: sdk/FlaunchSDK.ts:561

## Extended by

- `ReadWriteFlaunchSDK`

## Constructors

### Constructor

> **new ReadFlaunchSDK**(`chainId`, `drift?`, `publicClient?`): `ReadFlaunchSDK`

Defined in: sdk/FlaunchSDK.ts:775

Parameters

#chainId

`number`

#drift?

`Drift` = `...`

#publicClient?

Returns

`ReadFlaunchSDK`

## Properties

### chainId

> `readonly` **chainId**: `number`

Defined in: sdk/FlaunchSDK.ts:563

***

### drift

> `readonly` **drift**: `Drift`

Defined in: sdk/FlaunchSDK.ts:562

***

### publicClient

> `readonly` **publicClient**: \{ \} \| `undefined`

Defined in: sdk/FlaunchSDK.ts:564

***

### readFeeEscrow

> `readonly` **readFeeEscrow**: `ReadFeeEscrow`

Defined in: sdk/FlaunchSDK.ts:568

***

### resolveIPFS

> **resolveIPFS**: (`value`) => `string`

Defined in: sdk/FlaunchSDK.ts:580

Parameters

#value

`string`

Returns

`string`

***

### TICK\_SPACING

> `readonly` **TICK\_SPACING**: `60`

Defined in: sdk/FlaunchSDK.ts:565

## Accessors

### readAnyBidWall

Get Signature

> **get** **readAnyBidWall**(): `AnyBidWall`

Defined in: sdk/FlaunchSDK.ts:746

#Returns

`AnyBidWall`

***

### readAnyFlaunch

Get Signature

> **get** **readAnyFlaunch**(): `ReadAnyFlaunch`

Defined in: sdk/FlaunchSDK.ts:765

#Returns

`ReadAnyFlaunch`

***

### readAnyPositionManager

Get Signature

> **get** **readAnyPositionManager**(): `ReadAnyPositionManager`

Defined in: sdk/FlaunchSDK.ts:709

#Returns

`ReadAnyPositionManager`

***

### readBidWall

Get Signature

> **get** **readBidWall**(): `ReadBidWall`

Defined in: sdk/FlaunchSDK.ts:733

#Returns

`ReadBidWall`

***

### readBidWallV1\_1

Get Signature

> **get** **readBidWallV1\_1**(): `ReadBidWallV1_1`

Defined in: sdk/FlaunchSDK.ts:736

#Returns

`ReadBidWallV1_1`

***

### readBidWallV1\_3

Get Signature

> **get** **readBidWallV1\_3**(): `ReadBidWallV1_1` \| `undefined`

Defined in: sdk/FlaunchSDK.ts:743

#Returns

`ReadBidWallV1_1` \| `undefined`

***

### readFairLaunch

Get Signature

> **get** **readFairLaunch**(): `ReadFairLaunch`

Defined in: sdk/FlaunchSDK.ts:727

#Returns

`ReadFairLaunch`

***

### readFairLaunchV1\_1

Get Signature

> **get** **readFairLaunchV1\_1**(): `ReadFairLaunchV1_1`

Defined in: sdk/FlaunchSDK.ts:730

#Returns

`ReadFairLaunchV1_1`

***

### readFeeEscrowV1\_3

Get Signature

> **get** **readFeeEscrowV1\_3**(): `ReadFeeEscrowV1_3`

Defined in: sdk/FlaunchSDK.ts:586

The v1.3.1 multi-token FeeEscrow. Throws on chains without one — gate with
`doesChainSupportMultiTokenFeeEscrow()`.

#Returns

`ReadFeeEscrowV1_3`

***

### readFlaunch

Get Signature

> **get** **readFlaunch**(): `ReadFlaunch`

Defined in: sdk/FlaunchSDK.ts:749

#Returns

`ReadFlaunch`

***

### readFlaunchManagerZapV1\_3

Get Signature

> **get** **readFlaunchManagerZapV1\_3**(): `ReadFlaunchManagerZapV1_3`

Defined in: sdk/FlaunchSDK.ts:599

The v1.3.1 FlaunchManagerZap, which deploys managers of the multi-asset generation.
Throws on chains without one — gate with `doesChainSupportMultiAssetManagers()`.

#Returns

`ReadFlaunchManagerZapV1_3`

***

### readFlaunchV1\_1

Get Signature

> **get** **readFlaunchV1\_1**(): `ReadFlaunchV1_1`

Defined in: sdk/FlaunchSDK.ts:752

#Returns

`ReadFlaunchV1_1`

***

### readFlaunchV1\_2

Get Signature

> **get** **readFlaunchV1\_2**(): `ReadFlaunchV1_2`

Defined in: sdk/FlaunchSDK.ts:755

#Returns

`ReadFlaunchV1_2`

***

### readFlaunchV1\_3

Get Signature

> **get** **readFlaunchV1\_3**(): `ReadFlaunchV1_2` \| `undefined`

Defined in: sdk/FlaunchSDK.ts:762

#Returns

`ReadFlaunchV1_2` \| `undefined`

***

### readFlaunchZap

Get Signature

> **get** **readFlaunchZap**(): `ReadFlaunchZap`

Defined in: sdk/FlaunchSDK.ts:718

#Returns

`ReadFlaunchZap`

***

### readFlaunchZapV1\_3

Get Signature

> **get** **readFlaunchZapV1\_3**(): `ReadFlaunchZapV1_3`

Defined in: sdk/FlaunchSDK.ts:672

#Returns

`ReadFlaunchZapV1_3`

***

### readPairedTokenAcquisition

Get Signature

> **get** **readPairedTokenAcquisition**(): `ReadPairedTokenAcquisition`

Defined in: sdk/FlaunchSDK.ts:1209

Reads for buying a non-ETH paired token from ETH / the USD hub. Gate with `doesChainSupportPairedTokenAcquisition()`.

#Returns

`ReadPairedTokenAcquisition`

***

### readPairedTokenPositionManagerV1\_3

Get Signature

> **get** **readPairedTokenPositionManagerV1\_3**(): `ReadPairedTokenPositionManagerV1_3`

Defined in: sdk/FlaunchSDK.ts:701

The paired-token PositionManager V1.3 (pool keys, paired tokens). Same gate as `readPoolSwapV1_3`.

#Returns

`ReadPairedTokenPositionManagerV1_3`

***

### readPairedTokenRegistryV1\_3

Get Signature

> **get** **readPairedTokenRegistryV1\_3**(): `ReadPairedTokenRegistryV1_3`

Defined in: sdk/FlaunchSDK.ts:680

#Returns

`ReadPairedTokenRegistryV1_3`

***

### readPermit2

Get Signature

> **get** **readPermit2**(): `ReadPermit2`

Defined in: sdk/FlaunchSDK.ts:771

#Returns

`ReadPermit2`

***

### readPoolManager

Get Signature

> **get** **readPoolManager**(): `ReadPoolManager`

Defined in: sdk/FlaunchSDK.ts:721

#Returns

`ReadPoolManager`

***

### readPoolSwapV1\_3

Get Signature

> **get** **readPoolSwapV1\_3**(): `ReadPoolSwapV1_3`

Defined in: sdk/FlaunchSDK.ts:692

The v1.3.1 PoolSwap router. Throws on chains without paired-token swap support — gate with
`doesChainSupportPairedTokenSwap()`.

#Returns

`ReadPoolSwapV1_3`

***

### readPositionManager

Get Signature

> **get** **readPositionManager**(): `ReadFlaunchPositionManager`

Defined in: sdk/FlaunchSDK.ts:660

#Returns

`ReadFlaunchPositionManager`

***

### readPositionManagerV1\_1

Get Signature

> **get** **readPositionManagerV1\_1**(): `ReadFlaunchPositionManagerV1_1`

Defined in: sdk/FlaunchSDK.ts:663

#Returns

`ReadFlaunchPositionManagerV1_1`

***

### readPositionManagerV1\_2

Get Signature

> **get** **readPositionManagerV1\_2**(): `ReadFlaunchPositionManagerV1_2`

Defined in: sdk/FlaunchSDK.ts:666

#Returns

`ReadFlaunchPositionManagerV1_2`

***

### readPositionManagerV1\_3

Get Signature

> **get** **readPositionManagerV1\_3**(): `ReadFlaunchPositionManagerV1_2` \| `undefined`

Defined in: sdk/FlaunchSDK.ts:669

#Returns

`ReadFlaunchPositionManagerV1_2` \| `undefined`

***

### readQuoter

Get Signature

> **get** **readQuoter**(): `ReadQuoter`

Defined in: sdk/FlaunchSDK.ts:768

#Returns

`ReadQuoter`

***

### readReferralEscrow

Get Signature

> **get** **readReferralEscrow**(): `ReadReferralEscrow`

Defined in: sdk/FlaunchSDK.ts:715

#Returns

`ReadReferralEscrow`

***

### readStateView

Get Signature

> **get** **readStateView**(): `ReadStateView`

Defined in: sdk/FlaunchSDK.ts:724

#Returns

`ReadStateView`

***

### readTokenImporter

Get Signature

> **get** **readTokenImporter**(): `ReadTokenImporter`

Defined in: sdk/FlaunchSDK.ts:712

#Returns

`ReadTokenImporter`

***

### readTreasuryManagerFactoryV1\_3

Get Signature

> **get** **readTreasuryManagerFactoryV1\_3**(): `ReadTreasuryManagerFactory`

Defined in: sdk/FlaunchSDK.ts:613

The v1.3.1 TreasuryManagerFactory (multi-asset manager generation), used to resolve
`ManagerDeployed` events from that factory only. Throws on chains without one — gate
with `doesChainSupportMultiAssetManagers()`.

#Returns

`ReadTreasuryManagerFactory`

## Methods

### assertBaseOnlyOperation()

> `protected` **assertBaseOnlyOperation**(`operation`): `void`

Defined in: sdk/FlaunchSDK.ts:642

Parameters

#operation

`string`

Returns

`void`

***

### assertMultiAssetManagersSupported()

> `protected` **assertMultiAssetManagersSupported**(`operation`): `void`

Defined in: sdk/FlaunchSDK.ts:652

The v1.3.1 multi-asset manager generation is Base mainnet only; the `*V1_3` manager
methods refuse to run anywhere it is not deployed rather than sending calls that revert.

Parameters

#operation

`string`

Returns

`void`

***

### assertPairedTokenAcquisitionSupported()

> `protected` **assertPairedTokenAcquisitionSupported**(`operation`): `void`

Defined in: sdk/FlaunchSDK.ts:1200

Parameters

#operation

`string`

Returns

`void`

***

### assertPairedTokenSwapSupported()

> `protected` **assertPairedTokenSwapSupported**(`operation`): `void`

Defined in: sdk/FlaunchSDK.ts:1043

Parameters

#operation

`string`

Returns

`void`

***

### bidWallPosition()

> **bidWallPosition**(`coinAddress`, `version?`): `Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `pendingEth`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:2571

Gets information about the bid wall position for a coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `pendingEth`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Promise<{flETHAmount: bigint, coinAmount: bigint, pendingEth: bigint, tickLower: number, tickUpper: number}> - Bid wall position details

***

### calculateAddLiquidityAmounts()

> **calculateAddLiquidityAmounts**(`params`): `Promise`\<\{ `coinAmount`: `bigint`; `currentTick`: `number`; `ethAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:3421

Parameters

#params

`CalculateAddLiquidityAmountsParams`

Returns

`Promise`\<\{ `coinAmount`: `bigint`; `currentTick`: `number`; `ethAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

***

### calculateAddLiquidityTicks()

> **calculateAddLiquidityTicks**(`__namedParameters`): `Promise`\<\{ `coinDecimals`: `number`; `coinTotalSupply`: `bigint`; `currentTick?`: `number`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:3208

Parameters

#\_\_namedParameters

##coinAddress

`` `0x${string}` ``

##currentMarketCap?

`string`

##liquidityMode

`LiquidityMode`

##maxMarketCap

`string`

##minMarketCap

`string`

Returns

`Promise`\<\{ `coinDecimals`: `number`; `coinTotalSupply`: `bigint`; `currentTick?`: `number`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

***

### calculateCurrentTickFromMarketCap()

> **calculateCurrentTickFromMarketCap**(`currentMarketCap`, `formattedTotalSupplyInDecimals`, `marketContext`): `number` \| `undefined`

Defined in: sdk/FlaunchSDK.ts:3179

Calculates current tick from market cap if provided

Parameters

#currentMarketCap

`string` \| `undefined`

#formattedTotalSupplyInDecimals

`number`

#marketContext

##decimals0

`number`

##decimals1

`number`

##ethUsdPrice

`number`

##isFlethZero

`boolean`

Returns

`number` \| `undefined`

***

### calculatePairedTokenFlaunchFee()

> **calculatePairedTokenFlaunchFee**(`params`): `Promise`\<`PairedTokenFlaunchFee`\>

Defined in: sdk/FlaunchSDK.ts:959

Parameters

#params

`CalculatePairedTokenFlaunchFeeParams`

Returns

`Promise`\<`PairedTokenFlaunchFee`\>

***

### checkSingleSidedAddLiquidity()

> **checkSingleSidedAddLiquidity**(`params`): `Promise`\<`SingleSidedLiquidityInfo`\>

Defined in: sdk/FlaunchSDK.ts:3325

Parameters

#params

`CheckSingleSidedAddLiquidityParams`

Returns

`Promise`\<`SingleSidedLiquidityInfo`\>

***

### coinMarketCapInUSD()

> **coinMarketCapInUSD**(`__namedParameters`): `Promise`\<`string`\>

Defined in: sdk/FlaunchSDK.ts:2255

Parameters

#\_\_namedParameters

##coinAddress

`` `0x${string}` ``

##drift?

`Drift`

##version?

`FlaunchVersion`

Returns

`Promise`\<`string`\>

***

### coinPriceInETH()

> **coinPriceInETH**(`coinAddress`, `version?`): `Promise`\<`string`\>

Defined in: sdk/FlaunchSDK.ts:2215

Calculates the coin price in ETH based on the current tick

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`string`\>

Promise<string> - The price of the coin in ETH with 18 decimals precision

***

### coinPriceInUSD()

> **coinPriceInUSD**(`coinAddress`): `Promise`\<`string`\>

Defined in: sdk/FlaunchSDK.ts:2239

Calculates the coin price in USD based on the current ETH/USDC price

Parameters

#coinAddress

The address of the coin

##coinAddress

`` `0x${string}` ``

##drift?

`Drift`

##version?

`FlaunchVersion`

Returns

`Promise`\<`string`\>

Promise<string> - The price of the coin in USD with 18 decimal precision

***

### convertPriceToTick()

> **convertPriceToTick**(`priceEth`, `isFlethZero`, `decimals0`, `decimals1`): `number`

Defined in: sdk/FlaunchSDK.ts:3161

Converts token price in ETH to tick

Parameters

#priceEth

`number`

#isFlethZero

`boolean`

#decimals0

`number`

#decimals1

`number`

Returns

`number`

***

### createPoolKey()

> `protected` **createPoolKey**(`coinAddress`, `version`): `object`

Defined in: sdk/FlaunchSDK.ts:3676

Creates a pool key for the given coin and version

Parameters

#coinAddress

`` `0x${string}` ``

The coin address

#version

`FlaunchVersion`

The version to use for position manager

Returns

`object`

The ordered pool key

#currency0

> **currency0**: `` `0x${string}` ``

#currency1

> **currency1**: `` `0x${string}` ``

#fee

> **fee**: `number`

#hooks

> **hooks**: `` `0x${string}` ``

#tickSpacing

> **tickSpacing**: `number`

***

### createPoolKeyForCoin()

> `protected` **createPoolKeyForCoin**(`coinAddress`, `version?`): `Promise`\<\{ `currency0`: `` `0x${string}` ``; `currency1`: `` `0x${string}` ``; `fee`: `number`; `hooks`: `` `0x${string}` ``; `tickSpacing`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:3692

`createPoolKey` resolved by coin rather than by version: on a multichain deployment the
hook is probed per coin (see `getPositionManagerAddressForCoin`), so the key matches the
pool the coin was actually created on. The liquidity helpers use this.

Parameters

#coinAddress

`` `0x${string}` ``

#version?

`FlaunchVersion`

Returns

`Promise`\<\{ `currency0`: `` `0x${string}` ``; `currency1`: `` `0x${string}` ``; `fee`: `number`; `hooks`: `` `0x${string}` ``; `tickSpacing`: `number`; \}\>

***

### creatorRevenue()

> **creatorRevenue**(`creator`, `isV1?`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2605

Gets the ETH balance for the creator to claim

Parameters

#creator

`` `0x${string}` ``

The address of the creator to check

#isV1?

`boolean`

Optional boolean to check the balance for V1. V1.1 & AnyPositionManager use the same FeeEscrow contract

Returns

`Promise`\<`bigint`\>

The balance of the creator

***

### creatorRevenueByToken()

> **creatorRevenueByToken**(`params`): `Promise`\<`EscrowTokenBalance`[]\>

Defined in: sdk/FlaunchSDK.ts:2622

Gets the creator's claimable revenue on the v1.3.1 multi-token FeeEscrow, per escrow token.
Coins paired with anything other than flETH (native ETH, the B20 equities, …) escrow their
creator fees there, denominated in the paired token, where `creatorRevenue()` cannot see
them. The escrow cannot enumerate a recipient's keys, so pass the paired tokens to look at.

Parameters

#params

##creator

`` `0x${string}` ``

The address of the creator to check

##tokens

`` `0x${string}` ``[]

The escrow tokens to read (`zeroAddress` for native ETH)

Returns

`Promise`\<`EscrowTokenBalance`[]\>

The claimable amount per token, in that token's raw units (zero balances included)

***

### currentTick()

> **currentTick**(`coinAddress`, `version?`): `Promise`\<`number`\>

Defined in: sdk/FlaunchSDK.ts:2200

Gets the current tick for a given coin's pool

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`number`\>

Promise<number> - The current tick of the pool

***

### determineCoinVersion()

> `protected` **determineCoinVersion**(`coinAddress`, `version?`): `Promise`\<`FlaunchVersion`\>

Defined in: sdk/FlaunchSDK.ts:3652

Determines the version for a coin, using provided version or fetching it

Parameters

#coinAddress

`` `0x${string}` ``

The coin address

#version?

`FlaunchVersion`

Optional version, if not provided will be fetched

Returns

`Promise`\<`FlaunchVersion`\>

The determined version

***

### ethRequiredToFlaunch()

> **ethRequiredToFlaunch**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2908

Calculates the ETH required to flaunch a token, takes into account the ETH for premine and the flaunching fee

Parameters

#params

##initialMarketCapUSD

`number`

The initial market cap in USD

##premineAmount

`bigint`

The amount of coins to be premined

##slippagePercent?

`number`

The slippage percent

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The ETH required to flaunch

***

### fairLaunchCoinOnlyPosition()

> **fairLaunchCoinOnlyPosition**(`coinAddress`, `version?`): `Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:2508

Gets information about the coin-only position in a fair launch

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Promise<{flETHAmount: bigint, coinAmount: bigint, tickLower: number, tickUpper: number}> - Position details

***

### fairLaunchDuration()

> **fairLaunchDuration**(`coinAddress`, `version?`): `Promise`\<`number` \| `bigint`\>

Defined in: sdk/FlaunchSDK.ts:2415

Gets the duration of a fair launch for a given coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`number` \| `bigint`\>

Promise<number> - The duration in seconds (30 minutes for V1, variable for V1.1)

***

### fairLaunchETHOnlyPosition()

> **fairLaunchETHOnlyPosition**(`coinAddress`, `version?`): `Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:2445

Gets information about the ETH-only position in a fair launch

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Promise<{flETHAmount: bigint, coinAmount: bigint, tickLower: number, tickUpper: number}> - Position details

***

### fairLaunchInfo()

> **fairLaunchInfo**(`coinAddress`, `version?`): `Promise`\<\{ `closed`: `boolean`; `endsAt`: `bigint`; `initialTick`: `number`; `revenue`: `bigint`; `startsAt`: `bigint`; `supply`: `bigint`; \}\>

Defined in: sdk/FlaunchSDK.ts:2323

Gets information about a fair launch for a given coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<\{ `closed`: `boolean`; `endsAt`: `bigint`; `initialTick`: `number`; `revenue`: `bigint`; `startsAt`: `bigint`; `supply`: `bigint`; \}\>

Fair launch information from the appropriate contract version

***

### flETHIsCurrencyZero()

> **flETHIsCurrencyZero**(`coinAddress`): `boolean`

Defined in: sdk/FlaunchSDK.ts:3050

Determines if flETH is currency0 in the pool

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

Returns

`boolean`

boolean - True if flETH is currency0, false otherwise

***

### getBidWall()

> **getBidWall**(`version`): `ReadBidWall` \| `AnyBidWall` \| `ReadBidWallV1_1`

Defined in: sdk/FlaunchSDK.ts:1705

Gets the bid wall instance for a given version

Parameters

#version

`FlaunchVersion`

The version to get the bid wall instance for

Returns

`ReadBidWall` \| `AnyBidWall` \| `ReadBidWallV1_1`

***

### getBidWallAddress()

> **getBidWallAddress**(`version`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1762

Parameters

#version

`FlaunchVersion`

Returns

`` `0x${string}` ``

***

### getBuyQuoteExactInput()

> **getBuyQuoteExactInput**(`coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2975

Gets a quote for buying tokens with an exact amount of ETH or inputToken

Parameters

#coinAddress

The address of the token to buy

##amountIn

`bigint`

##coinAddress

`` `0x${string}` ``

##hookData?

`` `0x${string}` ``

##intermediatePoolKey?

`PoolWithHookData`

##userWallet?

`` `0x${string}` ``

##version?

`FlaunchVersion`

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The expected amount of coins to receive

***

### getBuyQuoteExactOutput()

> **getBuyQuoteExactOutput**(`coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:3015

Gets a quote for buying an exact amount of tokens with ETH or inputToken

Parameters

#coinAddress

The address of the token to buy

##amountOut

`bigint`

##coinAddress

`` `0x${string}` ``

##hookData?

`` `0x${string}` ``

##intermediatePoolKey?

`PoolWithHookData`

##userWallet?

`` `0x${string}` ``

##version?

`FlaunchVersion`

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The required amount of ETH or inputToken to spend

***

### getCoinInfo()

> **getCoinInfo**(`coinAddress`): `Promise`\<\{ `decimals`: `number`; `formattedTotalSupplyInDecimals`: `number`; `totalSupply`: `bigint`; \}\>

Defined in: sdk/FlaunchSDK.ts:3102

Gets basic coin information (total supply and decimals)

Parameters

#coinAddress

`` `0x${string}` ``

Returns

`Promise`\<\{ `decimals`: `number`; `formattedTotalSupplyInDecimals`: `number`; `totalSupply`: `bigint`; \}\>

***

### getCoinMetadata()

> **getCoinMetadata**(`coinAddress`): `Promise`\<`CoinMetadata` & `object`\>

Defined in: sdk/FlaunchSDK.ts:1789

Retrieves metadata for a given Flaunch coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

Returns

`Promise`\<`CoinMetadata` & `object`\>

Promise<CoinMetadata & { symbol: string }> - The coin's metadata including name, symbol, description, and social links

***

### getCoinMetadataFromTokenId()

> **getCoinMetadataFromTokenId**(`flaunch`, `tokenId`): `Promise`\<`CoinMetadata` & `object`\>

Defined in: sdk/FlaunchSDK.ts:1819

Retrieves metadata for a given Flaunch coin using its token ID & Flaunch contract address

Parameters

#flaunch

`` `0x${string}` ``

The address of the Flaunch contract

#tokenId

`bigint`

The token ID of the coin

Returns

`Promise`\<`CoinMetadata` & `object`\>

The coin's metadata including name, symbol, description, and social links

***

### getCoinMetadataFromTokenIds()

> **getCoinMetadataFromTokenIds**(`params`, `batchSize?`, `batchDelay?`): `Promise`\<`object`[]\>

Defined in: sdk/FlaunchSDK.ts:1835

Retrieves metadata for multiple Flaunch coins using their token IDs & Flaunch contract addresses

Parameters

#params

`object`[]

An array of objects containing flaunch contract address and token ID

#batchSize?

`number` = `9`

Optional, the number of ipfs requests to process in each batch

#batchDelay?

`number` = `500`

Optional, the delay in milliseconds between batches

Returns

`Promise`\<`object`[]\>

An array of objects containing coin address, name, symbol, description, and social links

***

### getCoinVersion()

> **getCoinVersion**(`coinAddress`): `Promise`\<`FlaunchVersion`\>

Defined in: sdk/FlaunchSDK.ts:1634

Determines the version of a Flaunch coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to check

Returns

`Promise`\<`FlaunchVersion`\>

Promise<FlaunchVersion> - The version of the coin

***

### getETHUSDCPrice()

> **getETHUSDCPrice**(`drift?`): `Promise`\<`number`\>

Defined in: sdk/FlaunchSDK.ts:2278

Gets the current ETH/USDC price

Parameters

#drift?

`Drift`

Optional drift instance to get price from Base Mainnet

Returns

`Promise`\<`number`\>

Promise<number> - The current ETH/USDC price

***

### getFairLaunch()

> **getFairLaunch**(`version`): `ReadFairLaunch` \| `ReadFairLaunchV1_1`

Defined in: sdk/FlaunchSDK.ts:1684

Gets the fair launch instance for a given version

Parameters

#version

`FlaunchVersion`

The version to get the fair launch instance for

Returns

`ReadFairLaunch` \| `ReadFairLaunchV1_1`

***

### getFairLaunchAddress()

> **getFairLaunchAddress**(`version`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1758

Parameters

#version

`FlaunchVersion`

Returns

`` `0x${string}` ``

***

### getFlaunch()

> **getFlaunch**(`version`): `ReadFlaunch` \| `ReadAnyFlaunch` \| `ReadFlaunchV1_1` \| `ReadFlaunchV1_2`

Defined in: sdk/FlaunchSDK.ts:1726

Gets the flaunch contract instance for a given version

Parameters

#version

`FlaunchVersion`

The version to get the flaunch contract instance for

Returns

`ReadFlaunch` \| `ReadAnyFlaunch` \| `ReadFlaunchV1_1` \| `ReadFlaunchV1_2`

***

### getFlaunchAddress()

> **getFlaunchAddress**(`version`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1747

Gets the flaunch contract address for a given version

Parameters

#version

`FlaunchVersion`

The version to get the flaunch contract address for

Returns

`` `0x${string}` ``

***

### getFlaunchingFee()

> **getFlaunchingFee**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2876

Gets the flaunching fee for a given initial price and slippage percent

Parameters

#params

##initialMarketCapUSD

`number`

The initial market cap in USD

##sender

`` `0x${string}` ``

The address of the sender

##slippagePercent?

`number`

The slippage percent

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The flaunching fee

***

### getFlaunchTokenIdForMemecoin()

> **getFlaunchTokenIdForMemecoin**(`coinAddress`): `Promise`\<\{ `flaunchAddress`: `` `0x${string}` ``; `tokenId`: `bigint`; \}\>

Defined in: sdk/FlaunchSDK.ts:1771

Gets the flaunch contract address and token ID for a memecoin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the memecoin

Returns

`Promise`\<\{ `flaunchAddress`: `` `0x${string}` ``; `tokenId`: `bigint`; \}\>

Promise<{ flaunchAddress: Address; tokenId: bigint }> - The flaunch contract address and token ID

***

### getMarketContext()

> **getMarketContext**(`coinAddress`, `coinDecimals`): `Promise`\<\{ `decimals0`: `number`; `decimals1`: `number`; `ethUsdPrice`: `number`; `isFlethZero`: `boolean`; \}\>

Defined in: sdk/FlaunchSDK.ts:3121

Gets market context information needed for tick calculations

Parameters

#coinAddress

`` `0x${string}` ``

#coinDecimals

`number`

Returns

`Promise`\<\{ `decimals0`: `number`; `decimals1`: `number`; `ethUsdPrice`: `number`; `isFlethZero`: `boolean`; \}\>

***

### getPairedPoolQuoteExactInput()

> **getPairedPoolQuoteExactInput**(`__namedParameters`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:1135

Expected output of an exact-input swap on a paired-token pool, via the v4 Quoter's single-hop
quote (hook fees included). A buy spends the paired token for the coin; a sell the reverse.

Parameters

#\_\_namedParameters

`PairedPoolQuoteParams`

Returns

`Promise`\<`bigint`\>

***

### getPairedSwapFillFromLogs()

> **getPairedSwapFillFromLogs**(`logs`, `plan`): `PairedSwapFill` \| `null`

Defined in: sdk/FlaunchSDK.ts:1554

Decode one attributable execution. Multiple matching fills are ambiguous, never silently combined.

Parameters

#logs

readonly `Pick`\<`Log`, `"address"` \| `"data"` \| `"topics"`\>[]

#plan

`PairedSwapPlan`

Returns

`PairedSwapFill` \| `null`

***

### getPairedSwapFillFromTx()

> **getPairedSwapFillFromTx**(`txHash`, `plan`): `Promise`\<`PairedSwapFill` \| `null`\>

Defined in: sdk/FlaunchSDK.ts:1593

Parameters

#txHash

`` `0x${string}` ``

#plan

`PairedSwapPlan`

Returns

`Promise`\<`PairedSwapFill` \| `null`\>

***

### getPoolCreatedFromLogs()

> **getPoolCreatedFromLogs**(`logs`): `PoolCreatedEventData` \| `null`

Defined in: sdk/FlaunchSDK.ts:1994

Parses PoolCreated from logs emitted by a PositionManager on this chain.

Parameters

#logs

readonly `Log`[]

Returns

`PoolCreatedEventData` \| `null`

***

### getPoolCreatedFromTx()

> **getPoolCreatedFromTx**(`txHash`): `Promise`\<`PoolCreatedEventData` \| `null`\>

Defined in: sdk/FlaunchSDK.ts:2122

Parses a transaction to extract PoolCreated event data

Parameters

#txHash

`` `0x${string}` ``

The transaction hash to parse

Returns

`Promise`\<`PoolCreatedEventData` \| `null`\>

PoolCreated event parameters or null if not found

***

### getPositionManager()

> **getPositionManager**(`version`): `ReadFlaunchPositionManager` \| `ReadFlaunchPositionManagerV1_1` \| `ReadFlaunchPositionManagerV1_2` \| `ReadAnyPositionManager`

Defined in: sdk/FlaunchSDK.ts:1663

Gets the position manager instance for a given version

Parameters

#version

`FlaunchVersion`

The version to get the position manager instance for

Returns

`ReadFlaunchPositionManager` \| `ReadFlaunchPositionManagerV1_1` \| `ReadFlaunchPositionManagerV1_2` \| `ReadAnyPositionManager`

***

### getPositionManagerAddress()

> **getPositionManagerAddress**(`version`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1751

Parameters

#version

`FlaunchVersion`

Returns

`` `0x${string}` ``

***

### getPositionManagerAddressForCoin()

> **getPositionManagerAddressForCoin**(`coinAddress`, `version?`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:1023

The hook (PositionManager) a coin's pool lives on. On Base this is the position manager
for the coin's version; on a multichain deployment it is probed per coin (see
`probeMultichainCoinHook`), falling back to the chain's multichain hook for an unknown coin.

Parameters

#coinAddress

`` `0x${string}` ``

The coin to resolve

#version?

`FlaunchVersion`

Optional version override (skips detection on Base; on multichain a
  non-v1.3 version short-circuits to the multichain hook)

Returns

`Promise`\<`` `0x${string}` ``\>

***

### getSellQuoteExactInput()

> **getSellQuoteExactInput**(`coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2941

Gets a quote for selling an exact amount of tokens for ETH

Parameters

#coinAddress

The address of the token to sell

##amountIn

`bigint`

##coinAddress

`` `0x${string}` ``

##intermediatePoolKey?

`PoolWithHookData`

##version?

`FlaunchVersion`

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The expected amount of ETH to receive

***

### initialSqrtPriceX96()

> **initialSqrtPriceX96**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2288

Parameters

#params

##coinAddress

`` `0x${string}` ``

##initialMarketCapUSD

`number`

Returns

`Promise`\<`bigint`\>

***

### initialTick()

> **initialTick**(`coinAddress`, `version?`): `Promise`\<`number`\>

Defined in: sdk/FlaunchSDK.ts:2428

Gets the initial tick for a fair launch

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`number`\>

Promise<number> - The initial tick value

***

### isFairLaunchActive()

> **isFairLaunchActive**(`coinAddress`, `version?`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:2336

Checks if a fair launch is currently active for a given coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`boolean`\>

Promise<boolean> - True if fair launch is active, false otherwise

***

### isFlaunchTokenApprovedForAll()

> **isFlaunchTokenApprovedForAll**(`version`, `owner`, `operator`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:3631

Checks if an operator is approved for all flaunch tokens of an owner

Parameters

#version

`FlaunchVersion`

The flaunch version to determine the correct contract address

#owner

`` `0x${string}` ``

The owner address to check

#operator

`` `0x${string}` ``

The operator address to check

Returns

`Promise`\<`boolean`\>

Promise<boolean> - True if operator is approved for all tokens

***

### isMemecoinImported()

> **isMemecoinImported**(`memecoin`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:3607

Checks if an external memecoin has been imported to Flaunch

Parameters

#memecoin

`` `0x${string}` ``

The address of the memecoin to check

Returns

`Promise`\<`boolean`\>

Promise<boolean> - True if the memecoin has been imported

***

### isPairedTokenApproved()

> **isPairedTokenApproved**(`token`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:955

Parameters

#token

`` `0x${string}` ``

Returns

`Promise`\<`boolean`\>

***

### isValidCoin()

> **isValidCoin**(`coinAddress`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:1614

Checks if a given coin address is a valid Flaunch coin (supports all versions)

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to check

Returns

`Promise`\<`boolean`\>

Promise<boolean> - True if the coin is valid, false otherwise

***

### locatePairedPool()

> `protected` **locatePairedPool**(`coinAddress`): `Promise`\<\{ `hook`: `` `0x${string}` ``; `poolKey`: `PoolKey`; \} \| `null`\>

Defined in: sdk/FlaunchSDK.ts:1074

Which v1.3 hook a coin's paired pool lives on, and its key.

A chain can carry more than one v1.3 hook generation — on Robinhood the 2026-08-21 v1.3.1
hooks were regenerated as v1.3.3 and both keep serving their pools — so the hook is probed per
coin (`poolKey(coin)` on the current paired PositionManager, then each superseded one) rather
than taken from the chain's current default: a key built on the wrong hook names a pool that
does not exist, and the swap reverts. A hook that never launched the coin answers a zeroed key
(or reverts, for the AnyPositionManager in that list); the first real key wins. Memoised per
coin.

Parameters

#coinAddress

`` `0x${string}` ``

Returns

`Promise`\<\{ `hook`: `` `0x${string}` ``; `poolKey`: `PoolKey`; \} \| `null`\>

The hook and pool key, or null when no v1.3 hook on this chain knows the coin.

***

### marketCapToTokenPriceEth()

> **marketCapToTokenPriceEth**(`marketCapUsd`, `formattedTotalSupplyInDecimals`, `ethUsdPrice`): `number`

Defined in: sdk/FlaunchSDK.ts:3149

Converts market cap in USD to token price in ETH

Parameters

#marketCapUsd

`number`

#formattedTotalSupplyInDecimals

`number`

#ethUsdPrice

`number`

Returns

`number`

***

### pairedTokenPositionManagerAt()

> `protected` **pairedTokenPositionManagerAt**(`hook`): `ReadPairedTokenPositionManagerV1_3`

Defined in: sdk/FlaunchSDK.ts:1052

The paired-token PositionManager client for a given hook address, memoised per hook.

Parameters

#hook

`` `0x${string}` ``

Returns

`ReadPairedTokenPositionManagerV1_3`

***

### parseSwapTx()

> **parseSwapTx**\<`T`\>(`params`): `Promise`\<`T` *extends* `boolean` ? `GenericBuySwapLog` \| `GenericSellSwapLog` \| `undefined` : `GenericBaseSwapLog` \| `undefined`\>

Defined in: sdk/FlaunchSDK.ts:3074

Parses a transaction hash to extract PoolSwap events and return parsed swap data

Type Parameters

#T

`T` *extends* `boolean` \| `undefined` = `undefined`

Parameters

#params

Object containing parsing parameters

##flETHIsCurrencyZero?

`T`

Whether flETH is currency 0 in the pool (optional)

##txHash

`` `0x${string}` ``

The transaction hash to parse

##version

`FlaunchVersion`

The Flaunch version to use for parsing

Returns

`Promise`\<`T` *extends* `boolean` ? `GenericBuySwapLog` \| `GenericSellSwapLog` \| `undefined` : `GenericBaseSwapLog` \| `undefined`\>

Parsed swap log or undefined if no PoolSwap event found.
         If flETHIsCurrencyZero is provided, returns typed swap data with BUY/SELL information.
         If flETHIsCurrencyZero is undefined, returns basic swap log without parsed delta.

***

### planPairedTokenAcquisition()

> **planPairedTokenAcquisition**(`params`): `Promise`\<`PairedTokenAcquisitionPlan`\>

Defined in: sdk/FlaunchSDK.ts:1234

The calls that buy exactly `target` of a paired token from ETH or the USD hub: an optional
hub-token approve to the venue's router, then the exact-output router call (ETH rides as
`value = maxIn`; the router refunds the unspent part). Exact-output so a following PoolSwap leg
can be encoded up front — batched wallets resolve every call before the first executes.

Parameters

#params

`PairedTokenAcquisitionParams`

Returns

`Promise`\<`PairedTokenAcquisitionPlan`\>

***

### planPairedTokenAcquisitionForBudget()

> **planPairedTokenAcquisitionForBudget**(`params`): `Promise`\<`PairedTokenAcquisitionPlan` & `object`\>

Defined in: sdk/FlaunchSDK.ts:1282

Sizes and plans a routed buy from a BUDGET: quotes the exact input, takes `slippageBps` off, and
plans an exact-output buy of that much with the whole budget as the price protection — the
pattern that keeps a downstream PoolSwap leg deterministic. Returns the plan and the target.

Parameters

#params

##amountIn

`bigint`

##deadline?

`bigint`

##input

`PairedTokenAcquisitionInput`

##pairedToken

`` `0x${string}` ``

##recipient

`` `0x${string}` ``

##sender?

`` `0x${string}` ``

##slippageBps

`number`

Returns

`Promise`\<`PairedTokenAcquisitionPlan` & `object`\>

***

### planPairedTokenApproval()

> **planPairedTokenApproval**(`params`): `Promise`\<`PairedSwapApproveCall` \| `undefined`\>

Defined in: sdk/FlaunchSDK.ts:1183

Just the ERC20 approve a paired-token BUY needs, sized to `amount` — for a host that wants the
player ready before a round starts (approve the whole wallet cap once; every in-round buy is
then a single swap). `undefined` when the pool is paired with native ETH (nothing to approve)
or the standing allowance already covers `amount`.

Parameters

#params

`PairedTokenApprovalParams`

Returns

`Promise`\<`PairedSwapApproveCall` \| `undefined`\>

***

### planPairedTokenSwap()

> **planPairedTokenSwap**(`params`): `Promise`\<`PairedSwapPlan`\>

Defined in: sdk/FlaunchSDK.ts:1319

Builds one quote-backed exact-input plan. Its displayed output and encoded minimum share
the same quote; slot0 and quote reads use the same block. Requires a protected router.
The optional approval precedes swapExactInput. The router enforces full consumption,
minimum net output and expiry at inclusion; legacy deployments fail closed.

Parameters

#params

`PairedSwapPlanParams`

Returns

`Promise`\<`PairedSwapPlan`\>

***

### pollPoolCreatedNow()

> **pollPoolCreatedNow**(`version?`): `Promise`\<`void`\> \| `undefined`

Defined in: sdk/FlaunchSDK.ts:1979

Polls for current pool creation events

Parameters

#version?

`FlaunchVersion` = `FlaunchVersion.V1_1`

Version of Flaunch to use (defaults to V1_1)

Returns

`Promise`\<`void`\> \| `undefined`

Current pool creation events or undefined if polling is not available

***

### pollPoolSwapNow()

> **pollPoolSwapNow**(`version?`): `Promise`\<`void`\> \| `undefined`

Defined in: sdk/FlaunchSDK.ts:2171

Polls for current pool swap events

Parameters

#version?

`FlaunchVersion` = `FlaunchVersion.V1_1`

Version of Flaunch to use (defaults to V1_1)

Returns

`Promise`\<`void`\> \| `undefined`

Current pool swap events or undefined if polling is not available

***

### poolId()

> **poolId**(`coinAddress`, `version?`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:2852

Gets the pool ID for a given coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<string> - The pool ID

***

### positionInfo()

> **positionInfo**(`params`): `Promise`\<\{ `feeGrowthInside0LastX128`: `bigint`; `feeGrowthInside1LastX128`: `bigint`; `liquidity`: `bigint`; \} \| \{ `feeGrowthInside0LastX128`: `bigint`; `feeGrowthInside1LastX128`: `bigint`; `liquidity`: `bigint`; \}\>

Defined in: sdk/FlaunchSDK.ts:2190

Gets information about a liquidity position

Parameters

#params

`PositionInfoParams`

Parameters for querying position info

Returns

`Promise`\<\{ `feeGrowthInside0LastX128`: `bigint`; `feeGrowthInside1LastX128`: `bigint`; `liquidity`: `bigint`; \} \| \{ `feeGrowthInside0LastX128`: `bigint`; `feeGrowthInside1LastX128`: `bigint`; `liquidity`: `bigint`; \}\>

Position information from the state view contract

***

### probeMultichainCoinHook()

> `protected` **probeMultichainCoinHook**(`coinAddress`): `Promise`\<\{ `hook`: `` `0x${string}` ``; `version`: `FlaunchVersion`; \} \| `null`\>

Defined in: sdk/FlaunchSDK.ts:979

On a multichain deployment a chain can serve several hook generations at once (Robinhood:
the v1.2 multichain hook, the 2026-08-21 v1.3.1 hooks and the v1.3.3 regeneration). Every
pool key — and so every quote and swap — derives from the hook the coin was created on, so
it has to be probed per coin rather than taken from the chain's current default. Probes the
current and superseded v1.3 hooks first, then the multichain (v1.2) hook.

Parameters

#coinAddress

`` `0x${string}` ``

Returns

`Promise`\<\{ `hook`: `` `0x${string}` ``; `version`: `FlaunchVersion`; \} \| `null`\>

The coin's hook and version, or null when no hook on this chain knows the coin.

***

### quotePairedTokenAcquisition()

> **quotePairedTokenAcquisition**(`params`): `Promise`\<`PairedTokenAcquisitionQuote`\>

Defined in: sdk/FlaunchSDK.ts:1218

Expected paired-token output (and the venue) for an exact ETH / hub-token input.

Parameters

#params

##amountIn

`bigint`

##input

`PairedTokenAcquisitionInput`

##pairedToken

`` `0x${string}` ``

Returns

`Promise`\<`PairedTokenAcquisitionQuote`\>

***

### referralBalance()

> **referralBalance**(`recipient`, `coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2638

Gets the balance of a recipient for a given coin

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#coinAddress

`` `0x${string}` ``

The address of the coin

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The balance of the recipient

***

### resolvePairedPool()

> **resolvePairedPool**(`coinAddress`, `pairedToken?`): `Promise`\<`ResolvedPairedPool`\>

Defined in: sdk/FlaunchSDK.ts:1104

The pool a paired-token coin trades on: its full key, id and paired side, read from the hook
the coin was launched on (see `locatePairedPool` — one `poolKey` read per hook probed, then
memoised). `pairedToken`, when given, is checked against the pool's real paired side rather
than trusted: building a key from a caller's belief about the pairing is how a swap ends up
aimed at a pool that does not exist.

Parameters

#coinAddress

`` `0x${string}` ``

#pairedToken?

`` `0x${string}` ``

Returns

`Promise`\<`ResolvedPairedPool`\>

Throws

when no v1.3 hook on this chain launched the coin, or `pairedToken` disagrees with the pool

***

### revenueManagerAllTokensByCreator()

> **revenueManagerAllTokensByCreator**(`params`): `Promise`\<readonly `object`[]\>

Defined in: sdk/FlaunchSDK.ts:2698

Gets all tokens created by a specific creator address

Parameters

#params

Parameters for querying tokens by creator

##creator

`` `0x${string}` ``

The address of the creator to query tokens for

##revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

##sortByDesc?

`boolean`

Whether to sort the tokens by descending order

Returns

`Promise`\<readonly `object`[]\>

Promise<Array<{flaunch: Address, tokenId: bigint}>> - Array of token objects containing flaunch address and token ID

***

### revenueManagerAllTokensInManager()

> **revenueManagerAllTokensInManager**(`params`): `Promise`\<`object`[]\>

Defined in: sdk/FlaunchSDK.ts:2721

Gets all tokens currently managed by a revenue manager

Parameters

#params

Parameters for querying tokens in manager

##revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

##sortByDesc?

`boolean`

Optional boolean to sort tokens in descending order (default: false)

Returns

`Promise`\<`object`[]\>

Promise<Array<{flaunch: Address, tokenId: bigint}>> - Array of token objects containing flaunch address and token ID

***

### revenueManagerBalance()

> **revenueManagerBalance**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2649

Gets the claimable balance of ETH for the recipient from a revenue manager

Parameters

#params

Parameters for checking the balance

##recipient

`` `0x${string}` ``

The address of the recipient to check

##revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance of ETH

***

### revenueManagerBalanceV1\_3()

> **revenueManagerBalanceV1\_3**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2767

Gets the claimable balance of one payout asset for a recipient on a v1.3.1 multi-asset
RevenueManager. Managers of that generation keep a balance per payout asset — native ETH
(`zeroAddress`) for flETH / native pools, otherwise the coin's paired token (a wrapper's
underlying, or a B20 stock as itself) — so a stock-paired coin's fees do not show up under
ETH. `revenueManagerBalance()` only speaks to the previous generation.

Parameters

#params

##asset?

`` `0x${string}` ``

The payout asset to read. Defaults to native ETH

##recipient

`` `0x${string}` ``

The address of the recipient to check

##revenueManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the asset's raw units

***

### revenueManagerPayoutAssets()

> **revenueManagerPayoutAssets**(`revenueManagerAddress`): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: sdk/FlaunchSDK.ts:2790

Gets every payout asset a v1.3.1 revenue manager has ever been credited in. Native ETH
(`zeroAddress`) is always present; other entries come from the paired tokens of the coins
deposited into it. Use it to know which assets to read or claim.

Parameters

#revenueManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 revenue manager

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Promise<readonly Address[]> - The payout assets

***

### revenueManagerProtocolBalance()

> **revenueManagerProtocolBalance**(`revenueManagerAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2666

Gets the claimable balance of ETH for the protocol from a revenue manager

Parameters

#revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance of ETH

***

### revenueManagerProtocolBalanceV1\_3()

> **revenueManagerProtocolBalanceV1\_3**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2805

Gets the claimable balance of one payout asset for the protocol on a v1.3.1 revenue manager

Parameters

#params

##asset?

`` `0x${string}` ``

The payout asset to read. Defaults to native ETH

##revenueManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the asset's raw units

***

### revenueManagerTokensCount()

> **revenueManagerTokensCount**(`revenueManagerAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2681

Gets the total number of tokens managed by a revenue manager

Parameters

#revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total count of tokens

***

### routerForPool()

> `protected` **routerForPool**(`hook`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1162

The PoolSwap a pool on `hook` must be traded through. Router approval is per spend gate and
each hook generation ships its own gate, so a gated buy sent through the wrong generation's
router is refused on chain — see `PoolSwapForHookV1_3Address`.

Parameters

#hook

`` `0x${string}` ``

Returns

`` `0x${string}` ``

***

### setIPFSResolver()

> **setIPFSResolver**(`resolverFn`): `void`

Defined in: sdk/FlaunchSDK.ts:3060

Sets a custom IPFS resolver function

Parameters

#resolverFn

(`ipfsHash`) => `string`

Custom function to resolve IPFS URIs

Returns

`void`

Dev

this is used to resolve IPFS hash to a gateway URL
eg: input: Qabc, output: https://ipfs.io/ipfs/Qabc

***

### tokenImporterVerifyMemecoin()

> **tokenImporterVerifyMemecoin**(`memecoin`): `Promise`\<\{ `isValid`: `boolean`; `verifier`: `` `0x${string}` ``; \}\>

Defined in: sdk/FlaunchSDK.ts:3095

Verifies if a memecoin is valid for importing

Parameters

#memecoin

`` `0x${string}` ``

The address of the memecoin to import

Returns

`Promise`\<\{ `isValid`: `boolean`; `verifier`: `` `0x${string}` ``; \}\>

Promise<{ isValid: boolean; verifier: Address }> - The result of the verification

***

### treasuryManagerBalancesV1\_3()

> **treasuryManagerBalancesV1\_3**(`params`): `Promise`\<`AssetBalance`[]\>

Defined in: sdk/FlaunchSDK.ts:2830

Gets a recipient's claimable balances across payout assets on any v1.3.1 multi-asset
treasury manager (RevenueManager, the fee-split managers, StakingManager). Zero balances are
kept so callers can decide what to claim.

Parameters

#params

##assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

##recipient

`` `0x${string}` ``

The address of the recipient to check

##treasuryManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 treasury manager

Returns

`Promise`\<`AssetBalance`[]\>

Promise<AssetBalance[]> - One entry per asset, in the order given

***

### treasuryManagerInfo()

> **treasuryManagerInfo**(`treasuryManagerAddress`): `Promise`\<\{ `managerOwner`: `` `0x${string}` ``; `permissions`: `` `0x${string}` ``; \}\>

Defined in: sdk/FlaunchSDK.ts:2738

Gets treasury manager information including owner and permissions

Parameters

#treasuryManagerAddress

`` `0x${string}` ``

The address of the treasury manager

Returns

`Promise`\<\{ `managerOwner`: `` `0x${string}` ``; `permissions`: `` `0x${string}` ``; \}\>

Promise<{managerOwner: Address, permissions: Address}> - Treasury manager owner and permissions contract addresses

***

### trustedPoolKeySignerStatus()

> **trustedPoolKeySignerStatus**(`coinAddress`, `version?`): `Promise`\<\{ `fairLaunchEndsAt`: `number`; `fairLaunchStartsAt`: `number`; `isCurrentlyEnabled`: `boolean`; `isFairLaunchActive`: `boolean`; `signer`: `` `0x${string}` ``; `trustedSignerEnabled`: `boolean`; \}\>

Defined in: sdk/FlaunchSDK.ts:2343

Parameters

#coinAddress

`` `0x${string}` ``

#version?

`FlaunchVersion`

Returns

`Promise`\<\{ `fairLaunchEndsAt`: `number`; `fairLaunchStartsAt`: `number`; `isCurrentlyEnabled`: `boolean`; `isFairLaunchActive`: `boolean`; `signer`: `` `0x${string}` ``; `trustedSignerEnabled`: `boolean`; \}\>

***

### verifyPairedTokenSwapPlan()

> **verifyPairedTokenSwapPlan**(`plan`, `mode?`): `Promise`\<`PairedSwapVerification`\>

Defined in: sdk/FlaunchSDK.ts:1484

Preflight only; on-chain checks remain authoritative. Quote mode cannot measure input consumption.

Parameters

#plan

`PairedSwapPlan`

#mode?

`"simulate"` \| `"quote"`

Returns

`Promise`\<`PairedSwapVerification`\>

***

### watchPoolCreated()

> **watchPoolCreated**(`params`, `version?`): `Promise`\<\{ `cleanup`: () => `void`; `pollPoolCreatedNow`: () => `Promise`\<`void`\>; \}\>

Defined in: sdk/FlaunchSDK.ts:1965

Watches for pool creation events

Parameters

#params

`WatchPoolCreatedParams`

Parameters for watching pool creation

#version?

`FlaunchVersion` = `FlaunchVersion.V1_1`

Version of Flaunch to use (defaults to V1_1)

Returns

`Promise`\<\{ `cleanup`: () => `void`; `pollPoolCreatedNow`: () => `Promise`\<`void`\>; \}\>

Subscription to pool creation events

***

### watchPoolSwap()

> **watchPoolSwap**(`params`, `version?`): `Promise`\<\{ `cleanup`: () => `void`; `pollPoolSwapNow`: () => `Promise`\<`void`\>; \}\>

Defined in: sdk/FlaunchSDK.ts:2146

Watches for pool swap events

Parameters

#params

`WatchPoolSwapParams`

Parameters for watching pool swaps including optional coin filter

#version?

`FlaunchVersion` = `FlaunchVersion.V1_1`

Version of Flaunch to use (defaults to V1_1)

Returns

`Promise`\<\{ `cleanup`: () => `void`; `pollPoolSwapNow`: () => `Promise`\<`void`\>; \}\>

Subscription to pool swap events


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadFlaunchZapV1\_3

# Class: ReadFlaunchZapV1\_3

Defined in: clients/FlaunchZapV1\_3Client.ts:44

## Extended by

- `ReadWriteFlaunchZapV1_3`

## Constructors

### Constructor

> **new ReadFlaunchZapV1\_3**(`address`, `drift?`): `ReadFlaunchZapV1_3`

Defined in: clients/FlaunchZapV1\_3Client.ts:47

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift` = `...`

Returns

`ReadFlaunchZapV1_3`

## Properties

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}\]\>

Defined in: clients/FlaunchZapV1\_3Client.ts:45

## Methods

### calculateFee()

> **calculateFee**(`__namedParameters`): `Promise`\<`PairedTokenFlaunchFee`\>

Defined in: clients/FlaunchZapV1\_3Client.ts:55

Parameters

#\_\_namedParameters

`CalculatePairedTokenFlaunchFeeParams`

Returns

`Promise`\<`PairedTokenFlaunchFee`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadPairedTokenAcquisition

# Class: ReadPairedTokenAcquisition

Defined in: clients/PairedTokenAcquisitionClient.ts:41

Reads for buying a non-ETH paired token from ETH or the chain's USD hub. Route resolution comes
from live protocol config rather than a table: the registry's `tokenConfig(token).priceCalculator`
already binds the exact pool the launch pricing reads (`hubPairedPool`), so a newly registered
equity routes with no SDK change. On a chain with a `QuoterV2`, every existing pool for the token
is quoted and the best output wins.

## Constructors

### Constructor

> **new ReadPairedTokenAcquisition**(`chainId`, `drift?`): `ReadPairedTokenAcquisition`

Defined in: clients/PairedTokenAcquisitionClient.ts:49

Parameters

#chainId

`number`

#drift?

`Drift` = `...`

Returns

`ReadPairedTokenAcquisition`

## Properties

### chainId

> `readonly` **chainId**: `number`

Defined in: clients/PairedTokenAcquisitionClient.ts:42

***

### dex

> `readonly` **dex**: `PairedTokenAcquisitionDex`

Defined in: clients/PairedTokenAcquisitionClient.ts:43

## Methods

### quote()

> **quote**(`pairedToken`, `input`, `amountIn`): `Promise`\<`PairedTokenAcquisitionQuote`\>

Defined in: clients/PairedTokenAcquisitionClient.ts:174

Picks the venue and expected output for buying the paired token with an exact input. With a
QuoterV2 every existing pool is quoted depth-aware and the best output wins; otherwise (Slipstream,
no quoter, quoter failure) the registry calculator's pool is priced at its single-tick spot.

Parameters

#pairedToken

`` `0x${string}` ``

#input

`PairedTokenAcquisitionInput`

#amountIn

`bigint`

Returns

`Promise`\<`PairedTokenAcquisitionQuote`\>

***

### quoteSpot()

> **quoteSpot**(`pairedToken`, `route`, `input`, `amountIn`): `Promise`\<`bigint`\>

Defined in: clients/PairedTokenAcquisitionClient.ts:145

Spot output through a HUB route: ETH takes the WETH:hub hop first.

Parameters

#pairedToken

`` `0x${string}` ``

#route

`PairedTokenAcquisitionRoute`

#input

`PairedTokenAcquisitionInput`

#amountIn

`bigint`

Returns

`Promise`\<`bigint`\>

***

### resolveRoute()

> **resolveRoute**(`pairedToken`): `Promise`\<`PairedTokenAcquisitionRoute`\>

Defined in: clients/PairedTokenAcquisitionClient.ts:69

The calculator-bound hub pool for a paired token. Everything resolved here is immutable on
chain, so it caches for the session; a failed resolution is not cached.

Parameters

#pairedToken

`` `0x${string}` ``

Returns

`Promise`\<`PairedTokenAcquisitionRoute`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadPairedTokenPositionManagerV1\_3

# Class: ReadPairedTokenPositionManagerV1\_3

Defined in: clients/PairedTokenPositionManagerV1\_3Client.ts:19

Read side of the paired-token PositionManager V1.3 — the hook every `.vpt2` pool lives on.
Wired at `PairedTokenPositionManagerV1_3Address` (which, unlike `FlaunchPositionManagerV1_3Address`,
also covers Base Sepolia).

## Constructors

### Constructor

> **new ReadPairedTokenPositionManagerV1\_3**(`address`, `drift?`): `ReadPairedTokenPositionManagerV1_3`

Defined in: clients/PairedTokenPositionManagerV1\_3Client.ts:23

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift` = `...`

Returns

`ReadPairedTokenPositionManagerV1_3`

## Properties

### address

> `readonly` **address**: `` `0x${string}` ``

Defined in: clients/PairedTokenPositionManagerV1\_3Client.ts:21

***

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}\]\>

Defined in: clients/PairedTokenPositionManagerV1\_3Client.ts:20

## Methods

### pairedToken()

> **pairedToken**(`poolId`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/PairedTokenPositionManagerV1\_3Client.ts:45

The currency `poolId` is paired with; `address(0)` is native ETH. Reverts `UnknownPool` otherwise.

Parameters

#poolId

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### poolKey()

> **poolKey**(`memecoin`): `Promise`\<`PoolKey`\>

Defined in: clients/PairedTokenPositionManagerV1\_3Client.ts:40

The pool key this manager launched for `memecoin`. An unknown coin yields a zeroed key
(`hooks === address(0)`) rather than reverting — callers should treat that as "not a
paired-token coin on this manager".

Parameters

#memecoin

`` `0x${string}` ``

Returns

`Promise`\<`PoolKey`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadPairedTokenRegistryV1\_3

# Class: ReadPairedTokenRegistryV1\_3

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:26

## Constructors

### Constructor

> **new ReadPairedTokenRegistryV1\_3**(`address`, `drift?`): `ReadPairedTokenRegistryV1_3`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:29

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift` = `...`

Returns

`ReadPairedTokenRegistryV1_3`

## Properties

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}\]\>

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:27

## Methods

### isApproved()

> **isApproved**(`token`): `Promise`\<`boolean`\>

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:40

Parameters

#token

`` `0x${string}` ``

Returns

`Promise`\<`boolean`\>

***

### tokenConfig()

> **tokenConfig**(`token`): `Promise`\<`PairedTokenConfig`\>

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:45

The full registry row: approval, type, decimals, escrow and pricing hooks.

Parameters

#token

`` `0x${string}` ``

Returns

`Promise`\<`PairedTokenConfig`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadPoolSwapV1\_3

# Class: ReadPoolSwapV1\_3

Defined in: clients/PoolSwapV1\_3Client.ts:46

The v1.3.1 PoolSwap router: single-pool swaps against any PoolKey on the paired-token
PositionManager. Read side exposes `msgSender()`, the transient slot hooks read to learn who
initiated the in-flight swap (the spend gate's approved-router buyer binding); it is zero at rest.

## Extended by

- `ReadWritePoolSwapV1_3`

## Constructors

### Constructor

> **new ReadPoolSwapV1\_3**(`address`, `drift?`): `ReadPoolSwapV1_3`

Defined in: clients/PoolSwapV1\_3Client.ts:50

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift` = `...`

Returns

`ReadPoolSwapV1_3`

## Properties

### address

> `readonly` **address**: `` `0x${string}` ``

Defined in: clients/PoolSwapV1\_3Client.ts:48

***

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/PoolSwapV1\_3Client.ts:47

## Methods

### msgSender()

> **msgSender**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/PoolSwapV1\_3Client.ts:59

Returns

`Promise`\<`` `0x${string}` ``\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadRevenueManagerV1\_3

# Class: ReadRevenueManagerV1\_3

Defined in: clients/RevenueManagerV1\_3Client.ts:46

Client for a v1.3.1 multi-asset RevenueManager instance in read-only mode.

Unlike the previous generation, balances and claims are kept per payout asset:
`zeroAddress` is native ETH, otherwise the coin's paired token (or a wrapper's underlying).
The legacy `balances(address)` / `claim()` pair means the ETH bucket.

## Extended by

- `ReadWriteRevenueManagerV1_3`

## Constructors

### Constructor

> **new ReadRevenueManagerV1\_3**(`address`, `drift?`): `ReadRevenueManagerV1_3`

Defined in: clients/RevenueManagerV1\_3Client.ts:55

Creates a new ReadRevenueManagerV1_3 instance

Parameters

#address

`` `0x${string}` ``

The address of the v1.3.1 RevenueManager instance

#drift?

`Drift` = `...`

Optional drift instance for contract interactions (creates new instance if not provided)

Returns

`ReadRevenueManagerV1_3`

Throws

Error if address is not provided

## Properties

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/RevenueManagerV1\_3Client.ts:47

## Methods

### balances()

> **balances**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:72

Gets the claimable balance of one payout asset for a recipient

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the asset's raw units

***

### balancesByAsset()

> **balancesByAsset**(`recipient`, `assets?`): `Promise`\<`AssetBalance`[]\>

Defined in: clients/RevenueManagerV1\_3Client.ts:95

Gets the claimable balances across payout assets. Zero balances are kept so callers can
decide what to claim.

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

Returns

`Promise`\<`AssetBalance`[]\>

Promise<AssetBalance[]> - One entry per asset, in the order given

***

### balancesETH()

> **balancesETH**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:84

Gets the claimable native ETH balance for a recipient (the `address(0)` bucket)

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable ETH balance in wei

***

### creatorTotalClaimed()

> **creatorTotalClaimed**(`creator`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:171

Gets the total a creator has claimed in one payout asset

Parameters

#creator

`` `0x${string}` ``

The creator

#asset

`` `0x${string}` ``

The payout asset

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total claimed in the asset's raw units

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:143

Gets the manager owner address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The address of the manager owner

***

### payoutAssets()

> **payoutAssets**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/RevenueManagerV1\_3Client.ts:111

Gets every payout asset the manager has ever been credited in. `zeroAddress` (native ETH)
is always present; other entries come from the paired tokens of deposited pools.

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Promise<readonly Address[]> - The payout assets

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:135

Gets the permissions contract address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The address of the permissions contract

***

### protocolFee()

> **protocolFee**(): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:127

Gets the protocol fee in basis points (2 dp of a percent; 2000 = 20%)

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The protocol fee

***

### protocolRecipient()

> **protocolRecipient**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:119

Gets the protocol recipient address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The protocol recipient address

***

### protocolTotalClaimed()

> **protocolTotalClaimed**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:183

Gets the total the protocol recipient has claimed in one payout asset

Parameters

#asset

`` `0x${string}` ``

The payout asset

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total claimed in the asset's raw units

***

### tokens()

> **tokens**(`creator`): `Promise`\<readonly `object`[]\>

Defined in: clients/RevenueManagerV1\_3Client.ts:152

Gets the flaunch tokens a creator holds in this manager

Parameters

#creator

`` `0x${string}` ``

The address of the creator to query tokens for

Returns

`Promise`\<readonly `object`[]\>

Promise<readonly FlaunchToken[]> - The creator's tokens

***

### tokensCount()

> **tokensCount**(): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:160

Gets the total number of tokens managed by the revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total count of tokens

***

### unsettledCreatorFees()

> **unsettledCreatorFees**(`creator`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:194

Gets a creator settlement that was deferred because the asset refused the transfer
(a transfer-restricted stock on `rescue`), claimable via `withdrawUnsettledCreatorFees`

Parameters

#creator

`` `0x${string}` ``

The creator

#asset

`` `0x${string}` ``

The payout asset

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The deferred amount in the asset's raw units


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadStakingManagerV1\_3

# Class: ReadStakingManagerV1\_3

Defined in: clients/StakingManagerV1\_3Client.ts:36

Client for a v1.3.1 multi-asset StakingManager (Groups) instance in read-only mode.

Staking positions are in the staking token; rewards accrue per payout asset:
`zeroAddress` is native ETH, otherwise a deposited coin's paired token.

## Extended by

- `ReadWriteStakingManagerV1_3`

## Constructors

### Constructor

> **new ReadStakingManagerV1\_3**(`address`, `drift?`): `ReadStakingManagerV1_3`

Defined in: clients/StakingManagerV1\_3Client.ts:39

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift` = `...`

Returns

`ReadStakingManagerV1_3`

## Properties

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/StakingManagerV1\_3Client.ts:37

## Methods

### balances()

> **balances**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:128

Gets the claimable balance of one payout asset for a recipient (staker, creator or owner)

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

***

### balancesByAsset()

> **balancesByAsset**(`recipient`, `assets?`): `Promise`\<`AssetBalance`[]\>

Defined in: clients/StakingManagerV1\_3Client.ts:145

Gets the claimable balances across payout assets (zero balances kept)

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

Returns

`Promise`\<`AssetBalance`[]\>

***

### balancesETH()

> **balancesETH**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:136

Gets the claimable native ETH balance for a recipient (the `address(0)` bucket)

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### claimableOwnerFees()

> **claimableOwnerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:170

Gets the owner fees claimable in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### creatorShare()

> **creatorShare**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:70

Returns

`Promise`\<`bigint`\>

***

### managerFees()

> **managerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:157

Gets the manager-wide fees received in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:54

Returns

`Promise`\<`` `0x${string}` ``\>

***

### minEscrowDuration()

> **minEscrowDuration**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:62

Returns

`Promise`\<`bigint`\>

***

### minStakeDuration()

> **minStakeDuration**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:66

Returns

`Promise`\<`bigint`\>

***

### ownerShare()

> **ownerShare**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:74

Returns

`Promise`\<`bigint`\>

***

### payoutAssets()

> **payoutAssets**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/StakingManagerV1\_3Client.ts:86

Gets every payout asset the manager has ever been credited in (`zeroAddress` = native ETH)

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

***

### pendingCreatorFees()

> **pendingCreatorFees**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:162

Gets the creator fees pending for a recipient in one payout asset

Parameters

#recipient

`` `0x${string}` ``

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

***

### pendingStakeRewards()

> **pendingStakeRewards**(`user`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:116

Gets the staking rewards a user could claim right now in one payout asset

Parameters

#user

`` `0x${string}` ``

The staker

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:50

Returns

`Promise`\<`` `0x${string}` ``\>

***

### stakingToken()

> **stakingToken**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:58

Returns

`Promise`\<`` `0x${string}` ``\>

***

### tokens()

> **tokens**(`creator`): `Promise`\<readonly `object`[]\>

Defined in: clients/StakingManagerV1\_3Client.ts:175

Gets the flaunch tokens a creator holds in this manager

Parameters

#creator

`` `0x${string}` ``

Returns

`Promise`\<readonly `object`[]\>

***

### tokenTimelock()

> **tokenTimelock**(`flaunchToken`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:180

Gets when a deposited flaunch token's escrow unlocks

Parameters

#flaunchToken

`FlaunchToken`

Returns

`Promise`\<`bigint`\>

***

### totalDeposited()

> **totalDeposited**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:79

Total staking tokens deposited across all stakers

Returns

`Promise`\<`bigint`\>

***

### userPositions()

> **userPositions**(`user`): `Promise`\<`StakePositionV1_3`\>

Defined in: clients/StakingManagerV1\_3Client.ts:91

Gets a user's staked position

Parameters

#user

`` `0x${string}` ``

Returns

`Promise`\<`StakePositionV1_3`\>

***

### userRewards()

> **userRewards**(`user`, `asset`): `Promise`\<`StakeRewardsV1_3`\>

Defined in: clients/StakingManagerV1\_3Client.ts:100

Gets a user's reward checkpoint in one payout asset

Parameters

#user

`` `0x${string}` ``

#asset

`` `0x${string}` ``

Returns

`Promise`\<`StakeRewardsV1_3`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadTreasuryManagerV1\_3

# Class: ReadTreasuryManagerV1\_3

Defined in: clients/TreasuryManagerV1\_3Client.ts:32

Generic client for any v1.3.1 multi-asset treasury manager in read-only mode (the
`ITreasuryManager` + `IMultiAssetTreasuryManager` surface every manager of that generation
shares). Use the manager-specific clients for RevenueManager / split / staking extras.

Balances are kept per payout asset: `zeroAddress` is native ETH (flETH and native pools),
a wrapper's underlying (USDC for flUSDC) or the paired token itself (a B20 stock). The
legacy single-value `balances(address)` / `claim()` pair means the ETH bucket.

## Extended by

- `ReadWriteTreasuryManagerV1_3`

## Constructors

### Constructor

> **new ReadTreasuryManagerV1\_3**(`address`, `drift?`): `ReadTreasuryManagerV1_3`

Defined in: clients/TreasuryManagerV1\_3Client.ts:41

Creates a new ReadTreasuryManagerV1_3 instance

Parameters

#address

`` `0x${string}` ``

The address of a v1.3.1 treasury manager instance

#drift?

`Drift` = `...`

Optional drift instance for contract interactions (creates new instance if not provided)

Returns

`ReadTreasuryManagerV1_3`

Throws

Error if address is not provided

## Properties

### contract

> `readonly` **contract**: `ReadContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:33

## Methods

### balances()

> **balances**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:58

Gets the claimable balance of one payout asset for a recipient

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the asset's raw units

***

### balancesETH()

> **balancesETH**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:70

Gets the claimable native ETH balance for a recipient (the `address(0)` bucket)

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable ETH balance in wei

***

### balancesForAssets()

> **balancesForAssets**(`recipient`, `assets?`): `Promise`\<`AssetBalance`[]\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:81

Gets the claimable balances across payout assets. Zero balances are kept so callers can
decide what to claim.

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

Returns

`Promise`\<`AssetBalance`[]\>

Promise<AssetBalance[]> - One entry per asset, in the order given

***

### escrowTokens()

> **escrowTokens**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:105

Gets the escrow-token keys the manager sweeps from the multi-token FeeEscrow

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Promise<readonly Address[]> - The escrow tokens

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:139

Gets the manager owner address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The address of the manager owner

***

### payoutAsset()

> **payoutAsset**(`token`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:114

Gets the payout asset an escrow token unwraps to (ETH for flETH, USDC for flUSDC, …)

Parameters

#token

`` `0x${string}` ``

The escrow token

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The payout asset

***

### payoutAssets()

> **payoutAssets**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:97

Gets every payout asset the manager has ever been credited in. `zeroAddress` (native ETH)
is always present; other entries come from the paired tokens of deposited pools.

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Promise<readonly Address[]> - The payout assets

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:131

Gets the permissions contract address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The address of the permissions contract

***

### poolEscrowToken()

> **poolEscrowToken**(`poolId`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:123

Gets the escrow token a deposited pool's fees sit under

Parameters

#poolId

`` `0x${string}` ``

The pool to check

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The escrow token

***

### treasuryManagerFactory()

> **treasuryManagerFactory**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:147

Gets the TreasuryManagerFactory this manager was deployed from

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The factory address


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteDynamicAddressFeeSplitManager

# Class: ReadWriteDynamicAddressFeeSplitManager

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:140

## Extends

- `ReadDynamicAddressFeeSplitManager`

## Constructors

### Constructor

> **new ReadWriteDynamicAddressFeeSplitManager**(`address`, `drift?`): `ReadWriteDynamicAddressFeeSplitManager`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:143

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWriteDynamicAddressFeeSplitManager`

Overrides

`ReadDynamicAddressFeeSplitManager`.`constructor`

## Properties

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:141

Overrides

`ReadDynamicAddressFeeSplitManager`.`contract`

## Methods

### accumulatorPerShare()

> **accumulatorPerShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:66

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`accumulatorPerShare`

***

### allRecipients()

> **allRecipients**(`includeInactive?`): `Promise`\<`` `0x${string}` ``[]\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:117

Parameters

#includeInactive?

`boolean` = `false`

Returns

`Promise`\<`` `0x${string}` ``[]\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`allRecipients`

***

### balances()

> **balances**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:90

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`balances`

***

### claim()

> **claim**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:180

Returns

`Promise`\<`` `0x${string}` ``\>

***

### claimableOwnerFees()

> **claimableOwnerFees**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:113

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`claimableOwnerFees`

***

### claimForData()

> **claimForData**(`data`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:184

Parameters

#data

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### creatorShare()

> **creatorShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:54

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`creatorShare`

***

### deposit()

> **deposit**(`flaunchToken`, `creator`, `data`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:190

Parameters

#flaunchToken

`FlaunchToken`

#creator

`` `0x${string}` ``

#data

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### lastProcessedManagerFees()

> **lastProcessedManagerFees**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:70

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`lastProcessedManagerFees`

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:46

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`managerOwner`

***

### moderator()

> **moderator**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:50

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`moderator`

***

### ownerShare()

> **ownerShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:58

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`ownerShare`

***

### pendingCreatorFees()

> **pendingCreatorFees**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:103

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`pendingCreatorFees`

***

### pendingOwnerFees()

> **pendingOwnerFees**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:109

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`pendingOwnerFees`

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:42

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`permissions`

***

### recipientAt()

> **recipientAt**(`index`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:78

Parameters

#index

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`recipientAt`

***

### recipientCount()

> **recipientCount**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:74

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`recipientCount`

***

### recipients()

> **recipients**(`recipient`): `Promise`\<`DynamicRecipientInfo`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:84

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`DynamicRecipientInfo`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`recipients`

***

### recipientShare()

> **recipientShare**(`recipient`, `data?`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:96

Parameters

#recipient

`` `0x${string}` ``

#data?

`` `0x${string}` `` = `"0x"`

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`recipientShare`

***

### rescue()

> **rescue**(`flaunchToken`, `recipient`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:205

Parameters

#flaunchToken

`FlaunchToken`

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### setCreator()

> **setCreator**(`flaunchToken`, `creator`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:198

Parameters

#flaunchToken

`FlaunchToken`

#creator

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### setModerator()

> **setModerator**(`moderator`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:162

Parameters

#moderator

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### setPermissions()

> **setPermissions**(`permissions`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:150

Parameters

#permissions

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### totalActiveShares()

> **totalActiveShares**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:62

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManager`.`totalActiveShares`

***

### transferManagerOwnership()

> **transferManagerOwnership**(`newManagerOwner`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:156

Parameters

#newManagerOwner

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### transferRecipientShare()

> **transferRecipientShare**(`newRecipient`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:174

Parameters

#newRecipient

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### updateRecipients()

> **updateRecipients**(`recipients`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:168

Parameters

#recipients

`RecipientShare`[]

Returns

`Promise`\<`` `0x${string}` ``\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteDynamicAddressFeeSplitManagerV1\_3

# Class: ReadWriteDynamicAddressFeeSplitManagerV1\_3

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:196

Client for a v1.3.1 multi-asset DynamicAddressFeeSplitManager instance in read-only mode.

Shares are asset-agnostic; fee accounting (accumulators, pending and claimable fees) is kept
per payout asset: `zeroAddress` is native ETH, otherwise the coin's paired token.

## Extends

- `ReadDynamicAddressFeeSplitManagerV1_3`

## Constructors

### Constructor

> **new ReadWriteDynamicAddressFeeSplitManagerV1\_3**(`address`, `drift?`): `ReadWriteDynamicAddressFeeSplitManagerV1_3`

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:199

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWriteDynamicAddressFeeSplitManagerV1_3`

Overrides

`ReadDynamicAddressFeeSplitManagerV1_3`.`constructor`

## Properties

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:197

Overrides

`ReadDynamicAddressFeeSplitManagerV1_3`.`contract`

## Methods

### accumulatorPerShare()

> **accumulatorPerShare**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:148

Gets the accumulated fees per share in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`accumulatorPerShare`

***

### allRecipients()

> **allRecipients**(`includeInactive?`): `Promise`\<`` `0x${string}` ``[]\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:173

Lists the recipients, active ones only unless `includeInactive` is set

Parameters

#includeInactive?

`boolean` = `false`

Returns

`Promise`\<`` `0x${string}` ``[]\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`allRecipients`

***

### balances()

> **balances**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:114

Gets the claimable balance of one payout asset for a recipient

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`balances`

***

### balancesByAsset()

> **balancesByAsset**(`recipient`, `assets?`): `Promise`\<`AssetBalance`[]\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:131

Gets the claimable balances across payout assets (zero balances kept)

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

Returns

`Promise`\<`AssetBalance`[]\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`balancesByAsset`

***

### balancesETH()

> **balancesETH**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:122

Gets the claimable native ETH balance for a recipient (the `address(0)` bucket)

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`balancesETH`

***

### claim()

> **claim**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:237

Claims the caller's balance in every payout asset

Returns

`Promise`\<`` `0x${string}` ``\>

***

### claimableOwnerFees()

> **claimableOwnerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:166

Gets the owner fees claimable in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`claimableOwnerFees`

***

### claimAssets()

> **claimAssets**(`assets`, `data?`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:254

Claims a subset of payout assets, so a recipient blocked on one asset can still collect the
others. Every asset must be in `payoutAssets()`.

Parameters

#assets

`` `0x${string}` ``[]

The payout assets to settle (`zeroAddress` for native ETH)

#data?

`` `0x${string}` `` = `"0x"`

Manager-specific claim data (defaults to empty)

Returns

`Promise`\<`` `0x${string}` ``\>

***

### claimForData()

> **claimForData**(`data`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:242

Claims in every payout asset with manager-specific claim data

Parameters

#data

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### creatorShare()

> **creatorShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:59

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`creatorShare`

***

### deposit()

> **deposit**(`flaunchToken`, `creator`, `data`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:272

Parameters

#flaunchToken

`FlaunchToken`

#creator

`` `0x${string}` ``

#data

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### depositRevenue()

> **depositRevenue**(`asset`, `amount`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:265

Pushes an ERC20 amount into the manager to be split as revenue. The asset must already be
a payout asset and the manager needs an allowance for the amount.

Parameters

#asset

`` `0x${string}` ``

#amount

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### managerFees()

> **managerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:143

Gets the manager-wide fees received in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`managerFees`

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:51

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`managerOwner`

***

### moderator()

> **moderator**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:55

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`moderator`

***

### ownerShare()

> **ownerShare**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:63

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`ownerShare`

***

### payoutAssets()

> **payoutAssets**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:84

Gets every payout asset the manager has ever been credited in (`zeroAddress` = native ETH)

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`payoutAssets`

***

### pendingCreatorFees()

> **pendingCreatorFees**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:153

Gets the creator fees pending for a recipient in one payout asset

Parameters

#recipient

`` `0x${string}` ``

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`pendingCreatorFees`

***

### pendingOwnerFees()

> **pendingOwnerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:161

Gets the owner fees pending in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`pendingOwnerFees`

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:47

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`permissions`

***

### recipientAt()

> **recipientAt**(`index`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:75

Parameters

#index

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`recipientAt`

***

### recipientCount()

> **recipientCount**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:71

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`recipientCount`

***

### recipientInfo()

> **recipientInfo**(`recipient`, `asset`): `Promise`\<`DynamicRecipientInfoV1_3`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:93

Gets a recipient's split position in one payout asset. `share` is the same in every asset.

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`DynamicRecipientInfoV1_3`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`recipientInfo`

***

### rescue()

> **rescue**(`flaunchToken`, `recipient`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:287

Parameters

#flaunchToken

`FlaunchToken`

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### setCreator()

> **setCreator**(`flaunchToken`, `creator`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:280

Parameters

#flaunchToken

`FlaunchToken`

#creator

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### setModerator()

> **setModerator**(`moderator`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:218

Parameters

#moderator

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### setPermissions()

> **setPermissions**(`permissions`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:206

Parameters

#permissions

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### totalActiveShares()

> **totalActiveShares**(): `Promise`\<`bigint`\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:67

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadDynamicAddressFeeSplitManagerV1_3`.`totalActiveShares`

***

### transferManagerOwnership()

> **transferManagerOwnership**(`newManagerOwner`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:212

Parameters

#newManagerOwner

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### transferRecipientShare()

> **transferRecipientShare**(`newRecipient`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:230

Parameters

#newRecipient

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### updateRecipients()

> **updateRecipients**(`recipients`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:224

Parameters

#recipients

`RecipientShare`[]

Returns

`Promise`\<`` `0x${string}` ``\>

***

### withdrawUnsettledCreatorFees()

> **withdrawUnsettledCreatorFees**(`asset`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:295

Withdraws a creator settlement deferred because the asset refused the transfer

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteFeeEscrowV1\_3

# Class: ReadWriteFeeEscrowV1\_3

Defined in: clients/FeeEscrowV1\_3Client.ts:103

Extended client for the v1.3.1 multi-token FeeEscrow with write capabilities

## Extends

- `ReadFeeEscrowV1_3`

## Constructors

### Constructor

> **new ReadWriteFeeEscrowV1\_3**(`address`, `drift?`): `ReadWriteFeeEscrowV1_3`

Defined in: clients/FeeEscrowV1\_3Client.ts:106

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWriteFeeEscrowV1_3`

Overrides

`ReadFeeEscrowV1_3`.`constructor`

## Properties

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/FeeEscrowV1\_3Client.ts:104

Overrides

`ReadFeeEscrowV1_3`.`contract`

## Methods

### balances()

> **balances**(`recipient`, `token`): `Promise`\<`bigint`\>

Defined in: clients/FeeEscrowV1\_3Client.ts:54

Gets the claimable balance of one escrow token for a recipient

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#token

`` `0x${string}` ``

The escrow token (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the token's raw units

Inherited from

`ReadFeeEscrowV1_3`.`balances`

***

### balancesForTokens()

> **balancesForTokens**(`recipient`, `tokens`): `Promise`\<`EscrowTokenBalance`[]\>

Defined in: clients/FeeEscrowV1\_3Client.ts:68

Gets the claimable balances across several escrow tokens. Zero balances are kept so callers
can decide what to sweep.

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#tokens

`` `0x${string}` ``[]

The escrow tokens to read

Returns

`Promise`\<`EscrowTokenBalance`[]\>

Promise<EscrowTokenBalance[]> - One entry per token, in the order given

Inherited from

`ReadFeeEscrowV1_3`.`balancesForTokens`

***

### pairedTokenRegistry()

> **pairedTokenRegistry**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FeeEscrowV1\_3Client.ts:95

Gets the PairedTokenRegistry that decides each token's unwrap policy on withdrawal

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The registry address

Inherited from

`ReadFeeEscrowV1_3`.`pairedTokenRegistry`

***

### totalFeesAllocated()

> **totalFeesAllocated**(`poolId`, `token`): `Promise`\<`bigint`\>

Defined in: clients/FeeEscrowV1\_3Client.ts:84

Gets the total fees a pool has accrued in one escrow token

Parameters

#poolId

`` `0x${string}` ``

The pool to check

#token

`` `0x${string}` ``

The escrow token

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total allocated, in the token's raw units

Inherited from

`ReadFeeEscrowV1_3`.`totalFeesAllocated`

***

### withdrawFees()

> **withdrawFees**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FeeEscrowV1\_3Client.ts:123

Withdraws the caller's balance in every listed escrow token to a recipient, in one
transaction. A zero-balance or duplicate entry is a harmless no-op on-chain. With `unwrap`
(the default) flETH pays out as ETH and an ERC20 wrapper as its underlying; a plain ERC20
such as a stock is delivered as itself either way.

Parameters

#params

##recipient

`` `0x${string}` ``

The address to receive the fees

##tokens

`` `0x${string}` ``[]

The escrow tokens to withdraw (`zeroAddress` for native ETH)

##unwrap?

`boolean`

Whether to unwrap wrappers to their underlying asset. Defaults to true

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteFlaunchManagerZapV1\_3

# Class: ReadWriteFlaunchManagerZapV1\_3

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:77

Extended client for the v1.3.1 FlaunchManagerZap with write capabilities

## Extends

- `ReadFlaunchManagerZapV1_3`

## Constructors

### Constructor

> **new ReadWriteFlaunchManagerZapV1\_3**(`chainId`, `address`, `drift?`): `ReadWriteFlaunchManagerZapV1_3`

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:80

Parameters

#chainId

`number`

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWriteFlaunchManagerZapV1_3`

Overrides

`ReadFlaunchManagerZapV1_3`.`constructor`

## Properties

### chainId

> **chainId**: `number`

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:44

Inherited from

`ReadFlaunchManagerZapV1_3`.`chainId`

***

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}, \{ \}\]\>

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:78

Overrides

`ReadFlaunchManagerZapV1_3`.`contract`

## Methods

### deployAndInitializeManager()

> **deployAndInitializeManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:98

Deploys a manager from an approved v1.3.1 implementation, initializes it, sets its
permissions and transfers ownership to `owner`. The deployed address is the contract's
return value and the `ManagerDeployed` event on the v1.3.1 factory.

Parameters

#params

`DeployManagerV1_3Params`

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### deployRevenueManager()

> **deployRevenueManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:124

Deploys a new v1.3.1 multi-asset RevenueManager, owned by the protocol recipient

Parameters

#params

`DeployRevenueManagerParams`

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### deployStakingManager()

> **deployStakingManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:170

Deploys a new v1.3.1 multi-asset StakingManager (a Group whose rewards accrue per payout
asset), owned by `managerOwner`

Parameters

#params

`DeployStakingManagerParams`

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### treasuryManagerFactory()

> **treasuryManagerFactory**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:69

Gets the v1.3.1 TreasuryManagerFactory the zap deploys through

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The factory address

Inherited from

`ReadFlaunchManagerZapV1_3`.`treasuryManagerFactory`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteFlaunchSDK

# Class: ReadWriteFlaunchSDK

Defined in: sdk/FlaunchSDK.ts:3707

## Extends

- `ReadFlaunchSDK`

## Constructors

### Constructor

> **new ReadWriteFlaunchSDK**(`chainId`, `drift?`, `publicClient?`): `ReadWriteFlaunchSDK`

Defined in: sdk/FlaunchSDK.ts:3813

Parameters

#chainId

`number`

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

#publicClient?

Returns

`ReadWriteFlaunchSDK`

Overrides

`ReadFlaunchSDK`.`constructor`

## Properties

### chainId

> `readonly` **chainId**: `number`

Defined in: sdk/FlaunchSDK.ts:563

Inherited from

`ReadFlaunchSDK`.`chainId`

***

### drift

> **drift**: `Drift`\<`ReadWriteAdapter`\>

Defined in: sdk/FlaunchSDK.ts:3708

Overrides

`ReadFlaunchSDK`.`drift`

***

### publicClient

> `readonly` **publicClient**: \{ \} \| `undefined`

Defined in: sdk/FlaunchSDK.ts:564

Inherited from

`ReadFlaunchSDK`.`publicClient`

***

### readFeeEscrow

> `readonly` **readFeeEscrow**: `ReadFeeEscrow`

Defined in: sdk/FlaunchSDK.ts:568

Inherited from

`ReadFlaunchSDK`.`readFeeEscrow`

***

### readWriteFeeEscrow

> `readonly` **readWriteFeeEscrow**: `ReadWriteFeeEscrow`

Defined in: sdk/FlaunchSDK.ts:3713

***

### resolveIPFS

> **resolveIPFS**: (`value`) => `string`

Defined in: sdk/FlaunchSDK.ts:580

Parameters

#value

`string`

Returns

`string`

Inherited from

`ReadFlaunchSDK`.`resolveIPFS`

***

### TICK\_SPACING

> `readonly` **TICK\_SPACING**: `60`

Defined in: sdk/FlaunchSDK.ts:565

Inherited from

`ReadFlaunchSDK`.`TICK_SPACING`

## Accessors

### readAnyBidWall

Get Signature

> **get** **readAnyBidWall**(): `AnyBidWall`

Defined in: sdk/FlaunchSDK.ts:746

#Returns

`AnyBidWall`

Inherited from

`ReadFlaunchSDK`.`readAnyBidWall`

***

### readAnyFlaunch

Get Signature

> **get** **readAnyFlaunch**(): `ReadAnyFlaunch`

Defined in: sdk/FlaunchSDK.ts:765

#Returns

`ReadAnyFlaunch`

Inherited from

`ReadFlaunchSDK`.`readAnyFlaunch`

***

### readAnyPositionManager

Get Signature

> **get** **readAnyPositionManager**(): `ReadAnyPositionManager`

Defined in: sdk/FlaunchSDK.ts:709

#Returns

`ReadAnyPositionManager`

Inherited from

`ReadFlaunchSDK`.`readAnyPositionManager`

***

### readBidWall

Get Signature

> **get** **readBidWall**(): `ReadBidWall`

Defined in: sdk/FlaunchSDK.ts:733

#Returns

`ReadBidWall`

Inherited from

`ReadFlaunchSDK`.`readBidWall`

***

### readBidWallV1\_1

Get Signature

> **get** **readBidWallV1\_1**(): `ReadBidWallV1_1`

Defined in: sdk/FlaunchSDK.ts:736

#Returns

`ReadBidWallV1_1`

Inherited from

`ReadFlaunchSDK`.`readBidWallV1_1`

***

### readBidWallV1\_3

Get Signature

> **get** **readBidWallV1\_3**(): `ReadBidWallV1_1` \| `undefined`

Defined in: sdk/FlaunchSDK.ts:743

#Returns

`ReadBidWallV1_1` \| `undefined`

Inherited from

`ReadFlaunchSDK`.`readBidWallV1_3`

***

### readFairLaunch

Get Signature

> **get** **readFairLaunch**(): `ReadFairLaunch`

Defined in: sdk/FlaunchSDK.ts:727

#Returns

`ReadFairLaunch`

Inherited from

`ReadFlaunchSDK`.`readFairLaunch`

***

### readFairLaunchV1\_1

Get Signature

> **get** **readFairLaunchV1\_1**(): `ReadFairLaunchV1_1`

Defined in: sdk/FlaunchSDK.ts:730

#Returns

`ReadFairLaunchV1_1`

Inherited from

`ReadFlaunchSDK`.`readFairLaunchV1_1`

***

### readFeeEscrowV1\_3

Get Signature

> **get** **readFeeEscrowV1\_3**(): `ReadFeeEscrowV1_3`

Defined in: sdk/FlaunchSDK.ts:586

The v1.3.1 multi-token FeeEscrow. Throws on chains without one — gate with
`doesChainSupportMultiTokenFeeEscrow()`.

#Returns

`ReadFeeEscrowV1_3`

Inherited from

`ReadFlaunchSDK`.`readFeeEscrowV1_3`

***

### readFlaunch

Get Signature

> **get** **readFlaunch**(): `ReadFlaunch`

Defined in: sdk/FlaunchSDK.ts:749

#Returns

`ReadFlaunch`

Inherited from

`ReadFlaunchSDK`.`readFlaunch`

***

### readFlaunchManagerZapV1\_3

Get Signature

> **get** **readFlaunchManagerZapV1\_3**(): `ReadFlaunchManagerZapV1_3`

Defined in: sdk/FlaunchSDK.ts:599

The v1.3.1 FlaunchManagerZap, which deploys managers of the multi-asset generation.
Throws on chains without one — gate with `doesChainSupportMultiAssetManagers()`.

#Returns

`ReadFlaunchManagerZapV1_3`

Inherited from

`ReadFlaunchSDK`.`readFlaunchManagerZapV1_3`

***

### readFlaunchV1\_1

Get Signature

> **get** **readFlaunchV1\_1**(): `ReadFlaunchV1_1`

Defined in: sdk/FlaunchSDK.ts:752

#Returns

`ReadFlaunchV1_1`

Inherited from

`ReadFlaunchSDK`.`readFlaunchV1_1`

***

### readFlaunchV1\_2

Get Signature

> **get** **readFlaunchV1\_2**(): `ReadFlaunchV1_2`

Defined in: sdk/FlaunchSDK.ts:755

#Returns

`ReadFlaunchV1_2`

Inherited from

`ReadFlaunchSDK`.`readFlaunchV1_2`

***

### readFlaunchV1\_3

Get Signature

> **get** **readFlaunchV1\_3**(): `ReadFlaunchV1_2` \| `undefined`

Defined in: sdk/FlaunchSDK.ts:762

#Returns

`ReadFlaunchV1_2` \| `undefined`

Inherited from

`ReadFlaunchSDK`.`readFlaunchV1_3`

***

### readFlaunchZap

Get Signature

> **get** **readFlaunchZap**(): `ReadFlaunchZap`

Defined in: sdk/FlaunchSDK.ts:718

#Returns

`ReadFlaunchZap`

Inherited from

`ReadFlaunchSDK`.`readFlaunchZap`

***

### readFlaunchZapV1\_3

Get Signature

> **get** **readFlaunchZapV1\_3**(): `ReadFlaunchZapV1_3`

Defined in: sdk/FlaunchSDK.ts:672

#Returns

`ReadFlaunchZapV1_3`

Inherited from

`ReadFlaunchSDK`.`readFlaunchZapV1_3`

***

### readPairedTokenAcquisition

Get Signature

> **get** **readPairedTokenAcquisition**(): `ReadPairedTokenAcquisition`

Defined in: sdk/FlaunchSDK.ts:1209

Reads for buying a non-ETH paired token from ETH / the USD hub. Gate with `doesChainSupportPairedTokenAcquisition()`.

#Returns

`ReadPairedTokenAcquisition`

Inherited from

`ReadFlaunchSDK`.`readPairedTokenAcquisition`

***

### readPairedTokenPositionManagerV1\_3

Get Signature

> **get** **readPairedTokenPositionManagerV1\_3**(): `ReadPairedTokenPositionManagerV1_3`

Defined in: sdk/FlaunchSDK.ts:701

The paired-token PositionManager V1.3 (pool keys, paired tokens). Same gate as `readPoolSwapV1_3`.

#Returns

`ReadPairedTokenPositionManagerV1_3`

Inherited from

`ReadFlaunchSDK`.`readPairedTokenPositionManagerV1_3`

***

### readPairedTokenRegistryV1\_3

Get Signature

> **get** **readPairedTokenRegistryV1\_3**(): `ReadPairedTokenRegistryV1_3`

Defined in: sdk/FlaunchSDK.ts:680

#Returns

`ReadPairedTokenRegistryV1_3`

Inherited from

`ReadFlaunchSDK`.`readPairedTokenRegistryV1_3`

***

### readPermit2

Get Signature

> **get** **readPermit2**(): `ReadPermit2`

Defined in: sdk/FlaunchSDK.ts:771

#Returns

`ReadPermit2`

Inherited from

`ReadFlaunchSDK`.`readPermit2`

***

### readPoolManager

Get Signature

> **get** **readPoolManager**(): `ReadPoolManager`

Defined in: sdk/FlaunchSDK.ts:721

#Returns

`ReadPoolManager`

Inherited from

`ReadFlaunchSDK`.`readPoolManager`

***

### readPoolSwapV1\_3

Get Signature

> **get** **readPoolSwapV1\_3**(): `ReadPoolSwapV1_3`

Defined in: sdk/FlaunchSDK.ts:692

The v1.3.1 PoolSwap router. Throws on chains without paired-token swap support — gate with
`doesChainSupportPairedTokenSwap()`.

#Returns

`ReadPoolSwapV1_3`

Inherited from

`ReadFlaunchSDK`.`readPoolSwapV1_3`

***

### readPositionManager

Get Signature

> **get** **readPositionManager**(): `ReadFlaunchPositionManager`

Defined in: sdk/FlaunchSDK.ts:660

#Returns

`ReadFlaunchPositionManager`

Inherited from

`ReadFlaunchSDK`.`readPositionManager`

***

### readPositionManagerV1\_1

Get Signature

> **get** **readPositionManagerV1\_1**(): `ReadFlaunchPositionManagerV1_1`

Defined in: sdk/FlaunchSDK.ts:663

#Returns

`ReadFlaunchPositionManagerV1_1`

Inherited from

`ReadFlaunchSDK`.`readPositionManagerV1_1`

***

### readPositionManagerV1\_2

Get Signature

> **get** **readPositionManagerV1\_2**(): `ReadFlaunchPositionManagerV1_2`

Defined in: sdk/FlaunchSDK.ts:666

#Returns

`ReadFlaunchPositionManagerV1_2`

Inherited from

`ReadFlaunchSDK`.`readPositionManagerV1_2`

***

### readPositionManagerV1\_3

Get Signature

> **get** **readPositionManagerV1\_3**(): `ReadFlaunchPositionManagerV1_2` \| `undefined`

Defined in: sdk/FlaunchSDK.ts:669

#Returns

`ReadFlaunchPositionManagerV1_2` \| `undefined`

Inherited from

`ReadFlaunchSDK`.`readPositionManagerV1_3`

***

### readQuoter

Get Signature

> **get** **readQuoter**(): `ReadQuoter`

Defined in: sdk/FlaunchSDK.ts:768

#Returns

`ReadQuoter`

Inherited from

`ReadFlaunchSDK`.`readQuoter`

***

### readReferralEscrow

Get Signature

> **get** **readReferralEscrow**(): `ReadReferralEscrow`

Defined in: sdk/FlaunchSDK.ts:715

#Returns

`ReadReferralEscrow`

Inherited from

`ReadFlaunchSDK`.`readReferralEscrow`

***

### readStateView

Get Signature

> **get** **readStateView**(): `ReadStateView`

Defined in: sdk/FlaunchSDK.ts:724

#Returns

`ReadStateView`

Inherited from

`ReadFlaunchSDK`.`readStateView`

***

### readTokenImporter

Get Signature

> **get** **readTokenImporter**(): `ReadTokenImporter`

Defined in: sdk/FlaunchSDK.ts:712

#Returns

`ReadTokenImporter`

Inherited from

`ReadFlaunchSDK`.`readTokenImporter`

***

### readTreasuryManagerFactoryV1\_3

Get Signature

> **get** **readTreasuryManagerFactoryV1\_3**(): `ReadTreasuryManagerFactory`

Defined in: sdk/FlaunchSDK.ts:613

The v1.3.1 TreasuryManagerFactory (multi-asset manager generation), used to resolve
`ManagerDeployed` events from that factory only. Throws on chains without one — gate
with `doesChainSupportMultiAssetManagers()`.

#Returns

`ReadTreasuryManagerFactory`

Inherited from

`ReadFlaunchSDK`.`readTreasuryManagerFactoryV1_3`

***

### readWriteAnyPositionManager

Get Signature

> **get** **readWriteAnyPositionManager**(): `ReadWriteAnyPositionManager`

Defined in: sdk/FlaunchSDK.ts:3792

#Returns

`ReadWriteAnyPositionManager`

***

### readWriteFeeEscrowV1\_3

Get Signature

> **get** **readWriteFeeEscrowV1\_3**(): `ReadWriteFeeEscrowV1_3`

Defined in: sdk/FlaunchSDK.ts:3721

The v1.3.1 multi-token FeeEscrow with write capabilities. Throws on chains without one —
gate with `doesChainSupportMultiTokenFeeEscrow()`.

#Returns

`ReadWriteFeeEscrowV1_3`

***

### readWriteFlaunchManagerZapV1\_3

Get Signature

> **get** **readWriteFlaunchManagerZapV1\_3**(): `ReadWriteFlaunchManagerZapV1_3`

Defined in: sdk/FlaunchSDK.ts:3735

The v1.3.1 FlaunchManagerZap with write capabilities: deploys managers of the multi-asset
generation through the v1.3.1 factory. Throws on chains without one — gate with
`doesChainSupportMultiAssetManagers()`.

#Returns

`ReadWriteFlaunchManagerZapV1_3`

***

### readWriteFlaunchZap

Get Signature

> **get** **readWriteFlaunchZap**(): `ReadWriteFlaunchZap`

Defined in: sdk/FlaunchSDK.ts:3801

#Returns

`ReadWriteFlaunchZap`

***

### readWriteFlaunchZapV1\_3

Get Signature

> **get** **readWriteFlaunchZapV1\_3**(): `ReadWriteFlaunchZapV1_3`

Defined in: sdk/FlaunchSDK.ts:3744

#Returns

`ReadWriteFlaunchZapV1_3`

***

### readWritePermit2

Get Signature

> **get** **readWritePermit2**(): `ReadWritePermit2`

Defined in: sdk/FlaunchSDK.ts:3809

#Returns

`ReadWritePermit2`

***

### readWritePoolSwapV1\_3

Get Signature

> **get** **readWritePoolSwapV1\_3**(): `ReadWritePoolSwapV1_3`

Defined in: sdk/FlaunchSDK.ts:3767

The chain's CURRENT v1.3 PoolSwap router with write capabilities — gate with `doesChainSupportPairedTokenSwap()`.

#Returns

`ReadWritePoolSwapV1_3`

***

### readWritePositionManager

Get Signature

> **get** **readWritePositionManager**(): `ReadWriteFlaunchPositionManager`

Defined in: sdk/FlaunchSDK.ts:3786

#Returns

`ReadWriteFlaunchPositionManager`

***

### readWritePositionManagerV1\_1

Get Signature

> **get** **readWritePositionManagerV1\_1**(): `ReadWriteFlaunchPositionManagerV1_1`

Defined in: sdk/FlaunchSDK.ts:3789

#Returns

`ReadWriteFlaunchPositionManagerV1_1`

***

### readWriteReferralEscrow

Get Signature

> **get** **readWriteReferralEscrow**(): `ReadWriteReferralEscrow`

Defined in: sdk/FlaunchSDK.ts:3798

#Returns

`ReadWriteReferralEscrow`

***

### readWriteTokenImporter

Get Signature

> **get** **readWriteTokenImporter**(): `ReadWriteTokenImporter`

Defined in: sdk/FlaunchSDK.ts:3795

#Returns

`ReadWriteTokenImporter`

***

### readWriteTreasuryManagerFactory

Get Signature

> **get** **readWriteTreasuryManagerFactory**(): `ReadWriteTreasuryManagerFactory`

Defined in: sdk/FlaunchSDK.ts:3804

#Returns

`ReadWriteTreasuryManagerFactory`

## Methods

### addToTreasuryManager()

> **addToTreasuryManager**(`treasuryManagerAddress`, `version`, `tokenId`, `creator?`, `data?`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4679

Adds an existing flaunch token to a treasury manager. NFT approval must be given prior to calling this function.

Parameters

#treasuryManagerAddress

`` `0x${string}` ``

The address of the treasury manager

#version

`FlaunchVersion`

The flaunch version to determine the correct contract address

#tokenId

`bigint`

The token ID to deposit

#creator?

`` `0x${string}` ``

Optional creator address. If not provided, uses the connected wallet address

#data?

`` `0x${string}` `` = `"0x"`

Optional additional data for the deposit (defaults to empty bytes)

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### anyFlaunch()

> **anyFlaunch**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4199

Creates a new Flaunch with AnyPositionManager for external coins

Parameters

#params

`AnyFlaunchParams`

Parameters for creating the Flaunch with AnyPositionManager

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### assertBaseOnlyOperation()

> `protected` **assertBaseOnlyOperation**(`operation`): `void`

Defined in: sdk/FlaunchSDK.ts:642

Parameters

#operation

`string`

Returns

`void`

Inherited from

`ReadFlaunchSDK`.`assertBaseOnlyOperation`

***

### assertMultiAssetManagersSupported()

> `protected` **assertMultiAssetManagersSupported**(`operation`): `void`

Defined in: sdk/FlaunchSDK.ts:652

The v1.3.1 multi-asset manager generation is Base mainnet only; the `*V1_3` manager
methods refuse to run anywhere it is not deployed rather than sending calls that revert.

Parameters

#operation

`string`

Returns

`void`

Inherited from

`ReadFlaunchSDK`.`assertMultiAssetManagersSupported`

***

### assertPairedTokenAcquisitionSupported()

> `protected` **assertPairedTokenAcquisitionSupported**(`operation`): `void`

Defined in: sdk/FlaunchSDK.ts:1200

Parameters

#operation

`string`

Returns

`void`

Inherited from

`ReadFlaunchSDK`.`assertPairedTokenAcquisitionSupported`

***

### assertPairedTokenSwapSupported()

> `protected` **assertPairedTokenSwapSupported**(`operation`): `void`

Defined in: sdk/FlaunchSDK.ts:1043

Parameters

#operation

`string`

Returns

`void`

Inherited from

`ReadFlaunchSDK`.`assertPairedTokenSwapSupported`

***

### bidWallPosition()

> **bidWallPosition**(`coinAddress`, `version?`): `Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `pendingEth`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:2571

Gets information about the bid wall position for a coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `pendingEth`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Promise<{flETHAmount: bigint, coinAmount: bigint, pendingEth: bigint, tickLower: number, tickUpper: number}> - Bid wall position details

Inherited from

`ReadFlaunchSDK`.`bidWallPosition`

***

### buyCoin()

> **buyCoin**(`params`, `version?`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4221

Buys a coin with ETH or custom inputToken via intermediatePoolKey

Parameters

#params

`BuyCoinParams`

Parameters for buying the coin including amount, slippage, and referrer

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will determine automatically

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response for the buy operation

***

### buyCoinPairedToken()

> **buyCoinPairedToken**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4013

Buys a paired-token coin with an exact amount of its paired token (mUSD, native ETH, flETH, a
B20 equity) through the v1.3.1 PoolSwap. Sends the ERC20 approve first when the allowance is
short, then the swap; returns the swap transaction. To batch both into one wallet call, run
`planPairedTokenSwap({ ...params, direction: "buy" })` and submit its calls yourself.

Parameters

#params

`PairedTokenSwapParams`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### calculateAddLiquidityAmounts()

> **calculateAddLiquidityAmounts**(`params`): `Promise`\<\{ `coinAmount`: `bigint`; `currentTick`: `number`; `ethAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:3421

Parameters

#params

`CalculateAddLiquidityAmountsParams`

Returns

`Promise`\<\{ `coinAmount`: `bigint`; `currentTick`: `number`; `ethAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Inherited from

`ReadFlaunchSDK`.`calculateAddLiquidityAmounts`

***

### calculateAddLiquidityTicks()

> **calculateAddLiquidityTicks**(`__namedParameters`): `Promise`\<\{ `coinDecimals`: `number`; `coinTotalSupply`: `bigint`; `currentTick?`: `number`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:3208

Parameters

#\_\_namedParameters

##coinAddress

`` `0x${string}` ``

##currentMarketCap?

`string`

##liquidityMode

`LiquidityMode`

##maxMarketCap

`string`

##minMarketCap

`string`

Returns

`Promise`\<\{ `coinDecimals`: `number`; `coinTotalSupply`: `bigint`; `currentTick?`: `number`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Inherited from

`ReadFlaunchSDK`.`calculateAddLiquidityTicks`

***

### calculateCurrentTickFromMarketCap()

> **calculateCurrentTickFromMarketCap**(`currentMarketCap`, `formattedTotalSupplyInDecimals`, `marketContext`): `number` \| `undefined`

Defined in: sdk/FlaunchSDK.ts:3179

Calculates current tick from market cap if provided

Parameters

#currentMarketCap

`string` \| `undefined`

#formattedTotalSupplyInDecimals

`number`

#marketContext

##decimals0

`number`

##decimals1

`number`

##ethUsdPrice

`number`

##isFlethZero

`boolean`

Returns

`number` \| `undefined`

Inherited from

`ReadFlaunchSDK`.`calculateCurrentTickFromMarketCap`

***

### calculatePairedTokenFlaunchFee()

> **calculatePairedTokenFlaunchFee**(`params`): `Promise`\<`PairedTokenFlaunchFee`\>

Defined in: sdk/FlaunchSDK.ts:959

Parameters

#params

`CalculatePairedTokenFlaunchFeeParams`

Returns

`Promise`\<`PairedTokenFlaunchFee`\>

Inherited from

`ReadFlaunchSDK`.`calculatePairedTokenFlaunchFee`

***

### checkSingleSidedAddLiquidity()

> **checkSingleSidedAddLiquidity**(`params`): `Promise`\<`SingleSidedLiquidityInfo`\>

Defined in: sdk/FlaunchSDK.ts:3325

Parameters

#params

`CheckSingleSidedAddLiquidityParams`

Returns

`Promise`\<`SingleSidedLiquidityInfo`\>

Inherited from

`ReadFlaunchSDK`.`checkSingleSidedAddLiquidity`

***

### claimReferralBalance()

> **claimReferralBalance**(`coins`, `recipient`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4486

Claims the referral balance for a given recipient

Parameters

#coins

`` `0x${string}` ``[]

The addresses of the coins to claim

#recipient

`` `0x${string}` ``

The address of the recipient to claim the balance for

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### coinBalance()

> **coinBalance**(`coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:4208

Gets the balance of a specific coin for the connected wallet

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to check

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The balance of the coin

***

### coinMarketCapInUSD()

> **coinMarketCapInUSD**(`__namedParameters`): `Promise`\<`string`\>

Defined in: sdk/FlaunchSDK.ts:2255

Parameters

#\_\_namedParameters

##coinAddress

`` `0x${string}` ``

##drift?

`Drift`

##version?

`FlaunchVersion`

Returns

`Promise`\<`string`\>

Inherited from

`ReadFlaunchSDK`.`coinMarketCapInUSD`

***

### coinPriceInETH()

> **coinPriceInETH**(`coinAddress`, `version?`): `Promise`\<`string`\>

Defined in: sdk/FlaunchSDK.ts:2215

Calculates the coin price in ETH based on the current tick

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`string`\>

Promise<string> - The price of the coin in ETH with 18 decimals precision

Inherited from

`ReadFlaunchSDK`.`coinPriceInETH`

***

### coinPriceInUSD()

> **coinPriceInUSD**(`coinAddress`): `Promise`\<`string`\>

Defined in: sdk/FlaunchSDK.ts:2239

Calculates the coin price in USD based on the current ETH/USDC price

Parameters

#coinAddress

The address of the coin

##coinAddress

`` `0x${string}` ``

##drift?

`Drift`

##version?

`FlaunchVersion`

Returns

`Promise`\<`string`\>

Promise<string> - The price of the coin in USD with 18 decimal precision

Inherited from

`ReadFlaunchSDK`.`coinPriceInUSD`

***

### convertPriceToTick()

> **convertPriceToTick**(`priceEth`, `isFlethZero`, `decimals0`, `decimals1`): `number`

Defined in: sdk/FlaunchSDK.ts:3161

Converts token price in ETH to tick

Parameters

#priceEth

`number`

#isFlethZero

`boolean`

#decimals0

`number`

#decimals1

`number`

Returns

`number`

Inherited from

`ReadFlaunchSDK`.`convertPriceToTick`

***

### createPoolKey()

> `protected` **createPoolKey**(`coinAddress`, `version`): `object`

Defined in: sdk/FlaunchSDK.ts:3676

Creates a pool key for the given coin and version

Parameters

#coinAddress

`` `0x${string}` ``

The coin address

#version

`FlaunchVersion`

The version to use for position manager

Returns

`object`

The ordered pool key

#currency0

> **currency0**: `` `0x${string}` ``

#currency1

> **currency1**: `` `0x${string}` ``

#fee

> **fee**: `number`

#hooks

> **hooks**: `` `0x${string}` ``

#tickSpacing

> **tickSpacing**: `number`

Inherited from

`ReadFlaunchSDK`.`createPoolKey`

***

### createPoolKeyForCoin()

> `protected` **createPoolKeyForCoin**(`coinAddress`, `version?`): `Promise`\<\{ `currency0`: `` `0x${string}` ``; `currency1`: `` `0x${string}` ``; `fee`: `number`; `hooks`: `` `0x${string}` ``; `tickSpacing`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:3692

`createPoolKey` resolved by coin rather than by version: on a multichain deployment the
hook is probed per coin (see `getPositionManagerAddressForCoin`), so the key matches the
pool the coin was actually created on. The liquidity helpers use this.

Parameters

#coinAddress

`` `0x${string}` ``

#version?

`FlaunchVersion`

Returns

`Promise`\<\{ `currency0`: `` `0x${string}` ``; `currency1`: `` `0x${string}` ``; `fee`: `number`; `hooks`: `` `0x${string}` ``; `tickSpacing`: `number`; \}\>

Inherited from

`ReadFlaunchSDK`.`createPoolKeyForCoin`

***

### creatorRevenue()

> **creatorRevenue**(`creator`, `isV1?`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2605

Gets the ETH balance for the creator to claim

Parameters

#creator

`` `0x${string}` ``

The address of the creator to check

#isV1?

`boolean`

Optional boolean to check the balance for V1. V1.1 & AnyPositionManager use the same FeeEscrow contract

Returns

`Promise`\<`bigint`\>

The balance of the creator

Inherited from

`ReadFlaunchSDK`.`creatorRevenue`

***

### creatorRevenueByToken()

> **creatorRevenueByToken**(`params`): `Promise`\<`EscrowTokenBalance`[]\>

Defined in: sdk/FlaunchSDK.ts:2622

Gets the creator's claimable revenue on the v1.3.1 multi-token FeeEscrow, per escrow token.
Coins paired with anything other than flETH (native ETH, the B20 equities, …) escrow their
creator fees there, denominated in the paired token, where `creatorRevenue()` cannot see
them. The escrow cannot enumerate a recipient's keys, so pass the paired tokens to look at.

Parameters

#params

##creator

`` `0x${string}` ``

The address of the creator to check

##tokens

`` `0x${string}` ``[]

The escrow tokens to read (`zeroAddress` for native ETH)

Returns

`Promise`\<`EscrowTokenBalance`[]\>

The claimable amount per token, in that token's raw units (zero balances included)

Inherited from

`ReadFlaunchSDK`.`creatorRevenueByToken`

***

### currentTick()

> **currentTick**(`coinAddress`, `version?`): `Promise`\<`number`\>

Defined in: sdk/FlaunchSDK.ts:2200

Gets the current tick for a given coin's pool

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`number`\>

Promise<number> - The current tick of the pool

Inherited from

`ReadFlaunchSDK`.`currentTick`

***

### deployBuyBackManager()

> **deployBuyBackManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:3980

Deploys a new BuyBack manager

Parameters

#params

`DeployBuyBackManagerParams`

Parameters for deploying the BuyBack manager

Returns

`Promise`\<`` `0x${string}` ``\>

Address of the deployed BuyBack manager

***

### deployRevenueManager()

> **deployRevenueManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:3909

Deploys a new revenue manager

Parameters

#params

`DeployRevenueManagerParams`

Parameters for deploying the revenue manager

Returns

`Promise`\<`` `0x${string}` ``\>

Address of the deployed revenue manager

***

### deployRevenueManagerV1\_3()

> **deployRevenueManagerV1\_3**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:3931

Deploys a new v1.3.1 multi-asset revenue manager through the v1.3.1 FlaunchManagerZap.
Managers of this generation pay out per payout asset (native ETH or a coin's paired
token), so they are the ones to use for coins paired with a B20 stock or native ETH;
`deployRevenueManager()` keeps deploying the previous generation. The address is resolved
from the `ManagerDeployed` event of the v1.3.1 factory only.

Parameters

#params

`DeployRevenueManagerParams`

Parameters for deploying the revenue manager

Returns

`Promise`\<`` `0x${string}` ``\>

Address of the deployed revenue manager

***

### deployStakingManager()

> **deployStakingManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:3955

Deploys a new staking manager

Parameters

#params

`DeployStakingManagerParams`

Parameters for deploying the staking manager

Returns

`Promise`\<`` `0x${string}` ``\>

Address of the deployed staking manager

***

### determineCoinVersion()

> `protected` **determineCoinVersion**(`coinAddress`, `version?`): `Promise`\<`FlaunchVersion`\>

Defined in: sdk/FlaunchSDK.ts:3652

Determines the version for a coin, using provided version or fetching it

Parameters

#coinAddress

`` `0x${string}` ``

The coin address

#version?

`FlaunchVersion`

Optional version, if not provided will be fetched

Returns

`Promise`\<`FlaunchVersion`\>

The determined version

Inherited from

`ReadFlaunchSDK`.`determineCoinVersion`

***

### ethRequiredToFlaunch()

> **ethRequiredToFlaunch**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2908

Calculates the ETH required to flaunch a token, takes into account the ETH for premine and the flaunching fee

Parameters

#params

##initialMarketCapUSD

`number`

The initial market cap in USD

##premineAmount

`bigint`

The amount of coins to be premined

##slippagePercent?

`number`

The slippage percent

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The ETH required to flaunch

Inherited from

`ReadFlaunchSDK`.`ethRequiredToFlaunch`

***

### fairLaunchCoinOnlyPosition()

> **fairLaunchCoinOnlyPosition**(`coinAddress`, `version?`): `Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:2508

Gets information about the coin-only position in a fair launch

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Promise<{flETHAmount: bigint, coinAmount: bigint, tickLower: number, tickUpper: number}> - Position details

Inherited from

`ReadFlaunchSDK`.`fairLaunchCoinOnlyPosition`

***

### fairLaunchDuration()

> **fairLaunchDuration**(`coinAddress`, `version?`): `Promise`\<`number` \| `bigint`\>

Defined in: sdk/FlaunchSDK.ts:2415

Gets the duration of a fair launch for a given coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`number` \| `bigint`\>

Promise<number> - The duration in seconds (30 minutes for V1, variable for V1.1)

Inherited from

`ReadFlaunchSDK`.`fairLaunchDuration`

***

### fairLaunchETHOnlyPosition()

> **fairLaunchETHOnlyPosition**(`coinAddress`, `version?`): `Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:2445

Gets information about the ETH-only position in a fair launch

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<\{ `coinAmount`: `bigint`; `flETHAmount`: `bigint`; `tickLower`: `number`; `tickUpper`: `number`; \}\>

Promise<{flETHAmount: bigint, coinAmount: bigint, tickLower: number, tickUpper: number}> - Position details

Inherited from

`ReadFlaunchSDK`.`fairLaunchETHOnlyPosition`

***

### fairLaunchInfo()

> **fairLaunchInfo**(`coinAddress`, `version?`): `Promise`\<\{ `closed`: `boolean`; `endsAt`: `bigint`; `initialTick`: `number`; `revenue`: `bigint`; `startsAt`: `bigint`; `supply`: `bigint`; \}\>

Defined in: sdk/FlaunchSDK.ts:2323

Gets information about a fair launch for a given coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<\{ `closed`: `boolean`; `endsAt`: `bigint`; `initialTick`: `number`; `revenue`: `bigint`; `startsAt`: `bigint`; `supply`: `bigint`; \}\>

Fair launch information from the appropriate contract version

Inherited from

`ReadFlaunchSDK`.`fairLaunchInfo`

***

### flaunch()

> **flaunch**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:3995

Creates a new Flaunch on the specified version

Parameters

#params

`FlaunchParams`

Parameters for creating the Flaunch

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### flaunchIPFS()

> **flaunchIPFS**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4071

Creates a new Flaunch with IPFS metadata and optional version specification

Parameters

#params

`FlaunchIPFSParams`

Parameters for creating the Flaunch with IPFS data

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### flaunchIPFSWithDynamicSplitManager()

> **flaunchIPFSWithDynamicSplitManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4181

Creates a new Flaunch with dynamic split manager configuration and IPFS metadata.

Parameters

#params

`FlaunchWithDynamicSplitManagerIPFSParams`

Parameters for creating the Flaunch with dynamic split manager and IPFS data

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### flaunchIPFSWithRevenueManager()

> **flaunchIPFSWithRevenueManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4109

Creates a new Flaunch with revenue manager configuration and IPFS metadata

Parameters

#params

`FlaunchWithRevenueManagerIPFSParams`

Parameters for creating the Flaunch with revenue manager and IPFS data

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

Throws

Error if FlaunchZap is not deployed on the current chain

***

### flaunchIPFSWithSplitManager()

> **flaunchIPFSWithSplitManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4147

Creates a new Flaunch that splits the creator fees to the creator and a list of recipients, storing the token metadata on IPFS

Parameters

#params

`FlaunchWithSplitManagerIPFSParams`

Parameters for creating the Flaunch with split manager including all IPFS metadata

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### flaunchPairedToken()

> **flaunchPairedToken**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4003

Parameters

#params

`FlaunchPairedTokenParams`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### flaunchWithDynamicSplitManager()

> **flaunchWithDynamicSplitManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4163

Creates a new Flaunch with dynamic split manager configuration.

Parameters

#params

`FlaunchWithDynamicSplitManagerParams`

Parameters for creating the Flaunch with dynamic split manager

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### flaunchWithRevenueManager()

> **flaunchWithRevenueManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4088

Creates a new Flaunch with revenue manager configuration

Parameters

#params

`FlaunchWithRevenueManagerParams`

Parameters for creating the Flaunch with revenue manager

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

Throws

Error if FlaunchZap is not deployed on the current chain

***

### flaunchWithSplitManager()

> **flaunchWithSplitManager**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4131

Creates a new Flaunch that splits the creator fees to the creator and a list of recipients

Parameters

#params

`FlaunchWithSplitManagerParams`

Parameters for creating the Flaunch with split manager

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### flETHIsCurrencyZero()

> **flETHIsCurrencyZero**(`coinAddress`): `boolean`

Defined in: sdk/FlaunchSDK.ts:3050

Determines if flETH is currency0 in the pool

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

Returns

`boolean`

boolean - True if flETH is currency0, false otherwise

Inherited from

`ReadFlaunchSDK`.`flETHIsCurrencyZero`

***

### getAddLiquidityCalls()

> **getAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:4722

Gets the calls needed to add liquidity to flaunch or imported coins

Parameters

#params

`GetAddLiquidityCallsParams`

Parameters for adding liquidity

Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

***

### getBidWall()

> **getBidWall**(`version`): `ReadBidWall` \| `AnyBidWall` \| `ReadBidWallV1_1`

Defined in: sdk/FlaunchSDK.ts:1705

Gets the bid wall instance for a given version

Parameters

#version

`FlaunchVersion`

The version to get the bid wall instance for

Returns

`ReadBidWall` \| `AnyBidWall` \| `ReadBidWallV1_1`

Inherited from

`ReadFlaunchSDK`.`getBidWall`

***

### getBidWallAddress()

> **getBidWallAddress**(`version`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1762

Parameters

#version

`FlaunchVersion`

Returns

`` `0x${string}` ``

Inherited from

`ReadFlaunchSDK`.`getBidWallAddress`

***

### getBuyQuoteExactInput()

> **getBuyQuoteExactInput**(`coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2975

Gets a quote for buying tokens with an exact amount of ETH or inputToken

Parameters

#coinAddress

The address of the token to buy

##amountIn

`bigint`

##coinAddress

`` `0x${string}` ``

##hookData?

`` `0x${string}` ``

##intermediatePoolKey?

`PoolWithHookData`

##userWallet?

`` `0x${string}` ``

##version?

`FlaunchVersion`

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The expected amount of coins to receive

Inherited from

`ReadFlaunchSDK`.`getBuyQuoteExactInput`

***

### getBuyQuoteExactOutput()

> **getBuyQuoteExactOutput**(`coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:3015

Gets a quote for buying an exact amount of tokens with ETH or inputToken

Parameters

#coinAddress

The address of the token to buy

##amountOut

`bigint`

##coinAddress

`` `0x${string}` ``

##hookData?

`` `0x${string}` ``

##intermediatePoolKey?

`PoolWithHookData`

##userWallet?

`` `0x${string}` ``

##version?

`FlaunchVersion`

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The required amount of ETH or inputToken to spend

Inherited from

`ReadFlaunchSDK`.`getBuyQuoteExactOutput`

***

### getCoinInfo()

> **getCoinInfo**(`coinAddress`): `Promise`\<\{ `decimals`: `number`; `formattedTotalSupplyInDecimals`: `number`; `totalSupply`: `bigint`; \}\>

Defined in: sdk/FlaunchSDK.ts:3102

Gets basic coin information (total supply and decimals)

Parameters

#coinAddress

`` `0x${string}` ``

Returns

`Promise`\<\{ `decimals`: `number`; `formattedTotalSupplyInDecimals`: `number`; `totalSupply`: `bigint`; \}\>

Inherited from

`ReadFlaunchSDK`.`getCoinInfo`

***

### getCoinMetadata()

> **getCoinMetadata**(`coinAddress`): `Promise`\<`CoinMetadata` & `object`\>

Defined in: sdk/FlaunchSDK.ts:1789

Retrieves metadata for a given Flaunch coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

Returns

`Promise`\<`CoinMetadata` & `object`\>

Promise<CoinMetadata & { symbol: string }> - The coin's metadata including name, symbol, description, and social links

Inherited from

`ReadFlaunchSDK`.`getCoinMetadata`

***

### getCoinMetadataFromTokenId()

> **getCoinMetadataFromTokenId**(`flaunch`, `tokenId`): `Promise`\<`CoinMetadata` & `object`\>

Defined in: sdk/FlaunchSDK.ts:1819

Retrieves metadata for a given Flaunch coin using its token ID & Flaunch contract address

Parameters

#flaunch

`` `0x${string}` ``

The address of the Flaunch contract

#tokenId

`bigint`

The token ID of the coin

Returns

`Promise`\<`CoinMetadata` & `object`\>

The coin's metadata including name, symbol, description, and social links

Inherited from

`ReadFlaunchSDK`.`getCoinMetadataFromTokenId`

***

### getCoinMetadataFromTokenIds()

> **getCoinMetadataFromTokenIds**(`params`, `batchSize?`, `batchDelay?`): `Promise`\<`object`[]\>

Defined in: sdk/FlaunchSDK.ts:1835

Retrieves metadata for multiple Flaunch coins using their token IDs & Flaunch contract addresses

Parameters

#params

`object`[]

An array of objects containing flaunch contract address and token ID

#batchSize?

`number` = `9`

Optional, the number of ipfs requests to process in each batch

#batchDelay?

`number` = `500`

Optional, the delay in milliseconds between batches

Returns

`Promise`\<`object`[]\>

An array of objects containing coin address, name, symbol, description, and social links

Inherited from

`ReadFlaunchSDK`.`getCoinMetadataFromTokenIds`

***

### getCoinVersion()

> **getCoinVersion**(`coinAddress`): `Promise`\<`FlaunchVersion`\>

Defined in: sdk/FlaunchSDK.ts:1634

Determines the version of a Flaunch coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to check

Returns

`Promise`\<`FlaunchVersion`\>

Promise<FlaunchVersion> - The version of the coin

Inherited from

`ReadFlaunchSDK`.`getCoinVersion`

***

### getERC20AllowanceToPermit2()

> **getERC20AllowanceToPermit2**(`coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:4414

Gets the allowance of an ERC20 token to Permit2 contract. Flaunch coins automatically have infinite approval for Permit2.
this function is for external tokens.

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to check

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The allowance of the coin to Permit2

***

### getETHUSDCPrice()

> **getETHUSDCPrice**(`drift?`): `Promise`\<`number`\>

Defined in: sdk/FlaunchSDK.ts:2278

Gets the current ETH/USDC price

Parameters

#drift?

`Drift`

Optional drift instance to get price from Base Mainnet

Returns

`Promise`\<`number`\>

Promise<number> - The current ETH/USDC price

Inherited from

`ReadFlaunchSDK`.`getETHUSDCPrice`

***

### getFairLaunch()

> **getFairLaunch**(`version`): `ReadFairLaunch` \| `ReadFairLaunchV1_1`

Defined in: sdk/FlaunchSDK.ts:1684

Gets the fair launch instance for a given version

Parameters

#version

`FlaunchVersion`

The version to get the fair launch instance for

Returns

`ReadFairLaunch` \| `ReadFairLaunchV1_1`

Inherited from

`ReadFlaunchSDK`.`getFairLaunch`

***

### getFairLaunchAddress()

> **getFairLaunchAddress**(`version`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1758

Parameters

#version

`FlaunchVersion`

Returns

`` `0x${string}` ``

Inherited from

`ReadFlaunchSDK`.`getFairLaunchAddress`

***

### getFlaunch()

> **getFlaunch**(`version`): `ReadFlaunch` \| `ReadAnyFlaunch` \| `ReadFlaunchV1_1` \| `ReadFlaunchV1_2`

Defined in: sdk/FlaunchSDK.ts:1726

Gets the flaunch contract instance for a given version

Parameters

#version

`FlaunchVersion`

The version to get the flaunch contract instance for

Returns

`ReadFlaunch` \| `ReadAnyFlaunch` \| `ReadFlaunchV1_1` \| `ReadFlaunchV1_2`

Inherited from

`ReadFlaunchSDK`.`getFlaunch`

***

### getFlaunchAddress()

> **getFlaunchAddress**(`version`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1747

Gets the flaunch contract address for a given version

Parameters

#version

`FlaunchVersion`

The version to get the flaunch contract address for

Returns

`` `0x${string}` ``

Inherited from

`ReadFlaunchSDK`.`getFlaunchAddress`

***

### getFlaunchingFee()

> **getFlaunchingFee**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2876

Gets the flaunching fee for a given initial price and slippage percent

Parameters

#params

##initialMarketCapUSD

`number`

The initial market cap in USD

##sender

`` `0x${string}` ``

The address of the sender

##slippagePercent?

`number`

The slippage percent

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The flaunching fee

Inherited from

`ReadFlaunchSDK`.`getFlaunchingFee`

***

### getFlaunchTokenIdForMemecoin()

> **getFlaunchTokenIdForMemecoin**(`coinAddress`): `Promise`\<\{ `flaunchAddress`: `` `0x${string}` ``; `tokenId`: `bigint`; \}\>

Defined in: sdk/FlaunchSDK.ts:1771

Gets the flaunch contract address and token ID for a memecoin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the memecoin

Returns

`Promise`\<\{ `flaunchAddress`: `` `0x${string}` ``; `tokenId`: `bigint`; \}\>

Promise<{ flaunchAddress: Address; tokenId: bigint }> - The flaunch contract address and token ID

Inherited from

`ReadFlaunchSDK`.`getFlaunchTokenIdForMemecoin`

***

### getImportAndAddLiquidityCalls()

Call Signature

> **getImportAndAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:5007

Gets the calls needed to import a memecoin to Flaunch and add liquidity to AnyPositionManager as a batch

#Parameters

##params

Parameters for importing and adding liquidity with market cap constraints

##coinAddress

`` `0x${string}` ``

##coinOrEthInputAmount

`bigint`

##creatorFeeAllocationPercent

`number`

##initialMarketCapUSD

`number`

##inputToken

`"coin"` \| `"eth"`

##liquidityMode

`LiquidityMode`

##maxMarketCap

`string`

##minMarketCap

`string`

##slippagePercent?

`number`

##verifier?

`Verifier`

##version?

`FlaunchVersion`

#Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

#Example

```typescript
const calls = await sdk.getImportAndAddLiquidityCalls({
  coinAddress: "0x...",
  verifier: Verifier.CLANKER,
  creatorFeeAllocationPercent: 5,
  liquidityMode: LiquidityMode.CONCENTRATED,
  coinOrEthInputAmount: parseEther("1"),
  inputToken: "eth",
  minMarketCap: "10000",
  maxMarketCap: "100000",
  initialMarketCapUSD: 50000
});
```

Call Signature

> **getImportAndAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:5031

Gets the calls needed to import a memecoin to Flaunch and add liquidity to AnyPositionManager as a batch

#Parameters

##params

Parameters for importing and adding liquidity with price constraints

##coinAddress

`` `0x${string}` ``

##coinOrEthInputAmount

`bigint`

##creatorFeeAllocationPercent

`number`

##initialPriceUSD

`number`

##inputToken

`"coin"` \| `"eth"`

##liquidityMode

`LiquidityMode`

##maxPriceUSD

`string`

##minPriceUSD

`string`

##slippagePercent?

`number`

##verifier?

`Verifier`

##version?

`FlaunchVersion`

#Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

#Example

```typescript
const calls = await sdk.getImportAndAddLiquidityCalls({
  coinAddress: "0x...",
  verifier: Verifier.CLANKER,
  creatorFeeAllocationPercent: 5,
  liquidityMode: LiquidityMode.CONCENTRATED,
  coinOrEthInputAmount: parseEther("1"),
  inputToken: "eth",
  minPriceUSD: "0.0001",
  maxPriceUSD: "0.001",
  initialPriceUSD: 0.0005
});
```

Call Signature

> **getImportAndAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:5054

Gets the calls needed to import a memecoin to Flaunch and add liquidity to AnyPositionManager as a batch

#Parameters

##params

Parameters for importing and adding liquidity with exact amounts

##coinAddress

`` `0x${string}` ``

##coinAmount

`bigint`

##creatorFeeAllocationPercent

`number`

##currentTick?

`number`

##flethAmount

`bigint`

##slippagePercent?

`number`

##tickLower

`number`

##tickUpper

`number`

##verifier?

`Verifier`

##version?

`FlaunchVersion`

#Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

#Example

```typescript
const calls = await sdk.getImportAndAddLiquidityCalls({
  coinAddress: "0x...",
  verifier: Verifier.CLANKER,
  creatorFeeAllocationPercent: 5,
  coinAmount: parseEther("1000"),
  flethAmount: parseEther("0.5"),
  tickLower: -887220,
  tickUpper: 887220,
  currentTick: 0
});
```

***

### getImportAndSingleSidedCoinAddLiquidityCalls()

Call Signature

> **getImportAndSingleSidedCoinAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:5341

Gets the calls needed to import a memecoin to Flaunch and single-sided liquidity in coin (from current tick to infinity) to AnyPositionManager as a batch

#Parameters

##params

Parameters for importing and adding liquidity with initial market cap

##coinAddress

`` `0x${string}` ``

##coinAmount

`bigint`

##creatorFeeAllocationPercent

`number`

##initialMarketCapUSD

`number`

##verifier?

`Verifier`

##version?

`FlaunchVersion`

#Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

#Example

```typescript
const calls = await sdk.getImportAndSingleSidedCoinAddLiquidityCalls({
  coinAddress: "0x...",
  verifier: Verifier.CLANKER,
  creatorFeeAllocationPercent: 5,
  coinAmount: parseEther("1000"),
  initialMarketCapUSD: 50000
});
```

Call Signature

> **getImportAndSingleSidedCoinAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:5362

Gets the calls needed to import a memecoin to Flaunch and single-sided liquidity in coin (from current tick to infinity) to AnyPositionManager as a batch

#Parameters

##params

`object` & `object`

Parameters for importing and adding liquidity with initial market cap

#Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

#Example

```typescript
const calls = await sdk.getImportAndSingleSidedCoinAddLiquidityCalls({
  coinAddress: "0x...",
  verifier: Verifier.CLANKER,
  creatorFeeAllocationPercent: 5,
  coinAmount: parseEther("1000"),
  initialMarketCapUSD: 50000,
  tokenSupply: 1000000e6 // 1 million tokens (6 decimals)
});
```

Call Signature

> **getImportAndSingleSidedCoinAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:5384

Gets the calls needed to import a memecoin to Flaunch and single-sided liquidity in coin (from current tick to infinity) to AnyPositionManager as a batch

#Parameters

##params

Parameters for importing and adding liquidity with initial price

##coinAddress

`` `0x${string}` ``

##coinAmount

`bigint`

##creatorFeeAllocationPercent

`number`

##initialPriceUSD

`number`

##verifier?

`Verifier`

##version?

`FlaunchVersion`

#Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

#Example

```typescript
const calls = await sdk.getImportAndSingleSidedCoinAddLiquidityCalls({
  coinAddress: "0x...",
  verifier: Verifier.CLANKER,
  creatorFeeAllocationPercent: 5,
  coinAmount: parseEther("1000"),
  initialPriceUSD: 0.001
});
```

Call Signature

> **getImportAndSingleSidedCoinAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:5405

Gets the calls needed to import a memecoin to Flaunch and single-sided liquidity in coin (from current tick to infinity) to AnyPositionManager as a batch

#Parameters

##params

`object` & `object`

Parameters for importing and adding liquidity with initial price

#Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

#Example

```typescript
const calls = await sdk.getImportAndSingleSidedCoinAddLiquidityCalls({
  coinAddress: "0x...",
  verifier: Verifier.CLANKER,
  creatorFeeAllocationPercent: 5,
  coinAmount: parseEther("1000"),
  initialPriceUSD: 0.001,
  tokenSupply: 1000000e6 // 1 million tokens (6 decimals)
});
```

***

### getMarketContext()

> **getMarketContext**(`coinAddress`, `coinDecimals`): `Promise`\<\{ `decimals0`: `number`; `decimals1`: `number`; `ethUsdPrice`: `number`; `isFlethZero`: `boolean`; \}\>

Defined in: sdk/FlaunchSDK.ts:3121

Gets market context information needed for tick calculations

Parameters

#coinAddress

`` `0x${string}` ``

#coinDecimals

`number`

Returns

`Promise`\<\{ `decimals0`: `number`; `decimals1`: `number`; `ethUsdPrice`: `number`; `isFlethZero`: `boolean`; \}\>

Inherited from

`ReadFlaunchSDK`.`getMarketContext`

***

### getPairedPoolQuoteExactInput()

> **getPairedPoolQuoteExactInput**(`__namedParameters`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:1135

Expected output of an exact-input swap on a paired-token pool, via the v4 Quoter's single-hop
quote (hook fees included). A buy spends the paired token for the coin; a sell the reverse.

Parameters

#\_\_namedParameters

`PairedPoolQuoteParams`

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadFlaunchSDK`.`getPairedPoolQuoteExactInput`

***

### getPairedSwapFillFromLogs()

> **getPairedSwapFillFromLogs**(`logs`, `plan`): `PairedSwapFill` \| `null`

Defined in: sdk/FlaunchSDK.ts:1554

Decode one attributable execution. Multiple matching fills are ambiguous, never silently combined.

Parameters

#logs

readonly `Pick`\<`Log`, `"address"` \| `"data"` \| `"topics"`\>[]

#plan

`PairedSwapPlan`

Returns

`PairedSwapFill` \| `null`

Inherited from

`ReadFlaunchSDK`.`getPairedSwapFillFromLogs`

***

### getPairedSwapFillFromTx()

> **getPairedSwapFillFromTx**(`txHash`, `plan`): `Promise`\<`PairedSwapFill` \| `null`\>

Defined in: sdk/FlaunchSDK.ts:1593

Parameters

#txHash

`` `0x${string}` ``

#plan

`PairedSwapPlan`

Returns

`Promise`\<`PairedSwapFill` \| `null`\>

Inherited from

`ReadFlaunchSDK`.`getPairedSwapFillFromTx`

***

### getPermit2AllowanceAndNonce()

> **getPermit2AllowanceAndNonce**(`coinAddress`): `Promise`\<\{ `allowance`: `bigint`; `nonce`: `number`; \}\>

Defined in: sdk/FlaunchSDK.ts:4395

Gets the current Permit2 allowance and nonce for a coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to check

Returns

`Promise`\<\{ `allowance`: `bigint`; `nonce`: `number`; \}\>

Promise<{allowance: bigint, nonce: bigint}> - Current allowance and nonce

***

### getPermit2TypedData()

> **getPermit2TypedData**(`coinAddress`, `deadline?`): `Promise`\<\{ `permitSingle`: `PermitSingle`; `typedData`: \{ `domain`: \{ `chainId`: `number`; `name`: `string`; `verifyingContract`: `` `0x${string}` ``; \}; `message`: `PermitSingle`; `primaryType`: `string`; `types`: \{ `PermitDetails`: `object`[]; `PermitSingle`: `object`[]; \}; \}; \}\>

Defined in: sdk/FlaunchSDK.ts:4374

Gets the typed data for a Permit2 signature

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to permit

#deadline?

`bigint`

Optional deadline for the permit (defaults to 10 years)

Returns

`Promise`\<\{ `permitSingle`: `PermitSingle`; `typedData`: \{ `domain`: \{ `chainId`: `number`; `name`: `string`; `verifyingContract`: `` `0x${string}` ``; \}; `message`: `PermitSingle`; `primaryType`: `string`; `types`: \{ `PermitDetails`: `object`[]; `PermitSingle`: `object`[]; \}; \}; \}\>

The typed data object for signing

***

### getPoolCreatedFromLogs()

> **getPoolCreatedFromLogs**(`logs`): `PoolCreatedEventData` \| `null`

Defined in: sdk/FlaunchSDK.ts:1994

Parses PoolCreated from logs emitted by a PositionManager on this chain.

Parameters

#logs

readonly `Log`[]

Returns

`PoolCreatedEventData` \| `null`

Inherited from

`ReadFlaunchSDK`.`getPoolCreatedFromLogs`

***

### getPoolCreatedFromTx()

> **getPoolCreatedFromTx**(`txHash`): `Promise`\<`PoolCreatedEventData` \| `null`\>

Defined in: sdk/FlaunchSDK.ts:2122

Parses a transaction to extract PoolCreated event data

Parameters

#txHash

`` `0x${string}` ``

The transaction hash to parse

Returns

`Promise`\<`PoolCreatedEventData` \| `null`\>

PoolCreated event parameters or null if not found

Inherited from

`ReadFlaunchSDK`.`getPoolCreatedFromTx`

***

### getPositionManager()

> **getPositionManager**(`version`): `ReadFlaunchPositionManager` \| `ReadFlaunchPositionManagerV1_1` \| `ReadFlaunchPositionManagerV1_2` \| `ReadAnyPositionManager`

Defined in: sdk/FlaunchSDK.ts:1663

Gets the position manager instance for a given version

Parameters

#version

`FlaunchVersion`

The version to get the position manager instance for

Returns

`ReadFlaunchPositionManager` \| `ReadFlaunchPositionManagerV1_1` \| `ReadFlaunchPositionManagerV1_2` \| `ReadAnyPositionManager`

Inherited from

`ReadFlaunchSDK`.`getPositionManager`

***

### getPositionManagerAddress()

> **getPositionManagerAddress**(`version`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1751

Parameters

#version

`FlaunchVersion`

Returns

`` `0x${string}` ``

Inherited from

`ReadFlaunchSDK`.`getPositionManagerAddress`

***

### getPositionManagerAddressForCoin()

> **getPositionManagerAddressForCoin**(`coinAddress`, `version?`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:1023

The hook (PositionManager) a coin's pool lives on. On Base this is the position manager
for the coin's version; on a multichain deployment it is probed per coin (see
`probeMultichainCoinHook`), falling back to the chain's multichain hook for an unknown coin.

Parameters

#coinAddress

`` `0x${string}` ``

The coin to resolve

#version?

`FlaunchVersion`

Optional version override (skips detection on Base; on multichain a
  non-v1.3 version short-circuits to the multichain hook)

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadFlaunchSDK`.`getPositionManagerAddressForCoin`

***

### getSellQuoteExactInput()

> **getSellQuoteExactInput**(`coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2941

Gets a quote for selling an exact amount of tokens for ETH

Parameters

#coinAddress

The address of the token to sell

##amountIn

`bigint`

##coinAddress

`` `0x${string}` ``

##intermediatePoolKey?

`PoolWithHookData`

##version?

`FlaunchVersion`

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The expected amount of ETH to receive

Inherited from

`ReadFlaunchSDK`.`getSellQuoteExactInput`

***

### getSingleSidedCoinAddLiquidityCalls()

> **getSingleSidedCoinAddLiquidityCalls**(`params`): `Promise`\<`CallWithDescription`[]\>

Defined in: sdk/FlaunchSDK.ts:5110

Gets the calls needed to add single-sided liquidity in coin from current tick to infinity

Parameters

#params

`GetSingleSidedCoinAddLiquidityCallsParams`

Parameters for adding single-sided liquidity

Returns

`Promise`\<`CallWithDescription`[]\>

Array of calls with descriptions

***

### importMemecoin()

> **importMemecoin**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4713

Imports a memecoin into the TokenImporter

Parameters

#params

`ImportMemecoinParams`

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### initialSqrtPriceX96()

> **initialSqrtPriceX96**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2288

Parameters

#params

##coinAddress

`` `0x${string}` ``

##initialMarketCapUSD

`number`

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadFlaunchSDK`.`initialSqrtPriceX96`

***

### initialTick()

> **initialTick**(`coinAddress`, `version?`): `Promise`\<`number`\>

Defined in: sdk/FlaunchSDK.ts:2428

Gets the initial tick for a fair launch

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`number`\>

Promise<number> - The initial tick value

Inherited from

`ReadFlaunchSDK`.`initialTick`

***

### isFairLaunchActive()

> **isFairLaunchActive**(`coinAddress`, `version?`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:2336

Checks if a fair launch is currently active for a given coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will be determined automatically

Returns

`Promise`\<`boolean`\>

Promise<boolean> - True if fair launch is active, false otherwise

Inherited from

`ReadFlaunchSDK`.`isFairLaunchActive`

***

### isFlaunchTokenApprovedForAll()

> **isFlaunchTokenApprovedForAll**(`version`, `owner`, `operator`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:3631

Checks if an operator is approved for all flaunch tokens of an owner

Parameters

#version

`FlaunchVersion`

The flaunch version to determine the correct contract address

#owner

`` `0x${string}` ``

The owner address to check

#operator

`` `0x${string}` ``

The operator address to check

Returns

`Promise`\<`boolean`\>

Promise<boolean> - True if operator is approved for all tokens

Inherited from

`ReadFlaunchSDK`.`isFlaunchTokenApprovedForAll`

***

### isMemecoinImported()

> **isMemecoinImported**(`memecoin`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:3607

Checks if an external memecoin has been imported to Flaunch

Parameters

#memecoin

`` `0x${string}` ``

The address of the memecoin to check

Returns

`Promise`\<`boolean`\>

Promise<boolean> - True if the memecoin has been imported

Inherited from

`ReadFlaunchSDK`.`isMemecoinImported`

***

### isPairedTokenApproved()

> **isPairedTokenApproved**(`token`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:955

Parameters

#token

`` `0x${string}` ``

Returns

`Promise`\<`boolean`\>

Inherited from

`ReadFlaunchSDK`.`isPairedTokenApproved`

***

### isValidCoin()

> **isValidCoin**(`coinAddress`): `Promise`\<`boolean`\>

Defined in: sdk/FlaunchSDK.ts:1614

Checks if a given coin address is a valid Flaunch coin (supports all versions)

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to check

Returns

`Promise`\<`boolean`\>

Promise<boolean> - True if the coin is valid, false otherwise

Inherited from

`ReadFlaunchSDK`.`isValidCoin`

***

### locatePairedPool()

> `protected` **locatePairedPool**(`coinAddress`): `Promise`\<\{ `hook`: `` `0x${string}` ``; `poolKey`: `PoolKey`; \} \| `null`\>

Defined in: sdk/FlaunchSDK.ts:1074

Which v1.3 hook a coin's paired pool lives on, and its key.

A chain can carry more than one v1.3 hook generation — on Robinhood the 2026-08-21 v1.3.1
hooks were regenerated as v1.3.3 and both keep serving their pools — so the hook is probed per
coin (`poolKey(coin)` on the current paired PositionManager, then each superseded one) rather
than taken from the chain's current default: a key built on the wrong hook names a pool that
does not exist, and the swap reverts. A hook that never launched the coin answers a zeroed key
(or reverts, for the AnyPositionManager in that list); the first real key wins. Memoised per
coin.

Parameters

#coinAddress

`` `0x${string}` ``

Returns

`Promise`\<\{ `hook`: `` `0x${string}` ``; `poolKey`: `PoolKey`; \} \| `null`\>

The hook and pool key, or null when no v1.3 hook on this chain knows the coin.

Inherited from

`ReadFlaunchSDK`.`locatePairedPool`

***

### marketCapToTokenPriceEth()

> **marketCapToTokenPriceEth**(`marketCapUsd`, `formattedTotalSupplyInDecimals`, `ethUsdPrice`): `number`

Defined in: sdk/FlaunchSDK.ts:3149

Converts market cap in USD to token price in ETH

Parameters

#marketCapUsd

`number`

#formattedTotalSupplyInDecimals

`number`

#ethUsdPrice

`number`

Returns

`number`

Inherited from

`ReadFlaunchSDK`.`marketCapToTokenPriceEth`

***

### pairedTokenPositionManagerAt()

> `protected` **pairedTokenPositionManagerAt**(`hook`): `ReadPairedTokenPositionManagerV1_3`

Defined in: sdk/FlaunchSDK.ts:1052

The paired-token PositionManager client for a given hook address, memoised per hook.

Parameters

#hook

`` `0x${string}` ``

Returns

`ReadPairedTokenPositionManagerV1_3`

Inherited from

`ReadFlaunchSDK`.`pairedTokenPositionManagerAt`

***

### parseSwapTx()

> **parseSwapTx**\<`T`\>(`params`): `Promise`\<`T` *extends* `boolean` ? `GenericBuySwapLog` \| `GenericSellSwapLog` \| `undefined` : `GenericBaseSwapLog` \| `undefined`\>

Defined in: sdk/FlaunchSDK.ts:3074

Parses a transaction hash to extract PoolSwap events and return parsed swap data

Type Parameters

#T

`T` *extends* `boolean` \| `undefined` = `undefined`

Parameters

#params

Object containing parsing parameters

##flETHIsCurrencyZero?

`T`

Whether flETH is currency 0 in the pool (optional)

##txHash

`` `0x${string}` ``

The transaction hash to parse

##version

`FlaunchVersion`

The Flaunch version to use for parsing

Returns

`Promise`\<`T` *extends* `boolean` ? `GenericBuySwapLog` \| `GenericSellSwapLog` \| `undefined` : `GenericBaseSwapLog` \| `undefined`\>

Parsed swap log or undefined if no PoolSwap event found.
         If flETHIsCurrencyZero is provided, returns typed swap data with BUY/SELL information.
         If flETHIsCurrencyZero is undefined, returns basic swap log without parsed delta.

Inherited from

`ReadFlaunchSDK`.`parseSwapTx`

***

### planPairedTokenAcquisition()

> **planPairedTokenAcquisition**(`params`): `Promise`\<`PairedTokenAcquisitionPlan`\>

Defined in: sdk/FlaunchSDK.ts:1234

The calls that buy exactly `target` of a paired token from ETH or the USD hub: an optional
hub-token approve to the venue's router, then the exact-output router call (ETH rides as
`value = maxIn`; the router refunds the unspent part). Exact-output so a following PoolSwap leg
can be encoded up front — batched wallets resolve every call before the first executes.

Parameters

#params

`PairedTokenAcquisitionParams`

Returns

`Promise`\<`PairedTokenAcquisitionPlan`\>

Inherited from

`ReadFlaunchSDK`.`planPairedTokenAcquisition`

***

### planPairedTokenAcquisitionForBudget()

> **planPairedTokenAcquisitionForBudget**(`params`): `Promise`\<`PairedTokenAcquisitionPlan` & `object`\>

Defined in: sdk/FlaunchSDK.ts:1282

Sizes and plans a routed buy from a BUDGET: quotes the exact input, takes `slippageBps` off, and
plans an exact-output buy of that much with the whole budget as the price protection — the
pattern that keeps a downstream PoolSwap leg deterministic. Returns the plan and the target.

Parameters

#params

##amountIn

`bigint`

##deadline?

`bigint`

##input

`PairedTokenAcquisitionInput`

##pairedToken

`` `0x${string}` ``

##recipient

`` `0x${string}` ``

##sender?

`` `0x${string}` ``

##slippageBps

`number`

Returns

`Promise`\<`PairedTokenAcquisitionPlan` & `object`\>

Inherited from

`ReadFlaunchSDK`.`planPairedTokenAcquisitionForBudget`

***

### planPairedTokenApproval()

> **planPairedTokenApproval**(`params`): `Promise`\<`PairedSwapApproveCall` \| `undefined`\>

Defined in: sdk/FlaunchSDK.ts:1183

Just the ERC20 approve a paired-token BUY needs, sized to `amount` — for a host that wants the
player ready before a round starts (approve the whole wallet cap once; every in-round buy is
then a single swap). `undefined` when the pool is paired with native ETH (nothing to approve)
or the standing allowance already covers `amount`.

Parameters

#params

`PairedTokenApprovalParams`

Returns

`Promise`\<`PairedSwapApproveCall` \| `undefined`\>

Inherited from

`ReadFlaunchSDK`.`planPairedTokenApproval`

***

### planPairedTokenSwap()

> **planPairedTokenSwap**(`params`): `Promise`\<`PairedSwapPlan`\>

Defined in: sdk/FlaunchSDK.ts:1319

Builds one quote-backed exact-input plan. Its displayed output and encoded minimum share
the same quote; slot0 and quote reads use the same block. Requires a protected router.
The optional approval precedes swapExactInput. The router enforces full consumption,
minimum net output and expiry at inclusion; legacy deployments fail closed.

Parameters

#params

`PairedSwapPlanParams`

Returns

`Promise`\<`PairedSwapPlan`\>

Inherited from

`ReadFlaunchSDK`.`planPairedTokenSwap`

***

### pollPoolCreatedNow()

> **pollPoolCreatedNow**(`version?`): `Promise`\<`void`\> \| `undefined`

Defined in: sdk/FlaunchSDK.ts:1979

Polls for current pool creation events

Parameters

#version?

`FlaunchVersion` = `FlaunchVersion.V1_1`

Version of Flaunch to use (defaults to V1_1)

Returns

`Promise`\<`void`\> \| `undefined`

Current pool creation events or undefined if polling is not available

Inherited from

`ReadFlaunchSDK`.`pollPoolCreatedNow`

***

### pollPoolSwapNow()

> **pollPoolSwapNow**(`version?`): `Promise`\<`void`\> \| `undefined`

Defined in: sdk/FlaunchSDK.ts:2171

Polls for current pool swap events

Parameters

#version?

`FlaunchVersion` = `FlaunchVersion.V1_1`

Version of Flaunch to use (defaults to V1_1)

Returns

`Promise`\<`void`\> \| `undefined`

Current pool swap events or undefined if polling is not available

Inherited from

`ReadFlaunchSDK`.`pollPoolSwapNow`

***

### poolId()

> **poolId**(`coinAddress`, `version?`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:2852

Gets the pool ID for a given coin

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin

#version?

`FlaunchVersion`

Optional specific version to use

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<string> - The pool ID

Inherited from

`ReadFlaunchSDK`.`poolId`

***

### poolSwapWriterAt()

> `protected` **poolSwapWriterAt**(`router`): `ReadWritePoolSwapV1_3`

Defined in: sdk/FlaunchSDK.ts:3756

A PoolSwap writer for a specific router — the one a plan resolved for its pool's hook generation.

Parameters

#router

`` `0x${string}` ``

Returns

`ReadWritePoolSwapV1_3`

***

### positionInfo()

> **positionInfo**(`params`): `Promise`\<\{ `feeGrowthInside0LastX128`: `bigint`; `feeGrowthInside1LastX128`: `bigint`; `liquidity`: `bigint`; \} \| \{ `feeGrowthInside0LastX128`: `bigint`; `feeGrowthInside1LastX128`: `bigint`; `liquidity`: `bigint`; \}\>

Defined in: sdk/FlaunchSDK.ts:2190

Gets information about a liquidity position

Parameters

#params

`PositionInfoParams`

Parameters for querying position info

Returns

`Promise`\<\{ `feeGrowthInside0LastX128`: `bigint`; `feeGrowthInside1LastX128`: `bigint`; `liquidity`: `bigint`; \} \| \{ `feeGrowthInside0LastX128`: `bigint`; `feeGrowthInside1LastX128`: `bigint`; `liquidity`: `bigint`; \}\>

Position information from the state view contract

Inherited from

`ReadFlaunchSDK`.`positionInfo`

***

### probeMultichainCoinHook()

> `protected` **probeMultichainCoinHook**(`coinAddress`): `Promise`\<\{ `hook`: `` `0x${string}` ``; `version`: `FlaunchVersion`; \} \| `null`\>

Defined in: sdk/FlaunchSDK.ts:979

On a multichain deployment a chain can serve several hook generations at once (Robinhood:
the v1.2 multichain hook, the 2026-08-21 v1.3.1 hooks and the v1.3.3 regeneration). Every
pool key — and so every quote and swap — derives from the hook the coin was created on, so
it has to be probed per coin rather than taken from the chain's current default. Probes the
current and superseded v1.3 hooks first, then the multichain (v1.2) hook.

Parameters

#coinAddress

`` `0x${string}` ``

Returns

`Promise`\<\{ `hook`: `` `0x${string}` ``; `version`: `FlaunchVersion`; \} \| `null`\>

The coin's hook and version, or null when no hook on this chain knows the coin.

Inherited from

`ReadFlaunchSDK`.`probeMultichainCoinHook`

***

### quotePairedTokenAcquisition()

> **quotePairedTokenAcquisition**(`params`): `Promise`\<`PairedTokenAcquisitionQuote`\>

Defined in: sdk/FlaunchSDK.ts:1218

Expected paired-token output (and the venue) for an exact ETH / hub-token input.

Parameters

#params

##amountIn

`bigint`

##input

`PairedTokenAcquisitionInput`

##pairedToken

`` `0x${string}` ``

Returns

`Promise`\<`PairedTokenAcquisitionQuote`\>

Inherited from

`ReadFlaunchSDK`.`quotePairedTokenAcquisition`

***

### referralBalance()

> **referralBalance**(`recipient`, `coinAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2638

Gets the balance of a recipient for a given coin

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#coinAddress

`` `0x${string}` ``

The address of the coin

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The balance of the recipient

Inherited from

`ReadFlaunchSDK`.`referralBalance`

***

### resolvePairedPool()

> **resolvePairedPool**(`coinAddress`, `pairedToken?`): `Promise`\<`ResolvedPairedPool`\>

Defined in: sdk/FlaunchSDK.ts:1104

The pool a paired-token coin trades on: its full key, id and paired side, read from the hook
the coin was launched on (see `locatePairedPool` — one `poolKey` read per hook probed, then
memoised). `pairedToken`, when given, is checked against the pool's real paired side rather
than trusted: building a key from a caller's belief about the pairing is how a swap ends up
aimed at a pool that does not exist.

Parameters

#coinAddress

`` `0x${string}` ``

#pairedToken?

`` `0x${string}` ``

Returns

`Promise`\<`ResolvedPairedPool`\>

Throws

when no v1.3 hook on this chain launched the coin, or `pairedToken` disagrees with the pool

Inherited from

`ReadFlaunchSDK`.`resolvePairedPool`

***

### revenueManagerAllTokensByCreator()

> **revenueManagerAllTokensByCreator**(`params`): `Promise`\<readonly `object`[]\>

Defined in: sdk/FlaunchSDK.ts:2698

Gets all tokens created by a specific creator address

Parameters

#params

Parameters for querying tokens by creator

##creator

`` `0x${string}` ``

The address of the creator to query tokens for

##revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

##sortByDesc?

`boolean`

Whether to sort the tokens by descending order

Returns

`Promise`\<readonly `object`[]\>

Promise<Array<{flaunch: Address, tokenId: bigint}>> - Array of token objects containing flaunch address and token ID

Inherited from

`ReadFlaunchSDK`.`revenueManagerAllTokensByCreator`

***

### revenueManagerAllTokensInManager()

> **revenueManagerAllTokensInManager**(`params`): `Promise`\<`object`[]\>

Defined in: sdk/FlaunchSDK.ts:2721

Gets all tokens currently managed by a revenue manager

Parameters

#params

Parameters for querying tokens in manager

##revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

##sortByDesc?

`boolean`

Optional boolean to sort tokens in descending order (default: false)

Returns

`Promise`\<`object`[]\>

Promise<Array<{flaunch: Address, tokenId: bigint}>> - Array of token objects containing flaunch address and token ID

Inherited from

`ReadFlaunchSDK`.`revenueManagerAllTokensInManager`

***

### revenueManagerBalance()

> **revenueManagerBalance**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2649

Gets the claimable balance of ETH for the recipient from a revenue manager

Parameters

#params

Parameters for checking the balance

##recipient

`` `0x${string}` ``

The address of the recipient to check

##revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance of ETH

Inherited from

`ReadFlaunchSDK`.`revenueManagerBalance`

***

### revenueManagerBalanceV1\_3()

> **revenueManagerBalanceV1\_3**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2767

Gets the claimable balance of one payout asset for a recipient on a v1.3.1 multi-asset
RevenueManager. Managers of that generation keep a balance per payout asset — native ETH
(`zeroAddress`) for flETH / native pools, otherwise the coin's paired token (a wrapper's
underlying, or a B20 stock as itself) — so a stock-paired coin's fees do not show up under
ETH. `revenueManagerBalance()` only speaks to the previous generation.

Parameters

#params

##asset?

`` `0x${string}` ``

The payout asset to read. Defaults to native ETH

##recipient

`` `0x${string}` ``

The address of the recipient to check

##revenueManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the asset's raw units

Inherited from

`ReadFlaunchSDK`.`revenueManagerBalanceV1_3`

***

### revenueManagerCreatorClaim()

> **revenueManagerCreatorClaim**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4509

Claims the total creator's share of the revenue from a revenue manager

Parameters

#params

Parameters for claiming the creator's share of the revenue

##revenueManagerAddress

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### revenueManagerCreatorClaimForTokens()

> **revenueManagerCreatorClaimForTokens**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4523

Claims the creator's share of the revenue from specific flaunch tokens

Parameters

#params

Parameters for claiming the creator's share of the revenue

##flaunchTokens

`object`[]

##revenueManagerAddress

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### revenueManagerCreatorClaimV1\_3()

> **revenueManagerCreatorClaimV1\_3**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4567

Claims a creator's share from a v1.3.1 multi-asset revenue manager. With no `assets`
every payout asset is settled, across all the creator's tokens or the `flaunchTokens`
given; pass `assets` to settle a subset (e.g. skip a transfer-restricted stock). The
connected wallet must be the creator.

Parameters

#params

##assets?

`` `0x${string}` ``[]

Optionally, the payout assets to settle (`zeroAddress` for native ETH)

##flaunchTokens?

`object`[]

Optionally, the flaunch tokens to claim against

##revenueManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 revenue manager

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### revenueManagerPayoutAssets()

> **revenueManagerPayoutAssets**(`revenueManagerAddress`): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: sdk/FlaunchSDK.ts:2790

Gets every payout asset a v1.3.1 revenue manager has ever been credited in. Native ETH
(`zeroAddress`) is always present; other entries come from the paired tokens of the coins
deposited into it. Use it to know which assets to read or claim.

Parameters

#revenueManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 revenue manager

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Promise<readonly Address[]> - The payout assets

Inherited from

`ReadFlaunchSDK`.`revenueManagerPayoutAssets`

***

### revenueManagerProtocolBalance()

> **revenueManagerProtocolBalance**(`revenueManagerAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2666

Gets the claimable balance of ETH for the protocol from a revenue manager

Parameters

#revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance of ETH

Inherited from

`ReadFlaunchSDK`.`revenueManagerProtocolBalance`

***

### revenueManagerProtocolBalanceV1\_3()

> **revenueManagerProtocolBalanceV1\_3**(`params`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2805

Gets the claimable balance of one payout asset for the protocol on a v1.3.1 revenue manager

Parameters

#params

##asset?

`` `0x${string}` ``

The payout asset to read. Defaults to native ETH

##revenueManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the asset's raw units

Inherited from

`ReadFlaunchSDK`.`revenueManagerProtocolBalanceV1_3`

***

### revenueManagerProtocolClaim()

> **revenueManagerProtocolClaim**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4495

Claims the protocol's share of the revenue

Parameters

#params

Parameters for claiming the protocol's share of the revenue

##revenueManagerAddress

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### revenueManagerProtocolClaimV1\_3()

> **revenueManagerProtocolClaimV1\_3**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4543

Claims the protocol's share from a v1.3.1 multi-asset revenue manager. With no `assets`
every payout asset is settled; pass a subset when one asset would block the claim (a
transfer-restricted stock). The connected wallet must be the protocol recipient.

Parameters

#params

##assets?

`` `0x${string}` ``[]

Optionally, the payout assets to settle (`zeroAddress` for native ETH)

##revenueManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 revenue manager

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### revenueManagerTokensCount()

> **revenueManagerTokensCount**(`revenueManagerAddress`): `Promise`\<`bigint`\>

Defined in: sdk/FlaunchSDK.ts:2681

Gets the total number of tokens managed by a revenue manager

Parameters

#revenueManagerAddress

`` `0x${string}` ``

The address of the revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total count of tokens

Inherited from

`ReadFlaunchSDK`.`revenueManagerTokensCount`

***

### routerForPool()

> `protected` **routerForPool**(`hook`): `` `0x${string}` ``

Defined in: sdk/FlaunchSDK.ts:1162

The PoolSwap a pool on `hook` must be traded through. Router approval is per spend gate and
each hook generation ships its own gate, so a gated buy sent through the wrong generation's
router is refused on chain — see `PoolSwapForHookV1_3Address`.

Parameters

#hook

`` `0x${string}` ``

Returns

`` `0x${string}` ``

Inherited from

`ReadFlaunchSDK`.`routerForPool`

***

### sellCoin()

> **sellCoin**(`params`, `version?`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4313

Sells a coin for ETH

Parameters

#params

`SellCoinParams`

Parameters for selling the coin including amount, slippage, permit data, and referrer

#version?

`FlaunchVersion`

Optional specific version to use. If not provided, will determine automatically

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response for the sell operation

***

### sellCoinPairedToken()

> **sellCoinPairedToken**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4018

Sells an exact amount of a paired-token coin for its paired token through PoolSwap. See `buyCoinPairedToken`.

Parameters

#params

`PairedTokenSwapParams`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### setERC20AllowanceToPermit2()

> **setERC20AllowanceToPermit2**(`coinAddress`, `amount`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4429

Sets the allowance of an ERC20 token to Permit2 contract. Flaunch coins automatically have infinite approval for Permit2.
this function is for external tokens.

Parameters

#coinAddress

`` `0x${string}` ``

The address of the coin to approve

#amount

`bigint`

The amount of the token to approve

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### setFlaunchTokenApprovalForAll()

> **setFlaunchTokenApprovalForAll**(`version`, `operator`, `approved`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4655

Sets approval for all flaunch tokens to an operator

Parameters

#version

`FlaunchVersion`

The flaunch version to determine the correct contract address

#operator

`` `0x${string}` ``

The operator address to approve/revoke

#approved

`boolean`

Whether to approve or revoke approval

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### setIPFSResolver()

> **setIPFSResolver**(`resolverFn`): `void`

Defined in: sdk/FlaunchSDK.ts:3060

Sets a custom IPFS resolver function

Parameters

#resolverFn

(`ipfsHash`) => `string`

Custom function to resolve IPFS URIs

Returns

`void`

Dev

this is used to resolve IPFS hash to a gateway URL
eg: input: Qabc, output: https://ipfs.io/ipfs/Qabc

Inherited from

`ReadFlaunchSDK`.`setIPFSResolver`

***

### tokenImporterVerifyMemecoin()

> **tokenImporterVerifyMemecoin**(`memecoin`): `Promise`\<\{ `isValid`: `boolean`; `verifier`: `` `0x${string}` ``; \}\>

Defined in: sdk/FlaunchSDK.ts:3095

Verifies if a memecoin is valid for importing

Parameters

#memecoin

`` `0x${string}` ``

The address of the memecoin to import

Returns

`Promise`\<\{ `isValid`: `boolean`; `verifier`: `` `0x${string}` ``; \}\>

Promise<{ isValid: boolean; verifier: Address }> - The result of the verification

Inherited from

`ReadFlaunchSDK`.`tokenImporterVerifyMemecoin`

***

### treasuryManagerBalancesV1\_3()

> **treasuryManagerBalancesV1\_3**(`params`): `Promise`\<`AssetBalance`[]\>

Defined in: sdk/FlaunchSDK.ts:2830

Gets a recipient's claimable balances across payout assets on any v1.3.1 multi-asset
treasury manager (RevenueManager, the fee-split managers, StakingManager). Zero balances are
kept so callers can decide what to claim.

Parameters

#params

##assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

##recipient

`` `0x${string}` ``

The address of the recipient to check

##treasuryManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 treasury manager

Returns

`Promise`\<`AssetBalance`[]\>

Promise<AssetBalance[]> - One entry per asset, in the order given

Inherited from

`ReadFlaunchSDK`.`treasuryManagerBalancesV1_3`

***

### treasuryManagerClaimAssetsV1\_3()

> **treasuryManagerClaimAssetsV1\_3**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4598

Claims a subset of payout assets from any v1.3.1 multi-asset treasury manager, so a
recipient blocked on one asset can still collect the others. Every asset must be one the
manager has paid out in (`treasuryManagerBalancesV1_3()` / `payoutAssets()`).

Parameters

#params

##assets

`` `0x${string}` ``[]

The payout assets to settle (`zeroAddress` for native ETH)

##data?

`` `0x${string}` ``

Manager-specific claim data (defaults to empty)

##treasuryManagerAddress

`` `0x${string}` ``

The address of the v1.3.1 treasury manager

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### treasuryManagerInfo()

> **treasuryManagerInfo**(`treasuryManagerAddress`): `Promise`\<\{ `managerOwner`: `` `0x${string}` ``; `permissions`: `` `0x${string}` ``; \}\>

Defined in: sdk/FlaunchSDK.ts:2738

Gets treasury manager information including owner and permissions

Parameters

#treasuryManagerAddress

`` `0x${string}` ``

The address of the treasury manager

Returns

`Promise`\<\{ `managerOwner`: `` `0x${string}` ``; `permissions`: `` `0x${string}` ``; \}\>

Promise<{managerOwner: Address, permissions: Address}> - Treasury manager owner and permissions contract addresses

Inherited from

`ReadFlaunchSDK`.`treasuryManagerInfo`

***

### treasuryManagerSetPermissions()

> **treasuryManagerSetPermissions**(`treasuryManagerAddress`, `permissions`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4617

Sets the permissions contract address for a treasury manager

Parameters

#treasuryManagerAddress

`` `0x${string}` ``

The address of the treasury manager

#permissions

`Permissions`

The permissions enum value to set

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### treasuryManagerTransferOwnership()

> **treasuryManagerTransferOwnership**(`treasuryManagerAddress`, `newManagerOwner`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4636

Transfers the ownership of a treasury manager to a new address

Parameters

#treasuryManagerAddress

`` `0x${string}` ``

The address of the treasury manager

#newManagerOwner

`` `0x${string}` ``

The address of the new manager owner

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### trustedPoolKeySignerStatus()

> **trustedPoolKeySignerStatus**(`coinAddress`, `version?`): `Promise`\<\{ `fairLaunchEndsAt`: `number`; `fairLaunchStartsAt`: `number`; `isCurrentlyEnabled`: `boolean`; `isFairLaunchActive`: `boolean`; `signer`: `` `0x${string}` ``; `trustedSignerEnabled`: `boolean`; \}\>

Defined in: sdk/FlaunchSDK.ts:2343

Parameters

#coinAddress

`` `0x${string}` ``

#version?

`FlaunchVersion`

Returns

`Promise`\<\{ `fairLaunchEndsAt`: `number`; `fairLaunchStartsAt`: `number`; `isCurrentlyEnabled`: `boolean`; `isFairLaunchActive`: `boolean`; `signer`: `` `0x${string}` ``; `trustedSignerEnabled`: `boolean`; \}\>

Inherited from

`ReadFlaunchSDK`.`trustedPoolKeySignerStatus`

***

### verifyPairedTokenSwapPlan()

> **verifyPairedTokenSwapPlan**(`plan`, `mode?`): `Promise`\<`PairedSwapVerification`\>

Defined in: sdk/FlaunchSDK.ts:1484

Preflight only; on-chain checks remain authoritative. Quote mode cannot measure input consumption.

Parameters

#plan

`PairedSwapPlan`

#mode?

`"simulate"` \| `"quote"`

Returns

`Promise`\<`PairedSwapVerification`\>

Inherited from

`ReadFlaunchSDK`.`verifyPairedTokenSwapPlan`

***

### watchPoolCreated()

> **watchPoolCreated**(`params`, `version?`): `Promise`\<\{ `cleanup`: () => `void`; `pollPoolCreatedNow`: () => `Promise`\<`void`\>; \}\>

Defined in: sdk/FlaunchSDK.ts:1965

Watches for pool creation events

Parameters

#params

`WatchPoolCreatedParams`

Parameters for watching pool creation

#version?

`FlaunchVersion` = `FlaunchVersion.V1_1`

Version of Flaunch to use (defaults to V1_1)

Returns

`Promise`\<\{ `cleanup`: () => `void`; `pollPoolCreatedNow`: () => `Promise`\<`void`\>; \}\>

Subscription to pool creation events

Inherited from

`ReadFlaunchSDK`.`watchPoolCreated`

***

### watchPoolSwap()

> **watchPoolSwap**(`params`, `version?`): `Promise`\<\{ `cleanup`: () => `void`; `pollPoolSwapNow`: () => `Promise`\<`void`\>; \}\>

Defined in: sdk/FlaunchSDK.ts:2146

Watches for pool swap events

Parameters

#params

`WatchPoolSwapParams`

Parameters for watching pool swaps including optional coin filter

#version?

`FlaunchVersion` = `FlaunchVersion.V1_1`

Version of Flaunch to use (defaults to V1_1)

Returns

`Promise`\<\{ `cleanup`: () => `void`; `pollPoolSwapNow`: () => `Promise`\<`void`\>; \}\>

Subscription to pool swap events

Inherited from

`ReadFlaunchSDK`.`watchPoolSwap`

***

### withdrawCreatorRevenue()

> **withdrawCreatorRevenue**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4441

Withdraws the creator's share of the revenue

Parameters

#params

Parameters for withdrawing the creator's share of the revenue

##isV1?

`boolean`

Optional boolean to withdraw from V1. V1.1 & AnyPositionManager use the same FeeEscrow contract

##recipient?

`` `0x${string}` ``

The address to withdraw the revenue to. Defaults to the connected wallet

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response

***

### withdrawCreatorRevenueByToken()

> **withdrawCreatorRevenueByToken**(`params`): `Promise`\<`` `0x${string}` ``\>

Defined in: sdk/FlaunchSDK.ts:4467

Withdraws the creator's revenue from the v1.3.1 multi-token FeeEscrow across the given
escrow tokens, in one transaction. Pass every token with a balance from
`creatorRevenueByToken()`; a zero balance is a harmless no-op. flETH pays out as ETH while
`unwrap` is left on; a stock pairing is delivered as the stock itself either way.

Parameters

#params

Parameters for withdrawing the creator's revenue

##recipient?

`` `0x${string}` ``

The address to withdraw the revenue to. Defaults to the connected wallet

##tokens

`` `0x${string}` ``[]

The escrow tokens to withdraw (`zeroAddress` for native ETH)

##unwrap?

`boolean`

Whether to unwrap wrappers to their underlying asset. Defaults to true

Returns

`Promise`\<`` `0x${string}` ``\>

Transaction response


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteFlaunchZapV1\_3

# Class: ReadWriteFlaunchZapV1\_3

Defined in: clients/FlaunchZapV1\_3Client.ts:74

## Extends

- `ReadFlaunchZapV1_3`

## Constructors

### Constructor

> **new ReadWriteFlaunchZapV1\_3**(`address`, `drift?`): `ReadWriteFlaunchZapV1_3`

Defined in: clients/FlaunchZapV1\_3Client.ts:77

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWriteFlaunchZapV1_3`

Overrides

`ReadFlaunchZapV1_3`.`constructor`

## Properties

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}\]\>

Defined in: clients/FlaunchZapV1\_3Client.ts:75

Overrides

`ReadFlaunchZapV1_3`.`contract`

## Methods

### calculateFee()

> **calculateFee**(`__namedParameters`): `Promise`\<`PairedTokenFlaunchFee`\>

Defined in: clients/FlaunchZapV1\_3Client.ts:55

Parameters

#\_\_namedParameters

`CalculatePairedTokenFlaunchFeeParams`

Returns

`Promise`\<`PairedTokenFlaunchFee`\>

Inherited from

`ReadFlaunchZapV1_3`.`calculateFee`

***

### flaunch()

> **flaunch**(`__namedParameters`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/FlaunchZapV1\_3Client.ts:84

Parameters

#\_\_namedParameters

`FlaunchPairedTokenParams`

Returns

`Promise`\<`` `0x${string}` ``\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWritePoolSwapV1\_3

# Class: ReadWritePoolSwapV1\_3

Defined in: clients/PoolSwapV1\_3Client.ts:64

The v1.3.1 PoolSwap router: single-pool swaps against any PoolKey on the paired-token
PositionManager. Read side exposes `msgSender()`, the transient slot hooks read to learn who
initiated the in-flight swap (the spend gate's approved-router buyer binding); it is zero at rest.

## Extends

- `ReadPoolSwapV1_3`

## Constructors

### Constructor

> **new ReadWritePoolSwapV1\_3**(`address`, `drift?`): `ReadWritePoolSwapV1_3`

Defined in: clients/PoolSwapV1\_3Client.ts:68

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWritePoolSwapV1_3`

Overrides

`ReadPoolSwapV1_3`.`constructor`

## Properties

### address

> `readonly` **address**: `` `0x${string}` ``

Defined in: clients/PoolSwapV1\_3Client.ts:48

Inherited from

`ReadPoolSwapV1_3`.`address`

***

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/PoolSwapV1\_3Client.ts:65

Overrides

`ReadPoolSwapV1_3`.`contract`

## Methods

### msgSender()

> **msgSender**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/PoolSwapV1\_3Client.ts:59

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadPoolSwapV1_3`.`msgSender`

***

### swap()

> **swap**(`__namedParameters`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/PoolSwapV1\_3Client.ts:81

Swaps against a paired-token pool. The overload is chosen by the payload: `hookData` → the
`bytes` overload, else the `address` overload with `referrer ?? address(0)`. Exact-input only
is enforced by callers (the spend gate rejects exact-output swaps).

Parameters

#\_\_namedParameters

`PoolSwapV1_3SwapParams`

Returns

`Promise`\<`` `0x${string}` ``\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteRevenueManagerV1\_3

# Class: ReadWriteRevenueManagerV1\_3

Defined in: clients/RevenueManagerV1\_3Client.ts:205

Extended client for a v1.3.1 multi-asset RevenueManager with write capabilities

## Extends

- `ReadRevenueManagerV1_3`

## Constructors

### Constructor

> **new ReadWriteRevenueManagerV1\_3**(`address`, `drift?`): `ReadWriteRevenueManagerV1_3`

Defined in: clients/RevenueManagerV1\_3Client.ts:208

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWriteRevenueManagerV1_3`

Overrides

`ReadRevenueManagerV1_3`.`constructor`

## Properties

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/RevenueManagerV1\_3Client.ts:206

Overrides

`ReadRevenueManagerV1_3`.`contract`

## Methods

### balances()

> **balances**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:72

Gets the claimable balance of one payout asset for a recipient

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the asset's raw units

Inherited from

`ReadRevenueManagerV1_3`.`balances`

***

### balancesByAsset()

> **balancesByAsset**(`recipient`, `assets?`): `Promise`\<`AssetBalance`[]\>

Defined in: clients/RevenueManagerV1\_3Client.ts:95

Gets the claimable balances across payout assets. Zero balances are kept so callers can
decide what to claim.

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

Returns

`Promise`\<`AssetBalance`[]\>

Promise<AssetBalance[]> - One entry per asset, in the order given

Inherited from

`ReadRevenueManagerV1_3`.`balancesByAsset`

***

### balancesETH()

> **balancesETH**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:84

Gets the claimable native ETH balance for a recipient (the `address(0)` bucket)

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable ETH balance in wei

Inherited from

`ReadRevenueManagerV1_3`.`balancesETH`

***

### claim()

> **claim**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:220

Claims the caller's balance in every payout asset — the protocol recipient's share, or a
creator's share across all their tokens. Same entrypoint for both roles.

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### claimAssets()

> **claimAssets**(`assets`, `flaunchTokens?`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:245

Claims a subset of payout assets, so a recipient blocked on one asset (a transfer-restricted
stock) can still collect the others. Behaves as `claim()` restricted to `assets` when no
tokens are given, otherwise as `claimForTokens(flaunchTokens)` restricted to `assets`. Every
asset must be in `payoutAssets()`, otherwise the call reverts with `UnsupportedAsset`.

Parameters

#assets

`` `0x${string}` ``[]

The payout assets to settle (`zeroAddress` for native ETH)

#flaunchTokens?

`FlaunchToken`[]

Optionally, the flaunch tokens to claim against

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### claimForTokens()

> **claimForTokens**(`flaunchTokens`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:230

Claims a creator's share for specific flaunch tokens, in every payout asset. The contract
returns `(assets, amounts)`.

Parameters

#flaunchTokens

`FlaunchToken`[]

The flaunch tokens to claim against

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### creatorTotalClaimed()

> **creatorTotalClaimed**(`creator`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:171

Gets the total a creator has claimed in one payout asset

Parameters

#creator

`` `0x${string}` ``

The creator

#asset

`` `0x${string}` ``

The payout asset

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total claimed in the asset's raw units

Inherited from

`ReadRevenueManagerV1_3`.`creatorTotalClaimed`

***

### depositRevenue()

> **depositRevenue**(`asset`, `amount`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:273

Pushes an ERC20 amount into the manager to be distributed as revenue (the protocol fee
share becomes claimable, as it does for ETH sent to the manager). The asset must already be
a payout asset and the manager needs an allowance for the amount.

Parameters

#asset

`` `0x${string}` ``

The payout asset to deposit

#amount

`bigint`

The amount to pull from the caller, in the asset's raw units

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:143

Gets the manager owner address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The address of the manager owner

Inherited from

`ReadRevenueManagerV1_3`.`managerOwner`

***

### payoutAssets()

> **payoutAssets**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/RevenueManagerV1\_3Client.ts:111

Gets every payout asset the manager has ever been credited in. `zeroAddress` (native ETH)
is always present; other entries come from the paired tokens of deposited pools.

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Promise<readonly Address[]> - The payout assets

Inherited from

`ReadRevenueManagerV1_3`.`payoutAssets`

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:135

Gets the permissions contract address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The address of the permissions contract

Inherited from

`ReadRevenueManagerV1_3`.`permissions`

***

### protocolFee()

> **protocolFee**(): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:127

Gets the protocol fee in basis points (2 dp of a percent; 2000 = 20%)

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The protocol fee

Inherited from

`ReadRevenueManagerV1_3`.`protocolFee`

***

### protocolRecipient()

> **protocolRecipient**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:119

Gets the protocol recipient address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The protocol recipient address

Inherited from

`ReadRevenueManagerV1_3`.`protocolRecipient`

***

### protocolTotalClaimed()

> **protocolTotalClaimed**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:183

Gets the total the protocol recipient has claimed in one payout asset

Parameters

#asset

`` `0x${string}` ``

The payout asset

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total claimed in the asset's raw units

Inherited from

`ReadRevenueManagerV1_3`.`protocolTotalClaimed`

***

### setPermissions()

> **setPermissions**(`permissions`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:296

Sets the permissions contract address

Parameters

#permissions

`` `0x${string}` ``

The address of the new permissions contract

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### setProtocolRecipient()

> **setProtocolRecipient**(`protocolRecipient`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:285

Sets the protocol recipient. Only the current protocol recipient may call this.

Parameters

#protocolRecipient

`` `0x${string}` ``

The new protocol recipient

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### tokens()

> **tokens**(`creator`): `Promise`\<readonly `object`[]\>

Defined in: clients/RevenueManagerV1\_3Client.ts:152

Gets the flaunch tokens a creator holds in this manager

Parameters

#creator

`` `0x${string}` ``

The address of the creator to query tokens for

Returns

`Promise`\<readonly `object`[]\>

Promise<readonly FlaunchToken[]> - The creator's tokens

Inherited from

`ReadRevenueManagerV1_3`.`tokens`

***

### tokensCount()

> **tokensCount**(): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:160

Gets the total number of tokens managed by the revenue manager

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The total count of tokens

Inherited from

`ReadRevenueManagerV1_3`.`tokensCount`

***

### transferManagerOwnership()

> **transferManagerOwnership**(`newManagerOwner`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:307

Transfers the manager ownership to a new address

Parameters

#newManagerOwner

`` `0x${string}` ``

The address of the new manager owner

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### unsettledCreatorFees()

> **unsettledCreatorFees**(`creator`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/RevenueManagerV1\_3Client.ts:194

Gets a creator settlement that was deferred because the asset refused the transfer
(a transfer-restricted stock on `rescue`), claimable via `withdrawUnsettledCreatorFees`

Parameters

#creator

`` `0x${string}` ``

The creator

#asset

`` `0x${string}` ``

The payout asset

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The deferred amount in the asset's raw units

Inherited from

`ReadRevenueManagerV1_3`.`unsettledCreatorFees`

***

### withdrawUnsettledCreatorFees()

> **withdrawUnsettledCreatorFees**(`asset`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/RevenueManagerV1\_3Client.ts:259

Withdraws a creator settlement that was deferred because the asset refused the transfer

Parameters

#asset

`` `0x${string}` ``

The payout asset

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteStakingManagerV1\_3

# Class: ReadWriteStakingManagerV1\_3

Defined in: clients/StakingManagerV1\_3Client.ts:188

Client for a v1.3.1 multi-asset StakingManager (Groups) instance in read-only mode.

Staking positions are in the staking token; rewards accrue per payout asset:
`zeroAddress` is native ETH, otherwise a deposited coin's paired token.

## Extends

- `ReadStakingManagerV1_3`

## Constructors

### Constructor

> **new ReadWriteStakingManagerV1\_3**(`address`, `drift?`): `ReadWriteStakingManagerV1_3`

Defined in: clients/StakingManagerV1\_3Client.ts:191

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWriteStakingManagerV1_3`

Overrides

`ReadStakingManagerV1_3`.`constructor`

## Properties

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/StakingManagerV1\_3Client.ts:189

Overrides

`ReadStakingManagerV1_3`.`contract`

## Methods

### balances()

> **balances**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:128

Gets the claimable balance of one payout asset for a recipient (staker, creator or owner)

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`balances`

***

### balancesByAsset()

> **balancesByAsset**(`recipient`, `assets?`): `Promise`\<`AssetBalance`[]\>

Defined in: clients/StakingManagerV1\_3Client.ts:145

Gets the claimable balances across payout assets (zero balances kept)

Parameters

#recipient

`` `0x${string}` ``

The recipient to check

#assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

Returns

`Promise`\<`AssetBalance`[]\>

Inherited from

`ReadStakingManagerV1_3`.`balancesByAsset`

***

### balancesETH()

> **balancesETH**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:136

Gets the claimable native ETH balance for a recipient (the `address(0)` bucket)

Parameters

#recipient

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`balancesETH`

***

### claim()

> **claim**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:215

Claims the caller's rewards / share in every payout asset

Returns

`Promise`\<`` `0x${string}` ``\>

***

### claimableOwnerFees()

> **claimableOwnerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:170

Gets the owner fees claimable in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`claimableOwnerFees`

***

### claimAssets()

> **claimAssets**(`assets`, `data?`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:232

Claims a subset of payout assets, so a recipient blocked on one asset can still collect the
others. Every asset must be in `payoutAssets()`.

Parameters

#assets

`` `0x${string}` ``[]

The payout assets to settle (`zeroAddress` for native ETH)

#data?

`` `0x${string}` `` = `"0x"`

Manager-specific claim data (defaults to empty)

Returns

`Promise`\<`` `0x${string}` ``\>

***

### claimForTokens()

> **claimForTokens**(`flaunchTokens`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:220

Claims a creator's share for specific flaunch tokens, in every payout asset

Parameters

#flaunchTokens

`FlaunchToken`[]

Returns

`Promise`\<`` `0x${string}` ``\>

***

### creatorShare()

> **creatorShare**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:70

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`creatorShare`

***

### deposit()

> **deposit**(`flaunchToken`, `creator`, `data`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:265

Parameters

#flaunchToken

`FlaunchToken`

#creator

`` `0x${string}` ``

#data

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### depositRevenue()

> **depositRevenue**(`asset`, `amount`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:258

Pushes an ERC20 amount into the manager to be distributed as rewards. The asset must
already be a payout asset and the manager needs an allowance for the amount.

Parameters

#asset

`` `0x${string}` ``

#amount

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### escrowWithdraw()

> **escrowWithdraw**(`flaunchToken`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:240

Withdraws a creator's flaunch token once its escrow has unlocked

Parameters

#flaunchToken

`FlaunchToken`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### extendEscrowDuration()

> **extendEscrowDuration**(`flaunchToken`, `extendBy`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:247

Extends a deposited flaunch token's escrow by `extendBy` seconds

Parameters

#flaunchToken

`FlaunchToken`

#extendBy

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### managerFees()

> **managerFees**(`asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:157

Gets the manager-wide fees received in one payout asset

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`managerFees`

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:54

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadStakingManagerV1_3`.`managerOwner`

***

### minEscrowDuration()

> **minEscrowDuration**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:62

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`minEscrowDuration`

***

### minStakeDuration()

> **minStakeDuration**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:66

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`minStakeDuration`

***

### ownerShare()

> **ownerShare**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:74

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`ownerShare`

***

### payoutAssets()

> **payoutAssets**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/StakingManagerV1\_3Client.ts:86

Gets every payout asset the manager has ever been credited in (`zeroAddress` = native ETH)

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Inherited from

`ReadStakingManagerV1_3`.`payoutAssets`

***

### pendingCreatorFees()

> **pendingCreatorFees**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:162

Gets the creator fees pending for a recipient in one payout asset

Parameters

#recipient

`` `0x${string}` ``

#asset

`` `0x${string}` ``

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`pendingCreatorFees`

***

### pendingStakeRewards()

> **pendingStakeRewards**(`user`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:116

Gets the staking rewards a user could claim right now in one payout asset

Parameters

#user

`` `0x${string}` ``

The staker

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`pendingStakeRewards`

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:50

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadStakingManagerV1_3`.`permissions`

***

### setCreator()

> **setCreator**(`flaunchToken`, `creator`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:273

Parameters

#flaunchToken

`FlaunchToken`

#creator

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### setPermissions()

> **setPermissions**(`permissions`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:287

Parameters

#permissions

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### stake()

> **stake**(`amount`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:202

Stakes staking tokens. The manager needs an allowance for the amount; the position is
timelocked for `minStakeDuration` from now.

Parameters

#amount

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### stakingToken()

> **stakingToken**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:58

Returns

`Promise`\<`` `0x${string}` ``\>

Inherited from

`ReadStakingManagerV1_3`.`stakingToken`

***

### tokens()

> **tokens**(`creator`): `Promise`\<readonly `object`[]\>

Defined in: clients/StakingManagerV1\_3Client.ts:175

Gets the flaunch tokens a creator holds in this manager

Parameters

#creator

`` `0x${string}` ``

Returns

`Promise`\<readonly `object`[]\>

Inherited from

`ReadStakingManagerV1_3`.`tokens`

***

### tokenTimelock()

> **tokenTimelock**(`flaunchToken`): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:180

Gets when a deposited flaunch token's escrow unlocks

Parameters

#flaunchToken

`FlaunchToken`

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`tokenTimelock`

***

### totalDeposited()

> **totalDeposited**(): `Promise`\<`bigint`\>

Defined in: clients/StakingManagerV1\_3Client.ts:79

Total staking tokens deposited across all stakers

Returns

`Promise`\<`bigint`\>

Inherited from

`ReadStakingManagerV1_3`.`totalDeposited`

***

### transferManagerOwnership()

> **transferManagerOwnership**(`newManagerOwner`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:293

Parameters

#newManagerOwner

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>

***

### unstake()

> **unstake**(`amount`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:210

Unstakes staking tokens once the timelock has passed. Rewards are settled in every payout
asset; one the asset refuses to transfer is deferred to `userRewards(user, asset).owed`.

Parameters

#amount

`bigint`

Returns

`Promise`\<`` `0x${string}` ``\>

***

### userPositions()

> **userPositions**(`user`): `Promise`\<`StakePositionV1_3`\>

Defined in: clients/StakingManagerV1\_3Client.ts:91

Gets a user's staked position

Parameters

#user

`` `0x${string}` ``

Returns

`Promise`\<`StakePositionV1_3`\>

Inherited from

`ReadStakingManagerV1_3`.`userPositions`

***

### userRewards()

> **userRewards**(`user`, `asset`): `Promise`\<`StakeRewardsV1_3`\>

Defined in: clients/StakingManagerV1\_3Client.ts:100

Gets a user's reward checkpoint in one payout asset

Parameters

#user

`` `0x${string}` ``

#asset

`` `0x${string}` ``

Returns

`Promise`\<`StakeRewardsV1_3`\>

Inherited from

`ReadStakingManagerV1_3`.`userRewards`

***

### withdrawUnsettledCreatorFees()

> **withdrawUnsettledCreatorFees**(`asset`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/StakingManagerV1\_3Client.ts:281

Withdraws a creator settlement deferred because the asset refused the transfer

Parameters

#asset

`` `0x${string}` ``

Returns

`Promise`\<`` `0x${string}` ``\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReadWriteTreasuryManagerV1\_3

# Class: ReadWriteTreasuryManagerV1\_3

Defined in: clients/TreasuryManagerV1\_3Client.ts:155

Extended generic client for a v1.3.1 multi-asset treasury manager with write capabilities

## Extends

- `ReadTreasuryManagerV1_3`

## Constructors

### Constructor

> **new ReadWriteTreasuryManagerV1\_3**(`address`, `drift?`): `ReadWriteTreasuryManagerV1_3`

Defined in: clients/TreasuryManagerV1\_3Client.ts:158

Parameters

#address

`` `0x${string}` ``

#drift?

`Drift`\<`ReadWriteAdapter`\> = `...`

Returns

`ReadWriteTreasuryManagerV1_3`

Overrides

`ReadTreasuryManagerV1_3`.`constructor`

## Properties

### contract

> **contract**: `ReadWriteContract`\<readonly \[\{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}, \{ \}\]\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:156

Overrides

`ReadTreasuryManagerV1_3`.`contract`

## Methods

### balances()

> **balances**(`recipient`, `asset`): `Promise`\<`bigint`\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:58

Gets the claimable balance of one payout asset for a recipient

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#asset

`` `0x${string}` ``

The payout asset (`zeroAddress` for native ETH)

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable balance in the asset's raw units

Inherited from

`ReadTreasuryManagerV1_3`.`balances`

***

### balancesETH()

> **balancesETH**(`recipient`): `Promise`\<`bigint`\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:70

Gets the claimable native ETH balance for a recipient (the `address(0)` bucket)

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

Returns

`Promise`\<`bigint`\>

Promise<bigint> - The claimable ETH balance in wei

Inherited from

`ReadTreasuryManagerV1_3`.`balancesETH`

***

### balancesForAssets()

> **balancesForAssets**(`recipient`, `assets?`): `Promise`\<`AssetBalance`[]\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:81

Gets the claimable balances across payout assets. Zero balances are kept so callers can
decide what to claim.

Parameters

#recipient

`` `0x${string}` ``

The address of the recipient to check

#assets?

`` `0x${string}` ``[]

The payout assets to read. Defaults to every asset the manager has paid out in

Returns

`Promise`\<`AssetBalance`[]\>

Promise<AssetBalance[]> - One entry per asset, in the order given

Inherited from

`ReadTreasuryManagerV1_3`.`balancesForAssets`

***

### claim()

> **claim**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:169

Claims the caller's balance in every payout asset (the legacy `claim()` entrypoint)

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### claimAssets()

> **claimAssets**(`assets`, `data?`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:182

Claims the caller's balance in a subset of payout assets, so a recipient blocked on one
asset (a transfer-restricted stock) can still collect the others. Every asset must be one
the manager has paid out in (`payoutAssets()`), otherwise the call reverts with
`UnsupportedAsset`. The contract returns `(assets, amounts)`.

Parameters

#assets

`` `0x${string}` ``[]

The payout assets to settle (`zeroAddress` for native ETH)

#data?

`` `0x${string}` `` = `"0x"`

Manager-specific claim data (defaults to empty)

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### deposit()

> **deposit**(`flaunchToken`, `creator`, `data`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:212

Deposits a flaunch token to the manager. NFT approval must be given beforehand.

Parameters

#flaunchToken

`FlaunchToken`

The flaunch token to deposit

#creator

`` `0x${string}` ``

The address of the creator

#data

`` `0x${string}` ``

Additional data for the deposit

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### depositRevenue()

> **depositRevenue**(`asset`, `amount`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:198

Pushes an ERC20 amount into the manager to be distributed as revenue — the token analogue
of sending ETH to the manager. The asset must already be a payout asset of the manager, and
the manager needs an allowance for the amount. An ERC20 sent straight to a manager is never
booked and has no recovery path.

Parameters

#asset

`` `0x${string}` ``

The payout asset to deposit

#amount

`bigint`

The amount to pull from the caller, in the asset's raw units

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### escrowTokens()

> **escrowTokens**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:105

Gets the escrow-token keys the manager sweeps from the multi-token FeeEscrow

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Promise<readonly Address[]> - The escrow tokens

Inherited from

`ReadTreasuryManagerV1_3`.`escrowTokens`

***

### managerOwner()

> **managerOwner**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:139

Gets the manager owner address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The address of the manager owner

Inherited from

`ReadTreasuryManagerV1_3`.`managerOwner`

***

### payoutAsset()

> **payoutAsset**(`token`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:114

Gets the payout asset an escrow token unwraps to (ETH for flETH, USDC for flUSDC, …)

Parameters

#token

`` `0x${string}` ``

The escrow token

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The payout asset

Inherited from

`ReadTreasuryManagerV1_3`.`payoutAsset`

***

### payoutAssets()

> **payoutAssets**(): `Promise`\<readonly `` `0x${string}` ``[]\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:97

Gets every payout asset the manager has ever been credited in. `zeroAddress` (native ETH)
is always present; other entries come from the paired tokens of deposited pools.

Returns

`Promise`\<readonly `` `0x${string}` ``[]\>

Promise<readonly Address[]> - The payout assets

Inherited from

`ReadTreasuryManagerV1_3`.`payoutAssets`

***

### permissions()

> **permissions**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:131

Gets the permissions contract address

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The address of the permissions contract

Inherited from

`ReadTreasuryManagerV1_3`.`permissions`

***

### poolEscrowToken()

> **poolEscrowToken**(`poolId`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:123

Gets the escrow token a deposited pool's fees sit under

Parameters

#poolId

`` `0x${string}` ``

The pool to check

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The escrow token

Inherited from

`ReadTreasuryManagerV1_3`.`poolEscrowToken`

***

### registerEscrowToken()

> **registerEscrowToken**(`token`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:226

Adds a registry-approved escrow token key without a pool, e.g. for balances credited to
the manager directly on the multi-token FeeEscrow

Parameters

#token

`` `0x${string}` ``

The escrow token to register

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### setPermissions()

> **setPermissions**(`permissions`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:235

Sets the permissions contract address

Parameters

#permissions

`` `0x${string}` ``

The address of the new permissions contract

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### transferManagerOwnership()

> **transferManagerOwnership**(`newManagerOwner`): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:246

Transfers the manager ownership to a new address

Parameters

#newManagerOwner

`` `0x${string}` ``

The address of the new manager owner

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Hex> - The transaction hash

***

### treasuryManagerFactory()

> **treasuryManagerFactory**(): `Promise`\<`` `0x${string}` ``\>

Defined in: clients/TreasuryManagerV1\_3Client.ts:147

Gets the TreasuryManagerFactory this manager was deployed from

Returns

`Promise`\<`` `0x${string}` ``\>

Promise<Address> - The factory address

Inherited from

`ReadTreasuryManagerV1_3`.`treasuryManagerFactory`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchVersion

# Enumeration: FlaunchVersion

Defined in: types.ts:66

Enumeration of Flaunch contract versions

## Enumeration Members

### ANY

> **ANY**: `"ANY"`

Defined in: types.ts:71

***

### V1

> **V1**: `"V1"`

Defined in: types.ts:67

***

### V1\_1

> **V1\_1**: `"V1_1"`

Defined in: types.ts:68

***

### V1\_2

> **V1\_2**: `"V1_2"`

Defined in: types.ts:69

***

### V1\_3

> **V1\_3**: `"V1_3"`

Defined in: types.ts:70


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / LiquidityMode

# Enumeration: LiquidityMode

Defined in: types.ts:86

## Enumeration Members

### CONCENTRATED

> **CONCENTRATED**: `"concentrated"`

Defined in: types.ts:88

***

### FULL\_RANGE

> **FULL\_RANGE**: `"full-range"`

Defined in: types.ts:87


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / Permissions

# Enumeration: Permissions

Defined in: types.ts:97

Enumeration of Permissions for TreasuryManagers. Defaults to OPEN.

## Enumeration Members

### CLOSED

> **CLOSED**: `"closed"`

Defined in: types.ts:99

***

### OPEN

> **OPEN**: `"open"`

Defined in: types.ts:98

***

### WHITELISTED

> **WHITELISTED**: `"whitelisted"`

Defined in: types.ts:100


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / Verifier

# Enumeration: Verifier

Defined in: types.ts:77

Enumeration of Verifiers for TokenImporter

## Enumeration Members

### CLANKER

> **CLANKER**: `"clanker"`

Defined in: types.ts:78

***

### DOPPLER

> **DOPPLER**: `"doppler"`

Defined in: types.ts:79

***

### SOLANA

> **SOLANA**: `"solana"`

Defined in: types.ts:80

***

### VIRTUALS

> **VIRTUALS**: `"virtuals"`

Defined in: types.ts:81

***

### WHITELIST

> **WHITELIST**: `"whitelist"`

Defined in: types.ts:82

***

### ZORA

> **ZORA**: `"zora"`

Defined in: types.ts:83


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / acquisitionPoolAbi

# Function: acquisitionPoolAbi()

> **acquisitionPoolAbi**(`dex`): readonly \[\{ `inputs`: readonly \[\]; `name`: `"slot0"`; `outputs`: readonly \[\{ `name`: `"sqrtPriceX96"`; `type`: `"uint160"`; \}, \{ `name`: `"tick"`; `type`: `"int24"`; \}, \{ `name`: `"observationIndex"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinality"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinalityNext"`; `type`: `"uint16"`; \}, \{ `name`: `"unlocked"`; `type`: `"bool"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"fee"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"uint24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"tickSpacing"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"int24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"token0"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}\] \| readonly \[\{ `inputs`: readonly \[\]; `name`: `"slot0"`; `outputs`: readonly \[\{ `name`: `"sqrtPriceX96"`; `type`: `"uint160"`; \}, \{ `name`: `"tick"`; `type`: `"int24"`; \}, \{ `name`: `"observationIndex"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinality"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinalityNext"`; `type`: `"uint16"`; \}, \{ `name`: `"feeProtocol"`; `type`: `"uint8"`; \}, \{ `name`: `"unlocked"`; `type`: `"bool"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"fee"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"uint24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"token0"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}\]

Defined in: utils/pairedTokenAcquisition.ts:208

The pool ABI a flavour's pools speak. Both expose `slot0`/`fee`/`token0`; only `slot0` differs.

## Parameters

### dex

`PairedTokenAcquisitionDex`

## Returns

readonly \[\{ `inputs`: readonly \[\]; `name`: `"slot0"`; `outputs`: readonly \[\{ `name`: `"sqrtPriceX96"`; `type`: `"uint160"`; \}, \{ `name`: `"tick"`; `type`: `"int24"`; \}, \{ `name`: `"observationIndex"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinality"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinalityNext"`; `type`: `"uint16"`; \}, \{ `name`: `"unlocked"`; `type`: `"bool"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"fee"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"uint24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"tickSpacing"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"int24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"token0"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}\] \| readonly \[\{ `inputs`: readonly \[\]; `name`: `"slot0"`; `outputs`: readonly \[\{ `name`: `"sqrtPriceX96"`; `type`: `"uint160"`; \}, \{ `name`: `"tick"`; `type`: `"int24"`; \}, \{ `name`: `"observationIndex"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinality"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinalityNext"`; `type`: `"uint16"`; \}, \{ `name`: `"feeProtocol"`; `type`: `"uint8"`; \}, \{ `name`: `"unlocked"`; `type`: `"bool"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"fee"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"uint24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"token0"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}\]


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / buyMemecoin

# Function: buyMemecoin()

> **buyMemecoin**(`params`): `object`

Defined in: utils/universalRouter.ts:231

## Parameters

### params

amountIn?

`bigint`

amountInMax?

`bigint`

amountOut?

`bigint`

amountOutMin?

`bigint`

chainId

`number`

hookData?

`` `0x${string}` ``

intermediatePoolKey?

`PoolWithHookData`

memecoin

`` `0x${string}` ``

permitSingle?

`PermitSingle`

positionManagerAddress

`` `0x${string}` ``

referrer

`` `0x${string}` `` \| `null`

sender

`` `0x${string}` ``

signature?

`` `0x${string}` ``

swapType

`"EXACT_IN"` \| `"EXACT_OUT"`

## Returns

`object`

### calldata

> **calldata**: `` `0x${string}` `` = `urExecuteCalldata`

### commands

> **commands**: `` `0x${string}` `` = `urCommands`

### inputs

> **inputs**: `` `0x${string}` ``[]


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / bytes32ToUint256

# Function: bytes32ToUint256()

> **bytes32ToUint256**(`value`): `bigint`

Defined in: helpers/hex.ts:3

## Parameters

### value

`` `0x${string}` ``

## Returns

`bigint`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / calculateUnderlyingTokenBalances

# Function: calculateUnderlyingTokenBalances()

> **calculateUnderlyingTokenBalances**(`liquidity`, `tickLower`, `tickUpper`, `tickCurrent`): `object`

Defined in: utils/univ4.ts:184

## Parameters

### liquidity

`bigint`

### tickLower

`number`

### tickUpper

`number`

### tickCurrent

`number`

## Returns

`object`

### amount0

> **amount0**: `bigint`

### amount1

> **amount1**: `bigint`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / clSpotAmountOut

# Function: clSpotAmountOut()

> **clSpotAmountOut**(`amountIn`, `sqrtPriceX96`, `feePips`, `zeroForOne`): `bigint`

Defined in: utils/pairedTokenAcquisition.ts:235

Spot output for an exact input against a CL pool at its current price: the fee comes off the
input, then the price ratio (`sqrtPriceX96²/2¹⁹²`, token1 per token0) converts it. Single-tick —
it ignores liquidity depth, so it slightly overstates output on sizes that cross ticks; the
caller's slippage haircut absorbs that and the router's `amountInMaximum` enforces it.

## Parameters

### amountIn

`bigint`

### sqrtPriceX96

`bigint`

### feePips

`bigint`

### zeroForOne

`boolean`

## Returns

`bigint`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / createCallDataWalletClient

# Function: createCallDataWalletClient()

> **createCallDataWalletClient**(`publicClient`, `walletAddress`): `object`

Defined in: sdk/calldata.ts:34

Creates a custom wallet client that returns encoded calldata instead of broadcasting transactions

## Parameters

### walletAddress

`` `0x${string}` ``

## Returns

`object`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / createDrift

# Function: createDrift()

Creates a Drift instance with the provided clients

## Param

**params**

Parameters for creating the Drift instance

## Throws

Error if publicClient.chain is not configured

## Call Signature

> **createDrift**(`params`): `Drift`

Defined in: sdk/drift.ts:20

Creates a read-only Drift instance with only public client

### Parameters

params

`Omit`\<`CreateDriftParams`, `"walletClient"`\>

Parameters with only publicClient

### Returns

`Drift`

Drift instance for read-only operations

### Throws

Error if publicClient.chain is not configured

## Call Signature

> **createDrift**(`params`): `Drift`\<`ReadWriteAdapter`\>

Defined in: sdk/drift.ts:29

Creates a read-write Drift instance with both public and wallet clients

### Parameters

params

`Required`\<`CreateDriftParams`\>

Parameters with both publicClient and walletClient

### Returns

`Drift`\<`ReadWriteAdapter`\>

Drift instance for read and write operations

### Throws

Error if publicClient.chain is not configured


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / createFlaunch

# Function: createFlaunch()

Creates a Flaunch SDK instance with the provided clients

## Param

**params**

Parameters for creating the SDK

## Throws

Error if publicClient.chain is not configured

## Call Signature

> **createFlaunch**(`params`): `ReadFlaunchSDK`

Defined in: sdk/factory.ts:18

Creates a read-only Flaunch SDK instance with only public client

### Parameters

params

`Omit`\<`CreateFlaunchParams`, `"walletClient"`\>

Parameters with only publicClient

### Returns

`ReadFlaunchSDK`

ReadFlaunchSDK for read-only operations

### Throws

Error if publicClient.chain is not configured

## Call Signature

> **createFlaunch**(`params`): `ReadWriteFlaunchSDK`

Defined in: sdk/factory.ts:27

Creates a read-write Flaunch SDK instance with both public and wallet clients

### Parameters

params

`Required`\<`CreateFlaunchParams`\>

Parameters with both publicClient and walletClient

### Returns

`ReadWriteFlaunchSDK`

ReadWriteFlaunchSDK for read and write operations

### Throws

Error if publicClient.chain is not configured


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / createFlaunchCalldata

# Function: createFlaunchCalldata()

Creates a Flaunch SDK instance that returns encoded calldata instead of broadcasting transactions

## Param

**params**

Parameters containing publicClient and optionally walletAddress

## Throws

Error if publicClient.chain is not configured

## Call Signature

> **createFlaunchCalldata**(`params`): `ReadFlaunchSDK`

Defined in: sdk/calldata.ts:129

Creates a read-only Flaunch SDK instance with only public client

### Parameters

params

`Omit`\<`CreateFlaunchCalldataParams`, `"walletAddress"`\>

Parameters with only publicClient

### Returns

`ReadFlaunchSDK`

ReadFlaunchSDK for read-only operations

### Throws

Error if publicClient.chain is not configured

## Call Signature

> **createFlaunchCalldata**(`params`): `ReadWriteFlaunchSDK`

Defined in: sdk/calldata.ts:138

Creates a read-write Flaunch SDK instance that returns encoded calldata instead of broadcasting transactions

### Parameters

params

`Required`\<`CreateFlaunchCalldataParams`\>

Parameters containing publicClient and walletAddress

### Returns

`ReadWriteFlaunchSDK`

ReadWriteFlaunchSDK instance configured for calldata generation

### Throws

Error if publicClient.chain is not configured


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / decodeBalanceDelta

# Function: decodeBalanceDelta()

> **decodeBalanceDelta**(`delta`): `object`

Defined in: utils/univ4.ts:496

A Uniswap v4 `BalanceDelta` packs two int128s into one int256: amount0 in the upper 128 bits,
amount1 in the lower. Positive = the pool owes the swapper, negative = the swapper owes the pool.

## Parameters

### delta

`bigint`

## Returns

`object`

### amount0

> **amount0**: `bigint`

### amount1

> **amount1**: `bigint`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / decodeCallData

# Function: decodeCallData()

> **decodeCallData**(`encodedCall`): `CallData`

Defined in: sdk/calldata.ts:184

Decodes the result from SDK methods into transaction object

## Parameters

### encodedCall

`` `0x${string}` ``

The result returned by SDK methods

## Returns

`CallData`

Call data containing to, value, and data


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / doesChainSupportMultiAssetManagers

# Function: doesChainSupportMultiAssetManagers()

> **doesChainSupportMultiAssetManagers**(`chainId`): `boolean`

Defined in: helpers/supportedChains.ts:66

Whether the v1.3.1 multi-asset manager generation (its own TreasuryManagerFactory,
manager implementations and FlaunchManagerZap) is deployed on the given chain — Base
mainnet and Robinhood Chain today. Managers from this generation pay out per payout asset (ETH or the
coin's paired token) and are driven through the `*V1_3` manager APIs; the unsuffixed
manager APIs keep talking to the previous generation. Gate on this before deploying,
reading or claiming from a v1.3.1 manager.

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / doesChainSupportMultiTokenFeeEscrow

# Function: doesChainSupportMultiTokenFeeEscrow()

> **doesChainSupportMultiTokenFeeEscrow**(`chainId`): `boolean`

Defined in: helpers/supportedChains.ts:54

Whether the v1.3.1 multi-token FeeEscrow is deployed on the given chain.
Coins launched against a non-flETH pairing (native ETH, the B20 equities)
escrow their creator fees there, keyed (recipient, token) and denominated in
that token; `creatorRevenue()` / `withdrawCreatorRevenue()` only see the
legacy single-token escrow, so gate on this before reading or claiming.

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / doesChainSupportMultichainNativeETHSwap

# Function: doesChainSupportMultichainNativeETHSwap()

> **doesChainSupportMultichainNativeETHSwap**(`chainId`): `boolean`

Defined in: addresses.ts:530

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / doesChainSupportPairedTokenAcquisition

# Function: doesChainSupportPairedTokenAcquisition()

> **doesChainSupportPairedTokenAcquisition**(`chainId`): `boolean`

Defined in: helpers/supportedChains.ts:127

Whether a non-ETH paired token (a B20 equity) can be bought from ETH or the chain's USD hub
through the SDK's acquisition route. Base Sepolia's mUSD has no venue — it is minted.

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / doesChainSupportPairedTokenLaunch

# Function: doesChainSupportPairedTokenLaunch()

> **doesChainSupportPairedTokenLaunch**(`chainId`): `boolean`

Defined in: helpers/supportedChains.ts:74

Whether all contracts required for paired-token launches are deployed.

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / doesChainSupportPairedTokenSwap

# Function: doesChainSupportPairedTokenSwap()

> **doesChainSupportPairedTokenSwap**(`chainId`): `boolean`

Defined in: helpers/supportedChains.ts:101

Whether a coin on the paired-token PositionManager can be swapped through the SDK: the v1.3.1
PoolSwap address plus the v4 Quoter and StateView. This is an infrastructure check only;
planPairedTokenSwap also probes the selected router for the protected exact-input API and
fails closed on legacy deployments.

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / doesChainSupportSplitManager

# Function: doesChainSupportSplitManager()

> **doesChainSupportSplitManager**(`chainId`): `boolean`

Defined in: helpers/supportedChains.ts:43

Whether the DynamicAddressFeeSplitManager is deployed
on the given chain. Split-manager flaunches revert on chains without a
deployment, so callers should gate on this before using them.

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / doesUniversalRouterUseV4HopPriceLimits

# Function: doesUniversalRouterUseV4HopPriceLimits()

> **doesUniversalRouterUseV4HopPriceLimits**(`chainId`): `boolean`

Defined in: addresses.ts:540

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / encodeAcquisitionEthBuy

# Function: encodeAcquisitionEthBuy()

> **encodeAcquisitionEthBuy**(`dex`, `leg`): `` `0x${string}` ``

Defined in: utils/pairedTokenAcquisition.ts:351

Router calldata for an ETH buy of `amountOut` paired token: ETH wraps inside the router (path
input is WETH9 + msg.value), multihops WETH → hub → token exact-output, and `refundETH` returns
whatever the leg did not need. Send with `value = amountInMaximum`.

## Parameters

### dex

`PairedTokenAcquisitionDex`

### leg

`PairedTokenAcquisitionLeg`

## Returns

`` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / encodeAcquisitionEthExactOutputPath

# Function: encodeAcquisitionEthExactOutputPath()

> **encodeAcquisitionEthExactOutputPath**(`dex`, `pairedToken`, `route`): `` `0x${string}` ``

Defined in: utils/pairedTokenAcquisition.ts:251

The exact-OUTPUT path for an ETH buy: output token first (`token → hub → WETH`), each hop's pool
key packed into the 3 bytes a V3 path spends on the fee. A Slipstream tickSpacing is a small
positive int24, so the uint24 packing is the value that router decodes too.

## Parameters

### dex

`PairedTokenAcquisitionDex`

### pairedToken

`` `0x${string}` ``

### route

`PairedTokenAcquisitionRoute`

## Returns

`` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / encodeAcquisitionExactInputPath

# Function: encodeAcquisitionExactInputPath()

> **encodeAcquisitionExactInputPath**(`dex`, `pairedToken`, `route`, `input`): `` `0x${string}` ``

Defined in: utils/pairedTokenAcquisition.ts:266

The exact-INPUT path for a venue (input first), the mirror of the exact-output path.

## Parameters

### dex

`PairedTokenAcquisitionDex`

### pairedToken

`` `0x${string}` ``

### route

`PairedTokenAcquisitionRoute`

### input

`PairedTokenAcquisitionInput`

## Returns

`` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / encodeAcquisitionHubBuy

# Function: encodeAcquisitionHubBuy()

> **encodeAcquisitionHubBuy**(`dex`, `leg`): `` `0x${string}` ``

Defined in: utils/pairedTokenAcquisition.ts:300

Router calldata for a hub-token (USDC/USDG) buy of `amountOut` paired token: a single-pool
exact-output swap, paid by allowance pull. Flavour differences are confined here.

## Parameters

### dex

`PairedTokenAcquisitionDex`

### leg

`PairedTokenAcquisitionLeg`

## Returns

`` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / encodeRevenueManagerClaimData

# Function: encodeRevenueManagerClaimData()

> **encodeRevenueManagerClaimData**(`flaunchTokens`): `` `0x${string}` ``

Defined in: clients/RevenueManagerV1\_3Client.ts:33

Encodes flaunch tokens as the `_data` argument of `claim(address[] assets, bytes data)`

## Parameters

### flaunchTokens

`FlaunchToken`[]

The flaunch tokens to claim against

## Returns

`` `0x${string}` ``

The ABI-encoded `FlaunchToken[]`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / generateTokenUri

# Function: generateTokenUri()

> **generateTokenUri**(`name`, `symbol`, `params`): `Promise`\<`string`\>

Defined in: helpers/ipfs.ts:318

## Parameters

### name

`string`

### symbol

`string`

### params

`IPFSParams`

## Returns

`Promise`\<`string`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getAmountWithSlippage

# Function: getAmountWithSlippage()

> **getAmountWithSlippage**(`__namedParameters`): `bigint`

Defined in: utils/universalRouter.ts:141

## Parameters

### \_\_namedParameters

amount

`bigint` \| `undefined`

slippage

`string`

swapType

`"EXACT_IN"` \| `"EXACT_OUT"`

## Returns

`bigint`

## Dev

EXACT_OUT adds the slippage, EXACT_IN removes it


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getAmountsForLiquidity

# Function: getAmountsForLiquidity()

> **getAmountsForLiquidity**(`params`): `object`

Defined in: utils/univ4.ts:389

Calculate the actual amounts needed for a given liquidity
This prevents MaximumAmountExceeded errors by ensuring we provide sufficient maximums

## Parameters

### params

currentTick

`number`

liquidity

`bigint`

tickLower

`number`

tickUpper

`number`

## Returns

`object`

### amount0

> **amount0**: `bigint`

### amount1

> **amount1**: `bigint`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getLiquidityFromAmounts

# Function: getLiquidityFromAmounts()

> **getLiquidityFromAmounts**(`params`): `bigint`

Defined in: utils/univ4.ts:293

Accurate liquidity calculation using proper Uniswap v3/v4 math

## Parameters

### params

amount0

`bigint`

amount1

`bigint`

currentTick

`number`

tickLower

`number`

tickUpper

`number`

## Returns

`bigint`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getNearestUsableTick

# Function: getNearestUsableTick()

> **getNearestUsableTick**(`__namedParameters`): `number`

Defined in: utils/univ4.ts:72

## Parameters

### \_\_namedParameters

tick

`number`

tickSpacing

`number`

## Returns

`number`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getPermissionsAddress

# Function: getPermissionsAddress()

> **getPermissionsAddress**(`permissions`, `chainId`): `` `0x${string}` ``

Defined in: helpers/permissions.ts:15

Maps a Permissions enum value to its corresponding contract address

## Parameters

### permissions

`Permissions`

The permissions enum value

### chainId

`number`

The chain ID to get the address for

## Returns

`` `0x${string}` ``

The corresponding permissions contract address


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getPermissionsAddressV1\_3

# Function: getPermissionsAddressV1\_3()

> **getPermissionsAddressV1\_3**(`permissions`, `chainId`): `` `0x${string}` ``

Defined in: helpers/permissions.ts:39

Maps a Permissions enum value to the contract address to set on a v1.3.1 multi-asset
manager. WhitelistedPermissions validates a group against the factory it was deployed
with, so managers from the v1.3.1 factory must use the v1.3.1 instance; ClosedPermissions
is factory-agnostic and shared with the previous generation.

## Parameters

### permissions

`Permissions`

The permissions enum value

### chainId

`number`

The chain ID to get the address for

## Returns

`` `0x${string}` ``

The corresponding permissions contract address


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getPermit2TypedData

# Function: getPermit2TypedData()

> **getPermit2TypedData**(`__namedParameters`): `object`

Defined in: utils/universalRouter.ts:700

## Parameters

### \_\_namedParameters

chainId

`number`

coinAddress

`` `0x${string}` ``

deadline?

`bigint`

nonce

`number`

## Returns

`object`

### permitSingle

> **permitSingle**: `PermitSingle`

### typedData

> **typedData**: `object`

typedData.domain

> **domain**: `object`

typedData.domain.chainId

> **chainId**: `number`

typedData.domain.name

> **name**: `string`

typedData.domain.verifyingContract

> **verifyingContract**: `` `0x${string}` ``

typedData.message

> **message**: `PermitSingle`

typedData.primaryType

> **primaryType**: `string`

typedData.types

> **types**: `object`

typedData.types.PermitDetails

> **PermitDetails**: `object`[] = `PERMIT_DETAILS`

typedData.types.PermitSingle

> **PermitSingle**: `object`[]


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getPoolId

# Function: getPoolId()

> **getPoolId**(`poolKey`): `` `0x${string}` ``

Defined in: utils/univ4.ts:15

## Parameters

### poolKey

`PoolKey`

## Returns

`` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getSqrtPriceX96FromTick

# Function: getSqrtPriceX96FromTick()

> **getSqrtPriceX96FromTick**(`tick`): `bigint`

Defined in: utils/univ4.ts:122

## Parameters

### tick

`number`

## Returns

`bigint`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getV1\_3PositionManagers

# Function: getV1\_3PositionManagers()

> **getV1\_3PositionManagers**(`chainId`): `` `0x${string}` ``[]

Defined in: helpers/supportedChains.ts:87

Every v1.3 hook that has ever been the paired-token PositionManager on a chain: the current
one plus any superseded generations that still serve their pools. Use this when deciding
whether an arbitrary hook address (e.g. from an indexer `Pool.positionManager`) is a v1.3 hook.

## Parameters

### chainId

`number`

## Returns

`` `0x${string}` ``[]


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / getValidTick

# Function: getValidTick()

> **getValidTick**(`__namedParameters`): `number`

Defined in: utils/univ4.ts:41

## Parameters

### \_\_namedParameters

roundDown?

`boolean` = `true`

tick

`number`

tickSpacing

`number`

## Returns

`number`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / isChainSupported

# Function: isChainSupported()

> **isChainSupported**(`chainId`): `boolean`

Defined in: helpers/supportedChains.ts:34

## Parameters

### chainId

`number`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / isEmptyPoolKey

# Function: isEmptyPoolKey()

> **isEmptyPoolKey**(`poolKey`): `boolean`

Defined in: utils/univ4.ts:489

Whether `poolKey` names a pool at all (an unknown coin reads back as a zeroed key).

## Parameters

### poolKey

`PoolKey`

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / isZeroForOne

# Function: isZeroForOne()

> **isZeroForOne**(`poolKey`, `tokenIn`): `boolean`

Defined in: utils/univ4.ts:476

Whether a swap selling `tokenIn` into this key moves currency0 → currency1.

## Parameters

### poolKey

`PoolKey`

### tokenIn

`` `0x${string}` ``

## Returns

`boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / maxLiquidityForAmount0Precise

# Function: maxLiquidityForAmount0Precise()

> **maxLiquidityForAmount0Precise**(`sqrtRatioAX96`, `sqrtRatioBX96`, `amount0`): `bigint`

Defined in: utils/univ4.ts:339

Returns a precise maximum amount of liquidity received for a given amount of token 0 by dividing by Q64 instead of Q96 in the intermediate step,
and shifting the subtracted ratio left by 32 bits.

## Parameters

### sqrtRatioAX96

`bigint`

The price at the lower boundary

### sqrtRatioBX96

`bigint`

The price at the upper boundary

### amount0

`bigint`

The token0 amount

## Returns

`bigint`

liquidity for amount0, precise


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / maxLiquidityForAmount1

# Function: maxLiquidityForAmount1()

> **maxLiquidityForAmount1**(`sqrtRatioAX96`, `sqrtRatioBX96`, `amount1`): `bigint`

Defined in: utils/univ4.ts:367

Computes the maximum amount of liquidity received for a given amount of token1

## Parameters

### sqrtRatioAX96

`bigint`

The price at the lower tick boundary

### sqrtRatioBX96

`bigint`

The price at the upper tick boundary

### amount1

`bigint`

The token1 amount

## Returns

`bigint`

liquidity for amount1


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / orderPoolKey

# Function: orderPoolKey()

> **orderPoolKey**(`poolKey`): `object`

Defined in: utils/univ4.ts:28

## Parameters

### poolKey

`PoolKey`

## Returns

`object`

### currency0

> **currency0**: `` `0x${string}` ``

### currency1

> **currency1**: `` `0x${string}` ``

### fee

> **fee**: `number`

### hooks

> **hooks**: `` `0x${string}` ``

### tickSpacing

> **tickSpacing**: `number`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / pairedPoolKey

# Function: pairedPoolKey()

> **pairedPoolKey**(`memecoin`, `pairedToken`, `hooks`): `PoolKey`

Defined in: utils/univ4.ts:459

The PoolKey of a paired-token pool, built without an RPC read: currencies sort numerically (so
native `address(0)` always lands in `currency0`), fee/tickSpacing are fixed at launch, and the
hook is the paired-token PositionManager. `pairedToken === zeroAddress` is a REAL value (raw
native ETH), not a default. `poolKey(memecoin)` on the hook the coin was launched on returns the same thing on chain.

## Parameters

### memecoin

`` `0x${string}` ``

### pairedToken

`` `0x${string}` ``

### hooks

`` `0x${string}` ``

## Returns

`PoolKey`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / pairedTokenOfPoolKey

# Function: pairedTokenOfPoolKey()

> **pairedTokenOfPoolKey**(`poolKey`, `memecoin`): `` `0x${string}` ``

Defined in: utils/univ4.ts:480

The non-coin side of a paired pool's key; `address(0)` means native ETH.

## Parameters

### poolKey

`PoolKey`

### memecoin

`` `0x${string}` ``

## Returns

`` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / parseCall

# Function: parseCall()

> **parseCall**\<`T`\>(`sdkMethod`): `Promise`\<`CallData`\>

Defined in: sdk/calldata.ts:202

Convenience function to call an SDK method and automatically parse the result as transaction object

## Type Parameters

### T

`T` *extends* (...`args`) => `Promise`\<`` `0x${string}` ``\>

## Parameters

### sdkMethod

`T`

A function that returns a Promise<Hex> (encoded calldata)

## Returns

`Promise`\<`CallData`\>

Promise<CallData> with decoded transaction parameters


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / parseSwapData

# Function: parseSwapData()

> **parseSwapData**(`args`, `flETHIsCurrencyZero`): `ParsedSwapData`

Defined in: utils/parseSwap.ts:47

Parses raw swap log arguments into structured swap data

## Parameters

### args

`SwapLogArgs`

The swap log arguments

### flETHIsCurrencyZero

`boolean`

Whether flETH is currency 0 in the pool

## Returns

`ParsedSwapData`

Parsed swap data with type and delta information


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / poolSwapForHook

# Function: poolSwapForHook()

> **poolSwapForHook**(`chainId`, `hook`): `` `0x${string}` `` \| `undefined`

Defined in: helpers/supportedChains.ts:116

The PoolSwap a swap against a pool on `hook` must go through. Ungated swaps could use any
PoolSwap (it is hook-agnostic), but a spend-gated buy is only accepted from a router that pool's
own spend gate has approved, and each hook generation ships its own gate — so the router follows
the hook. Falls back to the chain's current router for a hook the table does not name.

## Parameters

### chainId

`number`

### hook

`` `0x${string}` ``

## Returns

`` `0x${string}` `` \| `undefined`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / priceRatioToTick

# Function: priceRatioToTick()

> **priceRatioToTick**(`__namedParameters`): `number`

Defined in: utils/univ4.ts:229

## Parameters

### \_\_namedParameters

decimals0

`number`

decimals1

`number`

isDirection1Per0

`boolean`

priceInput

`string`

shouldGetNearestUsableTick?

`boolean` = `true`

spacing

`number`

## Returns

`number`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / resolveIPFS

# Function: resolveIPFS()

> **resolveIPFS**(`value`): `string`

Defined in: helpers/ipfs.ts:14

## Parameters

### value

`string`

## Returns

`string`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / sellMemecoinWithPermit2

# Function: sellMemecoinWithPermit2()

> **sellMemecoinWithPermit2**(`params`): `object`

Defined in: utils/universalRouter.ts:503

## Parameters

### params

amountIn

`bigint`

amountOutMin

`bigint`

chainId

`number`

hookData?

`` `0x${string}` ``

intermediatePoolKey?

`PoolWithHookData`

memecoin

`` `0x${string}` ``

permitSingle?

`PermitSingle`

positionManagerAddress

`` `0x${string}` ``

referrer

`` `0x${string}` `` \| `null`

signature?

`` `0x${string}` ``

## Returns

`object`

### calldata

> **calldata**: `` `0x${string}` `` = `urExecuteCalldata`

### commands

> **commands**: `` `0x${string}` `` = `urCommands`

### inputs

> **inputs**: `` `0x${string}` ``[]


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / sqrtPriceLimitFromSlippage

# ~~Function: sqrtPriceLimitFromSlippage()~~

> **sqrtPriceLimitFromSlippage**(`currentSqrtPriceX96`, `slippageBps`, `zeroForOne`): `bigint`

Defined in: utils/univ4.ts:526

## Parameters

### currentSqrtPriceX96

`bigint`

the pool's spot `sqrtPriceX96` (StateView `getSlot0`)

### slippageBps

`number`

tolerance in basis points, 1..9999 (50 = 0.5%)

### zeroForOne

`boolean`

## Returns

`bigint`

## Deprecated

A terminal spot-price limit is not quote-anchored slippage protection and may partially fill.
Converts an output-price tolerance into the v4 terminal sqrt-price bound for a legacy PoolSwap.
Price is `sqrtPriceX96²`: a zero-for-one swap may push price down to `P × (1 - s)`, a
one-for-zero swap may push it up to `P / (1 - s)`, so the inverse output price falls by at most
`s` either way.

## Throws

when the tolerance is out of range, or so small the bound would not move off spot


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / uint256ToBytes32

# Function: uint256ToBytes32()

> **uint256ToBytes32**(`value`): `` `0x${string}` ``

Defined in: helpers/hex.ts:7

## Parameters

### value

`bigint`

## Returns

`` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / uploadFileToIPFS

# Function: uploadFileToIPFS()

> **uploadFileToIPFS**(`params`): `Promise`\<`UploadResponse`\>

Defined in: helpers/ipfs.ts:42

Uploads a file to IPFS using Pinata

## Parameters

### params

Configuration and file data

file

`File`

metadata?

`Record`\<`string`, `string`\>

name?

`string`

pinataConfig

`PinataConfig`

## Returns

`Promise`\<`UploadResponse`\>

Upload response with CID and other details


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / uploadImageToFlaunchAPI

# Function: uploadImageToFlaunchAPI()

> **uploadImageToFlaunchAPI**(`base64Image`): `Promise`\<`FlaunchUploadResponse`\>

Defined in: helpers/ipfs.ts:253

Uploads a base64 image to IPFS using the Flaunch API

## Parameters

### base64Image

`string`

Base64 encoded image data

## Returns

`Promise`\<`FlaunchUploadResponse`\>

Upload response with IPFS hash


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / uploadImageToIPFS

# Function: uploadImageToIPFS()

> **uploadImageToIPFS**(`params`): `Promise`\<`UploadResponse`\>

Defined in: helpers/ipfs.ts:149

Uploads a base64 image to IPFS using Pinata

## Parameters

### params

Configuration and base64 image data

base64Image

`string`

metadata?

`Record`\<`string`, `string`\>

name?

`string`

pinataConfig

`PinataConfig`

## Returns

`Promise`\<`UploadResponse`\>

Upload response with CID and other details


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / uploadJsonToIPFS

# Function: uploadJsonToIPFS()

> **uploadJsonToIPFS**(`params`): `Promise`\<`UploadResponse`\>

Defined in: helpers/ipfs.ts:97

Uploads JSON data to IPFS using Pinata

## Parameters

### params

Configuration and JSON data

json

`Record`\<`string`, `any`\>

metadata?

`Record`\<`string`, `string`\>

name?

`string`

pinataConfig

`PinataConfig`

## Returns

`Promise`\<`UploadResponse`\>

Upload response with CID and other details


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / uploadMetadataToFlaunchAPI

# Function: uploadMetadataToFlaunchAPI()

> **uploadMetadataToFlaunchAPI**(`metadata`): `Promise`\<`FlaunchUploadResponse`\>

Defined in: helpers/ipfs.ts:288

Uploads metadata to IPFS using the Flaunch API

## Parameters

### metadata

`FlaunchMetadataRequest`

The metadata object to upload

## Returns

`Promise`\<`FlaunchUploadResponse`\>

Upload response with IPFS hash


---

**@flaunch/sdk v0.13.0**

***

# @flaunch/sdk v0.13.0

## Enumerations

- FlaunchVersion
- LiquidityMode
- Permissions
- Verifier

## Classes

- FlaunchBackend
- PairedSwapPartialFillError
- PairedSwapSlippageExceededError
- PairedSwapUnsupportedRouterError
- ReadDynamicAddressFeeSplitManager
- ReadDynamicAddressFeeSplitManagerV1\_3
- ReadFeeEscrowV1\_3
- ReadFlaunchManagerZapV1\_3
- ReadFlaunchSDK
- ReadFlaunchZapV1\_3
- ReadPairedTokenAcquisition
- ReadPairedTokenPositionManagerV1\_3
- ReadPairedTokenRegistryV1\_3
- ReadPoolSwapV1\_3
- ReadRevenueManagerV1\_3
- ReadStakingManagerV1\_3
- ReadTreasuryManagerV1\_3
- ReadWriteDynamicAddressFeeSplitManager
- ReadWriteDynamicAddressFeeSplitManagerV1\_3
- ReadWriteFeeEscrowV1\_3
- ReadWriteFlaunchManagerZapV1\_3
- ReadWriteFlaunchSDK
- ReadWriteFlaunchZapV1\_3
- ReadWritePoolSwapV1\_3
- ReadWriteRevenueManagerV1\_3
- ReadWriteStakingManagerV1\_3
- ReadWriteTreasuryManagerV1\_3

## Interfaces

- Addresses
- AssetBalance
- BuySwapData
- CallData
- CoinMetadata
- DeployManagerV1\_3Params
- EscrowTokenBalance
- FlaunchWithDynamicSplitManagerIPFSParams
- FlaunchWithDynamicSplitManagerParams
- IPFSParams
- PairedTokenAcquisitionDex
- PairedTokenAcquisitionLeg
- PairedTokenAcquisitionQuote
- PairedTokenAcquisitionRoute
- PinataConfig
- PoolKey
- PoolWithHookData
- SellSwapData
- SingleSidedLiquidityInfo
- SwapFees
- SwapLogArgs

## Type Aliases

- BaseSwapLog
- BuySwapLog
- CalculateAddLiquidityAmountsParams
- CalculatePairedTokenFlaunchFeeParams
- CallDataMethod
- CallDataResult
- CallWithDescription
- CheckSingleSidedAddLiquidityParams
- CreateFlaunchCalldataParams
- CreateFlaunchParams
- DynamicRecipientInfo
- DynamicRecipientInfoV1\_3
- FlaunchPairedTokenParams
- GetAddLiquidityCallsParams
- GetSingleSidedCoinAddLiquidityCallsParams
- ImportAndAddLiquidityParams
- ImportAndAddLiquidityWithExactAmounts
- ImportAndAddLiquidityWithMarketCap
- ImportAndAddLiquidityWithPrice
- ImportAndSingleSidedCoinAddLiquidityParams
- ImportAndSingleSidedCoinAddLiquidityWithMarketCap
- ImportAndSingleSidedCoinAddLiquidityWithPrice
- ImportMemecoinParams
- PairedPoolQuoteParams
- PairedSwapApproveCall
- PairedSwapCall
- PairedSwapDirection
- PairedSwapFill
- PairedSwapPlan
- PairedSwapPlanParams
- PairedSwapVerification
- PairedTokenAcquisitionDexFlavor
- PairedTokenAcquisitionInput
- PairedTokenAcquisitionParams
- PairedTokenAcquisitionPlan
- PairedTokenApprovalParams
- PairedTokenConfig
- PairedTokenFlaunchFee
- PairedTokenFlaunchParams
- PairedTokenSwapParams
- PairedTokenType
- ParsedSwapData
- PermitDetails
- PermitSingle
- PoolCreatedEventData
- PoolCreatedLogs
- PoolSwapLog
- PoolSwapParams
- PoolSwapV1\_3SwapParams
- RecipientShare
- ResolvedPairedPool
- SellSwapLog
- StakePositionV1\_3
- StakeRewardsV1\_3

## Variables

- AddressFeeSplitManagerAddress
- AddressFeeSplitManagerV1\_3Address
- AnyBidWallAddress
- AnyBidWallV1\_3Address
- AnyFlaunchAddress
- AnyFlaunchV1\_3Address
- AnyPositionManagerAddress
- AnyPositionManagerV1\_3Address
- BidWallAddress
- BidWallV1\_1Address
- BidWallV1\_3Address
- BuyBackManagerAddress
- chainIdToChain
- ClankerWorldVerifierAddress
- ClosedPermissionsAddress
- DopplerVerifierAddress
- DynamicAddressFeeSplitManagerAddress
- DynamicAddressFeeSplitManagerV1\_3Address
- encodedCallAbi
- ERC721OwnerFeeSplitManagerV1\_3Address
- FairLaunchAddress
- FairLaunchV1\_1Address
- FastFlaunchZapAddress
- FeeEscrowAddress
- FeeEscrowV1\_3Address
- FlaunchAddress
- FlaunchManagerZapV1\_3Address
- FlaunchMultichainAddress
- FlaunchPositionManagerAddress
- FlaunchPositionManagerMultichainAddress
- FlaunchPositionManagerV1\_1Address
- FlaunchPositionManagerV1\_2Address
- FlaunchPositionManagerV1\_3Address
- FlaunchSDK
- FlaunchV1\_1Address
- FlaunchV1\_2Address
- FlaunchV1\_3Address
- FlaunchZapAddress
- FlaunchZapMultichainAddress
- FlaunchZapV1\_3Address
- FLETHAddress
- FLETHHooksAddress
- GroupMapperV1\_3Address
- MAX\_SQRT\_PRICE\_LIMIT
- MIN\_SQRT\_PRICE\_LIMIT
- PAIRED\_TOKEN\_TYPE
- PairedTokenAcquisitionDexAddress
- PairedTokenPositionManagerV1\_3Address
- PairedTokenRegistryV1\_3Address
- PERMIT\_DETAILS
- PERMIT\_TYPES
- Permit2Address
- PoolManagerAddress
- PoolSwapForHookV1\_3Address
- PoolSwapV1\_3Address
- Q192
- Q96
- QuoterAddress
- ReferralEscrowAddress
- ReferralEscrowV1\_3Address
- RevenueManagerAddress
- RevenueManagerV1\_3Address
- SlipstreamPoolAbi
- SlipstreamSwapRouterAbi
- SolanaVerifierAddress
- StakingManagerAddress
- StakingManagerV1\_3Address
- StateViewAddress
- SupersededPositionManagerV1\_3Address
- TICK\_SPACING
- TickFinder
- TokenImporterAddress
- TokenImporterV1\_3Address
- TreasuryManagerFactoryAddress
- TreasuryManagerFactoryV1\_3Address
- TwoHopPriceCalculatorAbi
- UniswapV3FactoryAbi
- UniswapV3PoolAbi
- UniswapV3QuoterV2Abi
- UniswapV3SwapRouter02Abi
- UniV4PositionManagerAddress
- UniversalRouterAddress
- USDCETHPoolKeys
- VirtualsVerifierAddress
- WhitelistedPermissionsAddress
- WhitelistedPermissionsV1\_3Address
- WhitelistVerifierAddress
- ZoraVerifierAddress

## Functions

- acquisitionPoolAbi
- buyMemecoin
- bytes32ToUint256
- calculateUnderlyingTokenBalances
- clSpotAmountOut
- createCallDataWalletClient
- createDrift
- createFlaunch
- createFlaunchCalldata
- decodeBalanceDelta
- decodeCallData
- doesChainSupportMultiAssetManagers
- doesChainSupportMultichainNativeETHSwap
- doesChainSupportMultiTokenFeeEscrow
- doesChainSupportPairedTokenAcquisition
- doesChainSupportPairedTokenLaunch
- doesChainSupportPairedTokenSwap
- doesChainSupportSplitManager
- doesUniversalRouterUseV4HopPriceLimits
- encodeAcquisitionEthBuy
- encodeAcquisitionEthExactOutputPath
- encodeAcquisitionExactInputPath
- encodeAcquisitionHubBuy
- encodeRevenueManagerClaimData
- generateTokenUri
- getAmountsForLiquidity
- getAmountWithSlippage
- getLiquidityFromAmounts
- getNearestUsableTick
- getPermissionsAddress
- getPermissionsAddressV1\_3
- getPermit2TypedData
- getPoolId
- getSqrtPriceX96FromTick
- getV1\_3PositionManagers
- getValidTick
- isChainSupported
- isEmptyPoolKey
- isZeroForOne
- maxLiquidityForAmount0Precise
- maxLiquidityForAmount1
- orderPoolKey
- pairedPoolKey
- pairedTokenOfPoolKey
- parseCall
- parseSwapData
- poolSwapForHook
- priceRatioToTick
- resolveIPFS
- sellMemecoinWithPermit2
- ~~sqrtPriceLimitFromSlippage~~
- uint256ToBytes32
- uploadFileToIPFS
- uploadImageToFlaunchAPI
- uploadImageToIPFS
- uploadJsonToIPFS
- uploadMetadataToFlaunchAPI


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / Addresses

# Interface: Addresses

Defined in: types.ts:9

## Indexable

> \[`chainId`: `number`\]: `` `0x${string}` ``


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AssetBalance

# Interface: AssetBalance

Defined in: clients/TreasuryManagerV1\_3Client.ts:17

A claimable balance on a v1.3.1 multi-asset manager, in the payout asset's own raw units.

## Properties

### amount

> **amount**: `bigint`

Defined in: clients/TreasuryManagerV1\_3Client.ts:20

***

### asset

> **asset**: `` `0x${string}` ``

Defined in: clients/TreasuryManagerV1\_3Client.ts:19

The payout asset — `zeroAddress` for native ETH


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / BuySwapData

# Interface: BuySwapData

Defined in: utils/parseSwap.ts:21

## Properties

### delta

> **delta**: `object`

Defined in: utils/parseSwap.ts:23

coinsBought

> **coinsBought**: `bigint`

fees

> **fees**: `SwapFees`

flETHSold

> **flETHSold**: `bigint`

***

### type

> **type**: `"BUY"`

Defined in: utils/parseSwap.ts:22


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CallData

# Interface: CallData

Defined in: sdk/calldata.ts:20

## Properties

### data

> **data**: `` `0x${string}` ``

Defined in: sdk/calldata.ts:23

***

### to

> **to**: `` `0x${string}` ``

Defined in: sdk/calldata.ts:21

***

### value

> **value**: `bigint`

Defined in: sdk/calldata.ts:22


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CoinMetadata

# Interface: CoinMetadata

Defined in: types.ts:40

## Properties

### collaborators

> **collaborators**: `string`[]

Defined in: types.ts:45

***

### description

> **description**: `string`

Defined in: types.ts:42

***

### discordUrl

> **discordUrl**: `string`

Defined in: types.ts:46

***

### external\_link

> **external\_link**: `string`

Defined in: types.ts:44

***

### image

> **image**: `string`

Defined in: types.ts:43

***

### name

> **name**: `string`

Defined in: types.ts:41

***

### telegramUrl

> **telegramUrl**: `string`

Defined in: types.ts:48

***

### twitterUrl

> **twitterUrl**: `string`

Defined in: types.ts:47


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / DeployManagerV1\_3Params

# Interface: DeployManagerV1\_3Params

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:25

## Properties

### data

> **data**: `` `0x${string}` ``

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:31

The ABI-encoded initialization params of the implementation

***

### managerImplementation

> **managerImplementation**: `` `0x${string}` ``

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:27

A manager implementation approved on the v1.3.1 TreasuryManagerFactory (e.g. `RevenueManagerV1_3Address`)

***

### owner

> **owner**: `` `0x${string}` ``

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:29

The address that will own the deployed manager

***

### permissions?

> `optional` **permissions?**: `Permissions`

Defined in: clients/FlaunchManagerZapV1\_3Client.ts:33

The permissions to set on the manager. Defaults to OPEN


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / EscrowTokenBalance

# Interface: EscrowTokenBalance

Defined in: clients/FeeEscrowV1\_3Client.ts:14

A claimable balance on the multi-token escrow, in the escrow token's own raw units.

## Properties

### amount

> **amount**: `bigint`

Defined in: clients/FeeEscrowV1\_3Client.ts:17

***

### token

> **token**: `` `0x${string}` ``

Defined in: clients/FeeEscrowV1\_3Client.ts:16

The escrow token key — `zeroAddress` for native ETH


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchWithDynamicSplitManagerIPFSParams

# Interface: FlaunchWithDynamicSplitManagerIPFSParams

Defined in: clients/FlaunchZapClient.ts:115

## Extends

- `Omit`\<`FlaunchWithDynamicSplitManagerParams`, `"tokenUri"`\>.`IPFSParams`

## Properties

### creator

> **creator**: `` `0x${string}` ``

Defined in: clients/FlaunchZapClient.ts:41

Inherited from

`Omit.creator`

***

### creatorFeeAllocationPercent

> **creatorFeeAllocationPercent**: `number`

Defined in: clients/FlaunchZapClient.ts:42

Inherited from

`Omit.creatorFeeAllocationPercent`

***

### creatorShare

> **creatorShare**: `bigint`

Defined in: clients/FlaunchZapClient.ts:103

Inherited from

`FlaunchWithDynamicSplitManagerParams`.`creatorShare`

***

### fairLaunchDuration

> **fairLaunchDuration**: `number`

Defined in: clients/FlaunchZapClient.ts:39

Inherited from

`Omit.fairLaunchDuration`

***

### ~~fairLaunchPercent~~

> **fairLaunchPercent**: `number`

Defined in: clients/FlaunchZapClient.ts:38

Deprecated

FairLaunch has been deprecated. Please set fairLaunchPercent to 0.

Inherited from

`Omit.fairLaunchPercent`

***

### flaunchAt?

> `optional` **flaunchAt?**: `bigint`

Defined in: clients/FlaunchZapClient.ts:43

Inherited from

`Omit.flaunchAt`

***

### initialMarketCapUSD

> **initialMarketCapUSD**: `number`

Defined in: clients/FlaunchZapClient.ts:40

Inherited from

`Omit.initialMarketCapUSD`

***

### managerOwnerShare

> **managerOwnerShare**: `bigint`

Defined in: clients/FlaunchZapClient.ts:104

Inherited from

`FlaunchWithDynamicSplitManagerParams`.`managerOwnerShare`

***

### metadata

> **metadata**: `object`

Defined in: types.ts:52

base64Image

> **base64Image**: `string`

description

> **description**: `string`

discordUrl?

> `optional` **discordUrl?**: `string`

telegramUrl?

> `optional` **telegramUrl?**: `string`

twitterUrl?

> `optional` **twitterUrl?**: `string`

websiteUrl?

> `optional` **websiteUrl?**: `string`

Inherited from

`IPFSParams`.`metadata`

***

### moderator

> **moderator**: `` `0x${string}` ``

Defined in: clients/FlaunchZapClient.ts:105

Inherited from

`FlaunchWithDynamicSplitManagerParams`.`moderator`

***

### name

> **name**: `string`

Defined in: clients/FlaunchZapClient.ts:34

Inherited from

`Omit.name`

***

### pinataConfig?

> `optional` **pinataConfig?**: `PinataConfig`

Defined in: types.ts:60

Inherited from

`IPFSParams`.`pinataConfig`

***

### premineAmount?

> `optional` **premineAmount?**: `bigint`

Defined in: clients/FlaunchZapClient.ts:44

Inherited from

`Omit.premineAmount`

***

### splitReceivers

> **splitReceivers**: `object`[]

Defined in: clients/FlaunchZapClient.ts:106

address

> **address**: `` `0x${string}` ``

share

> **share**: `bigint`

Inherited from

`Omit.splitReceivers`

***

### symbol

> **symbol**: `string`

Defined in: clients/FlaunchZapClient.ts:35

Inherited from

`Omit.symbol`

***

### treasuryManagerParams?

> `optional` **treasuryManagerParams?**: `object`

Defined in: clients/FlaunchZapClient.ts:110

permissions?

> `optional` **permissions?**: `Permissions`

Inherited from

`Omit.treasuryManagerParams`

***

### trustedSignerSettings?

> `optional` **trustedSignerSettings?**: `object`

Defined in: clients/FlaunchZapClient.ts:53

enabled

> **enabled**: `boolean`

premineSignedMessage?

> `optional` **premineSignedMessage?**: `object`

#premineSignedMessage.deadline

> **deadline**: `number`

#premineSignedMessage.signature

> **signature**: `` `0x${string}` ``

trustedFeeSigner?

> `optional` **trustedFeeSigner?**: `` `0x${string}` ``

txCap?

> `optional` **txCap?**: `bigint`

walletCap?

> `optional` **walletCap?**: `bigint`

Inherited from

`Omit.trustedSignerSettings`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchWithDynamicSplitManagerParams

# Interface: FlaunchWithDynamicSplitManagerParams

Defined in: clients/FlaunchZapClient.ts:101

## Extends

- `Omit`\<`FlaunchParams`, `"treasuryManagerParams"`\>

## Properties

### creator

> **creator**: `` `0x${string}` ``

Defined in: clients/FlaunchZapClient.ts:41

Inherited from

`Omit.creator`

***

### creatorFeeAllocationPercent

> **creatorFeeAllocationPercent**: `number`

Defined in: clients/FlaunchZapClient.ts:42

Inherited from

`Omit.creatorFeeAllocationPercent`

***

### creatorShare

> **creatorShare**: `bigint`

Defined in: clients/FlaunchZapClient.ts:103

***

### fairLaunchDuration

> **fairLaunchDuration**: `number`

Defined in: clients/FlaunchZapClient.ts:39

Inherited from

`Omit.fairLaunchDuration`

***

### ~~fairLaunchPercent~~

> **fairLaunchPercent**: `number`

Defined in: clients/FlaunchZapClient.ts:38

Deprecated

FairLaunch has been deprecated. Please set fairLaunchPercent to 0.

Inherited from

`Omit.fairLaunchPercent`

***

### flaunchAt?

> `optional` **flaunchAt?**: `bigint`

Defined in: clients/FlaunchZapClient.ts:43

Inherited from

`Omit.flaunchAt`

***

### initialMarketCapUSD

> **initialMarketCapUSD**: `number`

Defined in: clients/FlaunchZapClient.ts:40

Inherited from

`Omit.initialMarketCapUSD`

***

### managerOwnerShare

> **managerOwnerShare**: `bigint`

Defined in: clients/FlaunchZapClient.ts:104

***

### moderator

> **moderator**: `` `0x${string}` ``

Defined in: clients/FlaunchZapClient.ts:105

***

### name

> **name**: `string`

Defined in: clients/FlaunchZapClient.ts:34

Inherited from

`Omit.name`

***

### premineAmount?

> `optional` **premineAmount?**: `bigint`

Defined in: clients/FlaunchZapClient.ts:44

Inherited from

`Omit.premineAmount`

***

### splitReceivers

> **splitReceivers**: `object`[]

Defined in: clients/FlaunchZapClient.ts:106

address

> **address**: `` `0x${string}` ``

share

> **share**: `bigint`

***

### symbol

> **symbol**: `string`

Defined in: clients/FlaunchZapClient.ts:35

Inherited from

`Omit.symbol`

***

### tokenUri

> **tokenUri**: `string`

Defined in: clients/FlaunchZapClient.ts:36

Inherited from

`Omit.tokenUri`

***

### treasuryManagerParams?

> `optional` **treasuryManagerParams?**: `object`

Defined in: clients/FlaunchZapClient.ts:110

permissions?

> `optional` **permissions?**: `Permissions`

***

### trustedSignerSettings?

> `optional` **trustedSignerSettings?**: `object`

Defined in: clients/FlaunchZapClient.ts:53

enabled

> **enabled**: `boolean`

premineSignedMessage?

> `optional` **premineSignedMessage?**: `object`

#premineSignedMessage.deadline

> **deadline**: `number`

#premineSignedMessage.signature

> **signature**: `` `0x${string}` ``

trustedFeeSigner?

> `optional` **trustedFeeSigner?**: `` `0x${string}` ``

txCap?

> `optional` **txCap?**: `bigint`

walletCap?

> `optional` **walletCap?**: `bigint`

Inherited from

`Omit.trustedSignerSettings`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / IPFSParams

# Interface: IPFSParams

Defined in: types.ts:51

## Extended by

- `FlaunchWithDynamicSplitManagerIPFSParams`

## Properties

### metadata

> **metadata**: `object`

Defined in: types.ts:52

base64Image

> **base64Image**: `string`

description

> **description**: `string`

discordUrl?

> `optional` **discordUrl?**: `string`

telegramUrl?

> `optional` **telegramUrl?**: `string`

twitterUrl?

> `optional` **twitterUrl?**: `string`

websiteUrl?

> `optional` **websiteUrl?**: `string`

***

### pinataConfig?

> `optional` **pinataConfig?**: `PinataConfig`

Defined in: types.ts:60


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionDex

# Interface: PairedTokenAcquisitionDex

Defined in: addresses.ts:148

## Properties

### flavor

> **flavor**: `PairedTokenAcquisitionDexFlavor`

Defined in: addresses.ts:149

***

### hubSymbol

> **hubSymbol**: `string`

Defined in: addresses.ts:155

***

### hubToken

> **hubToken**: `` `0x${string}` ``

Defined in: addresses.ts:154

The USD hub every equity pool quotes in: USDC on Base, USDG on Robinhood.

***

### swapRouter

> **swapRouter**: `` `0x${string}` ``

Defined in: addresses.ts:151

The router a routed buy is written to.

***

### venues?

> `optional` **venues?**: `object`

Defined in: addresses.ts:161

Venue discovery (`uniswapV3` only): factory, fee tiers to probe, and a QuoterV2 for depth-aware quotes.

factory

> **factory**: `` `0x${string}` ``

feeTiers

> **feeTiers**: readonly `number`[]

quoterV2?

> `optional` **quoterV2?**: `` `0x${string}` ``

***

### weth

> **weth**: `` `0x${string}` ``

Defined in: addresses.ts:152

***

### wethHubPool

> **wethHubPool**: `` `0x${string}` ``

Defined in: addresses.ts:157

The deep WETH:hub pool on the same factory, for the ETH → hub hop.

***

### wethHubPoolKey

> **wethHubPoolKey**: `number`

Defined in: addresses.ts:159

That pool's key in the path's 3-byte slot: `tickSpacing` (slipstream) or `fee` (uniswapV3).


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionLeg

# Interface: PairedTokenAcquisitionLeg

Defined in: utils/pairedTokenAcquisition.ts:284

## Properties

### amountInMaximum

> **amountInMaximum**: `bigint`

Defined in: utils/pairedTokenAcquisition.ts:293

The whole input — the price protection; the leg reverts rather than spend more.

***

### amountOut

> **amountOut**: `bigint`

Defined in: utils/pairedTokenAcquisition.ts:291

The exact paired-token amount the leg must deliver.

***

### deadline

> **deadline**: `bigint`

Defined in: utils/pairedTokenAcquisition.ts:289

Unix seconds after which the router refuses to execute.

***

### pairedToken

> **pairedToken**: `` `0x${string}` ``

Defined in: utils/pairedTokenAcquisition.ts:285

***

### recipient

> **recipient**: `` `0x${string}` ``

Defined in: utils/pairedTokenAcquisition.ts:287

***

### route

> **route**: `PairedTokenAcquisitionRoute`

Defined in: utils/pairedTokenAcquisition.ts:286


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionQuote

# Interface: PairedTokenAcquisitionQuote

Defined in: clients/PairedTokenAcquisitionClient.ts:20

## Properties

### expectedOut

> **expectedOut**: `bigint`

Defined in: clients/PairedTokenAcquisitionClient.ts:23

Expected paired-token output for the exact input, before any slippage haircut.

***

### route

> **route**: `PairedTokenAcquisitionRoute`

Defined in: clients/PairedTokenAcquisitionClient.ts:21

***

### source

> **source**: `"quoter"` \| `"spot"`

Defined in: clients/PairedTokenAcquisitionClient.ts:25

How the number was produced — a depth-aware quoter, or the calculator pool's single-tick spot.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionRoute

# Interface: PairedTokenAcquisitionRoute

Defined in: utils/pairedTokenAcquisition.ts:215

## Properties

### direct?

> `optional` **direct?**: `boolean`

Defined in: utils/pairedTokenAcquisition.ts:223

A WETH:token pool reached in one hop from ETH, skipping the hub. Only chosen for an ETH input.

***

### payingIsToken0

> **payingIsToken0**: `boolean`

Defined in: utils/pairedTokenAcquisition.ts:221

Whether the paying side (hub, or WETH when `direct`) is the pool's token0.

***

### pool

> **pool**: `` `0x${string}` ``

Defined in: utils/pairedTokenAcquisition.ts:217

The pool the paired-token leg trades in: hub:token, or WETH:token when `direct`.

***

### poolKey

> **poolKey**: `number`

Defined in: utils/pairedTokenAcquisition.ts:219

The pool's key for the path / router params: `tickSpacing` (slipstream) or `fee` (uniswapV3).


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PinataConfig

# Interface: PinataConfig

Defined in: helpers/ipfs.ts:26

## Properties

### jwt

> **jwt**: `string`

Defined in: helpers/ipfs.ts:27


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolKey

# Interface: PoolKey

Defined in: types.ts:13

## Extended by

- `PoolWithHookData`

## Properties

### currency0

> **currency0**: `` `0x${string}` ``

Defined in: types.ts:14

***

### currency1

> **currency1**: `` `0x${string}` ``

Defined in: types.ts:15

***

### fee

> **fee**: `number`

Defined in: types.ts:16

***

### hooks

> **hooks**: `` `0x${string}` ``

Defined in: types.ts:18

***

### tickSpacing

> **tickSpacing**: `number`

Defined in: types.ts:17


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolWithHookData

# Interface: PoolWithHookData

Defined in: types.ts:21

## Extends

- `PoolKey`

## Properties

### currency0

> **currency0**: `` `0x${string}` ``

Defined in: types.ts:14

Inherited from

`PoolKey`.`currency0`

***

### currency1

> **currency1**: `` `0x${string}` ``

Defined in: types.ts:15

Inherited from

`PoolKey`.`currency1`

***

### fee

> **fee**: `number`

Defined in: types.ts:16

Inherited from

`PoolKey`.`fee`

***

### hookData

> **hookData**: `` `0x${string}` ``

Defined in: types.ts:22

***

### hooks

> **hooks**: `` `0x${string}` ``

Defined in: types.ts:18

Inherited from

`PoolKey`.`hooks`

***

### tickSpacing

> **tickSpacing**: `number`

Defined in: types.ts:17

Inherited from

`PoolKey`.`tickSpacing`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SellSwapData

# Interface: SellSwapData

Defined in: utils/parseSwap.ts:30

## Properties

### delta

> **delta**: `object`

Defined in: utils/parseSwap.ts:32

coinsSold

> **coinsSold**: `bigint`

fees

> **fees**: `SwapFees`

flETHBought

> **flETHBought**: `bigint`

***

### type

> **type**: `"SELL"`

Defined in: utils/parseSwap.ts:31


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SingleSidedLiquidityInfo

# Interface: SingleSidedLiquidityInfo

Defined in: types.ts:210

## Properties

### isSingleSided

> **isSingleSided**: `boolean`

Defined in: types.ts:211

***

### shouldHideCoinInput

> **shouldHideCoinInput**: `boolean`

Defined in: types.ts:212

***

### shouldHideETHInput

> **shouldHideETHInput**: `boolean`

Defined in: types.ts:213


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SwapFees

# Interface: SwapFees

Defined in: utils/parseSwap.ts:16

## Properties

### amount

> **amount**: `bigint`

Defined in: utils/parseSwap.ts:18

***

### isInFLETH

> **isInFLETH**: `boolean`

Defined in: utils/parseSwap.ts:17


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SwapLogArgs

# Interface: SwapLogArgs

Defined in: utils/parseSwap.ts:1

## Properties

### flAmount0

> **flAmount0**: `bigint`

Defined in: utils/parseSwap.ts:2

***

### flAmount1

> **flAmount1**: `bigint`

Defined in: utils/parseSwap.ts:3

***

### flFee0

> **flFee0**: `bigint`

Defined in: utils/parseSwap.ts:4

***

### flFee1

> **flFee1**: `bigint`

Defined in: utils/parseSwap.ts:5

***

### ispAmount0

> **ispAmount0**: `bigint`

Defined in: utils/parseSwap.ts:6

***

### ispAmount1

> **ispAmount1**: `bigint`

Defined in: utils/parseSwap.ts:7

***

### ispFee0

> **ispFee0**: `bigint`

Defined in: utils/parseSwap.ts:8

***

### ispFee1

> **ispFee1**: `bigint`

Defined in: utils/parseSwap.ts:9

***

### uniAmount0

> **uniAmount0**: `bigint`

Defined in: utils/parseSwap.ts:10

***

### uniAmount1

> **uniAmount1**: `bigint`

Defined in: utils/parseSwap.ts:11

***

### uniFee0

> **uniFee0**: `bigint`

Defined in: utils/parseSwap.ts:12

***

### uniFee1

> **uniFee1**: `bigint`

Defined in: utils/parseSwap.ts:13


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / BaseSwapLog

# Type Alias: BaseSwapLog

> **BaseSwapLog** = `EventLog`\<`FlaunchPositionManagerABI`, `"PoolSwap"`\> & `object`

Defined in: clients/FlaunchPositionManagerClient.ts:40

## Type Declaration

### timestamp

> **timestamp**: `number`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / BuySwapLog

# Type Alias: BuySwapLog

> **BuySwapLog** = `BaseSwapLog` & `object`

Defined in: clients/FlaunchPositionManagerClient.ts:44

## Type Declaration

### delta

> **delta**: `object`

delta.coinsBought

> **coinsBought**: `bigint`

delta.fees

> **fees**: `object`

delta.fees.amount

> **amount**: `bigint`

delta.fees.isInFLETH

> **isInFLETH**: `boolean`

delta.flETHSold

> **flETHSold**: `bigint`

### type

> **type**: `"BUY"`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CalculateAddLiquidityAmountsParams

# Type Alias: CalculateAddLiquidityAmountsParams

> **CalculateAddLiquidityAmountsParams** = \{ `coinAddress`: `Address`; `coinOrEthInputAmount`: `bigint`; `currentMarketCap?`: `string`; `inputToken`: `"coin"` \| `"eth"`; `liquidityMode`: `LiquidityMode`; `maxMarketCap`: `string`; `minMarketCap`: `string`; `version?`: `FlaunchVersion`; \} \| \{ `coinAddress`: `Address`; `coinOrEthInputAmount`: `bigint`; `currentPriceUSD?`: `number`; `inputToken`: `"coin"` \| `"eth"`; `liquidityMode`: `LiquidityMode`; `maxPriceUSD`: `string`; `minPriceUSD`: `string`; `version?`: `FlaunchVersion`; \}

Defined in: types.ts:118


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CalculatePairedTokenFlaunchFeeParams

# Type Alias: CalculatePairedTokenFlaunchFeeParams

> **CalculatePairedTokenFlaunchFeeParams** = `object`

Defined in: clients/FlaunchZapV1\_3Client.ts:32

## Properties

### flaunchParams

> **flaunchParams**: `PairedTokenFlaunchParams`

Defined in: clients/FlaunchZapV1\_3Client.ts:33

***

### slippageBps

> **slippageBps**: `bigint`

Defined in: clients/FlaunchZapV1\_3Client.ts:34


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CallDataMethod

# Type Alias: CallDataMethod\<T\>

> **CallDataMethod**\<`T`\> = (...`args`) => `Promise`\<`Hex`\>

Defined in: sdk/calldata.ts:212

Type helper for SDK methods that return encoded calldata

## Type Parameters

### T

`T` *extends* `any`[] = `any`[]

## Parameters

### args

...`T`

## Returns

`Promise`\<`Hex`\>


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CallDataResult

# Type Alias: CallDataResult

> **CallDataResult** = `Promise`\<`CallData`\>

Defined in: sdk/calldata.ts:219

Type helper for the result of a calldata operation


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CallWithDescription

# Type Alias: CallWithDescription

> **CallWithDescription** = `Call` & `object`

Defined in: types.ts:91

## Type Declaration

### description?

> `optional` **description?**: `string`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CheckSingleSidedAddLiquidityParams

# Type Alias: CheckSingleSidedAddLiquidityParams

> **CheckSingleSidedAddLiquidityParams** = \{ `coinAddress`: `Address`; `currentMarketCap?`: `string`; `liquidityMode`: `LiquidityMode`; `maxMarketCap`: `string`; `minMarketCap`: `string`; `version?`: `FlaunchVersion`; \} \| \{ `coinAddress`: `Address`; `currentPriceUSD?`: `number`; `liquidityMode`: `LiquidityMode`; `maxPriceUSD`: `string`; `minPriceUSD`: `string`; `version?`: `FlaunchVersion`; \}

Defined in: types.ts:192


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CreateFlaunchCalldataParams

# Type Alias: CreateFlaunchCalldataParams

> **CreateFlaunchCalldataParams** = `object`

Defined in: sdk/calldata.ts:26

## Properties

### publicClient

> **publicClient**: `PublicClient`

Defined in: sdk/calldata.ts:27

***

### walletAddress?

> `optional` **walletAddress?**: `Address`

Defined in: sdk/calldata.ts:28


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / CreateFlaunchParams

# Type Alias: CreateFlaunchParams

> **CreateFlaunchParams** = `object`

Defined in: sdk/factory.ts:7

## Properties

### publicClient

> **publicClient**: `PublicClient`

Defined in: sdk/factory.ts:8

***

### walletClient?

> `optional` **walletClient?**: `WalletClient`

Defined in: sdk/factory.ts:9


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / DynamicRecipientInfo

# Type Alias: DynamicRecipientInfo

> **DynamicRecipientInfo** = `object`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:21

## Properties

### claimed

> **claimed**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:25

***

### debtPerShare

> **debtPerShare**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:23

***

### share

> **share**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:22

***

### snapshotBalance

> **snapshotBalance**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:24


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / DynamicRecipientInfoV1\_3

# Type Alias: DynamicRecipientInfoV1\_3

> **DynamicRecipientInfoV1\_3** = `object`

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:20

A recipient's split position in one payout asset

## Properties

### claimed

> **claimed**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:24

***

### debtPerShare

> **debtPerShare**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:22

***

### share

> **share**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:21

***

### snapshotBalance

> **snapshotBalance**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerV1\_3Client.ts:23


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchPairedTokenParams

# Type Alias: FlaunchPairedTokenParams

> **FlaunchPairedTokenParams** = `object`

Defined in: clients/FlaunchZapV1\_3Client.ts:37

## Properties

### flaunchParams

> **flaunchParams**: `PairedTokenFlaunchParams`

Defined in: clients/FlaunchZapV1\_3Client.ts:38

***

### maxPremineCost

> **maxPremineCost**: `bigint`

Defined in: clients/FlaunchZapV1\_3Client.ts:40

***

### trustedFeeSigner

> **trustedFeeSigner**: `Address`

Defined in: clients/FlaunchZapV1\_3Client.ts:39

***

### value

> **value**: `bigint`

Defined in: clients/FlaunchZapV1\_3Client.ts:41


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / GetAddLiquidityCallsParams

# Type Alias: GetAddLiquidityCallsParams

> **GetAddLiquidityCallsParams** = \{ `coinAddress`: `Address`; `coinOrEthInputAmount`: `bigint`; `initialMarketCapUSD?`: `number`; `inputToken`: `"coin"` \| `"eth"`; `liquidityMode`: `LiquidityMode`; `maxMarketCap`: `string`; `minMarketCap`: `string`; `slippagePercent?`: `number`; `version?`: `FlaunchVersion`; \} \| \{ `coinAddress`: `Address`; `coinOrEthInputAmount`: `bigint`; `initialPriceUSD?`: `number`; `inputToken`: `"coin"` \| `"eth"`; `liquidityMode`: `LiquidityMode`; `maxPriceUSD`: `string`; `minPriceUSD`: `string`; `slippagePercent?`: `number`; `version?`: `FlaunchVersion`; \} \| \{ `coinAddress`: `Address`; `coinAmount`: `bigint`; `currentTick?`: `number`; `flethAmount`: `bigint`; `slippagePercent?`: `number`; `tickLower`: `number`; `tickUpper`: `number`; `version?`: `FlaunchVersion`; \}

Defined in: types.ts:140


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / GetSingleSidedCoinAddLiquidityCallsParams

# Type Alias: GetSingleSidedCoinAddLiquidityCallsParams

> **GetSingleSidedCoinAddLiquidityCallsParams** = \{ `coinAddress`: `Address`; `coinAmount`: `bigint`; `initialMarketCapUSD?`: `number`; `slippagePercent?`: `number`; `tokenSupply?`: `bigint`; `version?`: `FlaunchVersion`; \} \| \{ `coinAddress`: `Address`; `coinAmount`: `bigint`; `initialPriceUSD?`: `number`; `slippagePercent?`: `number`; `tokenSupply?`: `bigint`; `version?`: `FlaunchVersion`; \}

Defined in: types.ts:174


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ImportAndAddLiquidityParams

# Type Alias: ImportAndAddLiquidityParams

> **ImportAndAddLiquidityParams** = `ImportAndAddLiquidityWithMarketCap` \| `ImportAndAddLiquidityWithPrice` \| `ImportAndAddLiquidityWithExactAmounts`

Defined in: types.ts:259


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ImportAndAddLiquidityWithExactAmounts

# Type Alias: ImportAndAddLiquidityWithExactAmounts

> **ImportAndAddLiquidityWithExactAmounts** = `Flatten`\<\{ `coinAddress`: `Address`; `coinAmount`: `bigint`; `creatorFeeAllocationPercent`: `number`; `currentTick?`: `number`; `flethAmount`: `bigint`; `slippagePercent?`: `number`; `tickLower`: `number`; `tickUpper`: `number`; `verifier?`: `Verifier`; `version?`: `FlaunchVersion`; \}\>

Defined in: types.ts:245


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ImportAndAddLiquidityWithMarketCap

# Type Alias: ImportAndAddLiquidityWithMarketCap

> **ImportAndAddLiquidityWithMarketCap** = `Flatten`\<\{ `coinAddress`: `Address`; `coinOrEthInputAmount`: `bigint`; `creatorFeeAllocationPercent`: `number`; `initialMarketCapUSD`: `number`; `inputToken`: `"coin"` \| `"eth"`; `liquidityMode`: `LiquidityMode`; `maxMarketCap`: `string`; `minMarketCap`: `string`; `slippagePercent?`: `number`; `verifier?`: `Verifier`; `version?`: `FlaunchVersion`; \}\>

Defined in: types.ts:217


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ImportAndAddLiquidityWithPrice

# Type Alias: ImportAndAddLiquidityWithPrice

> **ImportAndAddLiquidityWithPrice** = `Flatten`\<\{ `coinAddress`: `Address`; `coinOrEthInputAmount`: `bigint`; `creatorFeeAllocationPercent`: `number`; `initialPriceUSD`: `number`; `inputToken`: `"coin"` \| `"eth"`; `liquidityMode`: `LiquidityMode`; `maxPriceUSD`: `string`; `minPriceUSD`: `string`; `slippagePercent?`: `number`; `verifier?`: `Verifier`; `version?`: `FlaunchVersion`; \}\>

Defined in: types.ts:231


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ImportAndSingleSidedCoinAddLiquidityParams

# Type Alias: ImportAndSingleSidedCoinAddLiquidityParams

> **ImportAndSingleSidedCoinAddLiquidityParams** = `ImportAndSingleSidedCoinAddLiquidityWithMarketCap` \| `ImportAndSingleSidedCoinAddLiquidityWithPrice`

Defined in: types.ts:286


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ImportAndSingleSidedCoinAddLiquidityWithMarketCap

# Type Alias: ImportAndSingleSidedCoinAddLiquidityWithMarketCap

> **ImportAndSingleSidedCoinAddLiquidityWithMarketCap** = `Flatten`\<\{ `coinAddress`: `Address`; `coinAmount`: `bigint`; `creatorFeeAllocationPercent`: `number`; `initialMarketCapUSD`: `number`; `verifier?`: `Verifier`; `version?`: `FlaunchVersion`; \}\>

Defined in: types.ts:267


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ImportAndSingleSidedCoinAddLiquidityWithPrice

# Type Alias: ImportAndSingleSidedCoinAddLiquidityWithPrice

> **ImportAndSingleSidedCoinAddLiquidityWithPrice** = `Flatten`\<\{ `coinAddress`: `Address`; `coinAmount`: `bigint`; `creatorFeeAllocationPercent`: `number`; `initialPriceUSD`: `number`; `verifier?`: `Verifier`; `version?`: `FlaunchVersion`; \}\>

Defined in: types.ts:276


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ImportMemecoinParams

# Type Alias: ImportMemecoinParams

> **ImportMemecoinParams** = \{ `coinAddress`: `Address`; `creatorFeeAllocationPercent`: `number`; `initialMarketCapUSD`: `number`; `verifier?`: `Verifier`; \} \| \{ `coinAddress`: `Address`; `creatorFeeAllocationPercent`: `number`; `initialPriceUSD`: `number`; `verifier?`: `Verifier`; \}

Defined in: types.ts:104


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedPoolQuoteParams

# Type Alias: PairedPoolQuoteParams

> **PairedPoolQuoteParams** = `object`

Defined in: sdk/FlaunchSDK.ts:482

## Properties

### amountIn

> **amountIn**: `bigint`

Defined in: sdk/FlaunchSDK.ts:485

***

### coinAddress

> **coinAddress**: `Address`

Defined in: sdk/FlaunchSDK.ts:483

***

### direction?

> `optional` **direction?**: `PairedSwapDirection`

Defined in: sdk/FlaunchSDK.ts:486

***

### hookData?

> `optional` **hookData?**: `Hex`

Defined in: sdk/FlaunchSDK.ts:487

***

### pairedToken?

> `optional` **pairedToken?**: `Address`

Defined in: sdk/FlaunchSDK.ts:484

***

### userWallet?

> `optional` **userWallet?**: `Address`

Defined in: sdk/FlaunchSDK.ts:489

Simulate as this wallet — required when the hook binds `hookData` to a buyer.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapApproveCall

# Type Alias: PairedSwapApproveCall

> **PairedSwapApproveCall** = `PairedSwapCall` & `object`

Defined in: sdk/FlaunchSDK.ts:500

## Type Declaration

### amount

> **amount**: `bigint`

### spender

> **spender**: `Address`

### token

> **token**: `Address`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapCall

# Type Alias: PairedSwapCall

> **PairedSwapCall** = `object`

Defined in: sdk/FlaunchSDK.ts:499

## Properties

### data

> **data**: `Hex`

Defined in: sdk/FlaunchSDK.ts:499

***

### to

> **to**: `Address`

Defined in: sdk/FlaunchSDK.ts:499

***

### value

> **value**: `bigint`

Defined in: sdk/FlaunchSDK.ts:499


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapDirection

# Type Alias: PairedSwapDirection

> **PairedSwapDirection** = `"buy"` \| `"sell"`

Defined in: sdk/FlaunchSDK.ts:396


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapFill

# Type Alias: PairedSwapFill

> **PairedSwapFill** = `object`

Defined in: sdk/FlaunchSDK.ts:514

## Properties

### amountIn

> **amountIn**: `bigint`

Defined in: sdk/FlaunchSDK.ts:517

***

### amountOut

> **amountOut**: `bigint`

Defined in: sdk/FlaunchSDK.ts:518

***

### direction

> **direction**: `PairedSwapDirection`

Defined in: sdk/FlaunchSDK.ts:515

***

### poolId

> **poolId**: `Hex`

Defined in: sdk/FlaunchSDK.ts:516


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapPlan

# Type Alias: PairedSwapPlan

> **PairedSwapPlan** = `ResolvedPairedPool` & `object`

Defined in: sdk/FlaunchSDK.ts:521

## Type Declaration

### amountIn

> **amountIn**: `bigint`

### amountOutMin

> **amountOutMin**: `bigint`

### approve?

> `optional` **approve?**: `PairedSwapApproveCall`

Present when the PoolSwap allowance is short of `amountIn` (never for native input).

### chainId

> **chainId**: `number`

### deadline

> **deadline**: `bigint`

### direction

> **direction**: `PairedSwapDirection`

### expectedAmountOut

> **expectedAmountOut**: `bigint`

### hookData

> **hookData**: `Hex`

### isNativeInput

> **isNativeInput**: `boolean`

True for a buy on a native-ETH pool: `amountIn` travels as `swap.value`, no approve needed.

### quoteBlockNumber

> **quoteBlockNumber**: `bigint`

### quotedAtMs

> **quotedAtMs**: `number`

### sender

> **sender**: `Address`

### slippageBps

> **slippageBps**: `number`

### spotDeviationBps

> **spotDeviationBps**: `number`

Fee-inclusive output shortfall versus AMM spot, not pure price impact.

### sqrtPriceLimitX96

> **sqrtPriceLimitX96**: `bigint`

### swap

> **swap**: `PairedSwapCall`

### tokenIn

> **tokenIn**: `Address`

### tokenOut

> **tokenOut**: `Address`

### zeroForOne

> **zeroForOne**: `boolean`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapPlanParams

# Type Alias: PairedSwapPlanParams

> **PairedSwapPlanParams** = `PairedTokenSwapParams` & `object`

Defined in: sdk/FlaunchSDK.ts:446

planPairedTokenSwap's input: the swap params plus which way the swap goes.

## Type Declaration

### direction

> **direction**: `PairedSwapDirection`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedSwapVerification

# Type Alias: PairedSwapVerification

> **PairedSwapVerification** = \{ `amountOut`: `bigint`; `mode`: `"quote"`; \} \| \{ `amountOut`: `bigint`; `consumedIn`: `bigint`; `mode`: `"simulate"`; \}

Defined in: sdk/FlaunchSDK.ts:511

Everything a host needs to execute a paired-token swap itself — as one batched
`wallet_sendCalls` or two sequential transactions: the optional ERC20 approve, then the PoolSwap
call. `buyCoinPairedToken` / `sellCoinPairedToken` run exactly this plan.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionDexFlavor

# Type Alias: PairedTokenAcquisitionDexFlavor

> **PairedTokenAcquisitionDexFlavor** = `"slipstream"` \| `"uniswapV3"`

Defined in: addresses.ts:147

Where a paired token that is NOT ETH-equivalent can be bought from ETH or the chain's USD hub:
the concentrated-liquidity venue holding the only liquidity for a chain's tokenised equities.
A buy that starts from ETH/USDC(/USDG) routes through it before the coin's own pool.

- `slipstream` (Base): the B20 DEX, a Slipstream-style CL fork on a non-canonical factory; buys go
  through its own SwapRouter. Pools are keyed by `tickSpacing`; params carry a `deadline`.
- `uniswapV3` (Robinhood): canonical Uniswap V3 with a `SwapRouter02` whose params drop the
  `deadline` (it rides on `multicall(deadline, …)`) and key pools by `fee`.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionInput

# Type Alias: PairedTokenAcquisitionInput

> **PairedTokenAcquisitionInput** = `"eth"` \| `"hub"`

Defined in: utils/pairedTokenAcquisition.ts:213

What a routed buy pays with: native ETH, or the chain's USD hub token (USDC / USDG).


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionParams

# Type Alias: PairedTokenAcquisitionParams

> **PairedTokenAcquisitionParams** = `object`

Defined in: sdk/FlaunchSDK.ts:450

## Properties

### deadline?

> `optional` **deadline?**: `bigint`

Defined in: sdk/FlaunchSDK.ts:459

Unix seconds; defaults to now + 10 minutes.

***

### input

> **input**: `PairedTokenAcquisitionInput`

Defined in: sdk/FlaunchSDK.ts:452

***

### maxIn

> **maxIn**: `bigint`

Defined in: sdk/FlaunchSDK.ts:456

The most the router may spend — the price protection; native ETH travels as `value`.

***

### pairedToken

> **pairedToken**: `Address`

Defined in: sdk/FlaunchSDK.ts:451

***

### recipient

> **recipient**: `Address`

Defined in: sdk/FlaunchSDK.ts:457

***

### route?

> `optional` **route?**: `PairedTokenAcquisitionRoute`

Defined in: sdk/FlaunchSDK.ts:468

The venue to execute on. Defaults to the registry calculator's route — but a caller that
QUOTED first must pass the quote's route through, or the plan may execute on a different
pool than the one that priced the target (a better-priced venue's target can exceed what
the calculator pool delivers within `maxIn`, reverting the buy).

***

### sender?

> `optional` **sender?**: `Address`

Defined in: sdk/FlaunchSDK.ts:461

For the hub-token allowance check; defaults to the drift signer, else the approve is always planned.

***

### target

> **target**: `bigint`

Defined in: sdk/FlaunchSDK.ts:454

Exactly this much paired token is delivered (exact-output).


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionPlan

# Type Alias: PairedTokenAcquisitionPlan

> **PairedTokenAcquisitionPlan** = `object`

Defined in: sdk/FlaunchSDK.ts:471

## Properties

### approve?

> `optional` **approve?**: `PairedSwapApproveCall`

Defined in: sdk/FlaunchSDK.ts:477

Present for a hub-token input whose router allowance is short of `maxIn`.

***

### input

> **input**: `PairedTokenAcquisitionInput`

Defined in: sdk/FlaunchSDK.ts:473

***

### maxIn

> **maxIn**: `bigint`

Defined in: sdk/FlaunchSDK.ts:475

***

### route

> **route**: `PairedTokenAcquisitionRoute`

Defined in: sdk/FlaunchSDK.ts:472

***

### swap

> **swap**: `PairedSwapCall`

Defined in: sdk/FlaunchSDK.ts:479

The router call; `value` is `maxIn` for an ETH input (the router refunds the unspent part).

***

### target

> **target**: `bigint`

Defined in: sdk/FlaunchSDK.ts:474


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenApprovalParams

# Type Alias: PairedTokenApprovalParams

> **PairedTokenApprovalParams** = `object`

Defined in: sdk/FlaunchSDK.ts:435

## Properties

### amount

> **amount**: `bigint`

Defined in: sdk/FlaunchSDK.ts:439

The allowance to set when the current one is short of it.

***

### coinAddress

> **coinAddress**: `Address`

Defined in: sdk/FlaunchSDK.ts:436

***

### pairedToken?

> `optional` **pairedToken?**: `Address`

Defined in: sdk/FlaunchSDK.ts:437

***

### router?

> `optional` **router?**: `Address`

Defined in: sdk/FlaunchSDK.ts:441

***

### sender?

> `optional` **sender?**: `Address`

Defined in: sdk/FlaunchSDK.ts:440


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenConfig

# Type Alias: PairedTokenConfig

> **PairedTokenConfig** = `object`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:11

A registry row for a paired token — `IPairedTokenRegistry.PairedToken`.

## Properties

### approved

> **approved**: `boolean`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:12

***

### bidWallThreshold

> **bidWallThreshold**: `bigint`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:20

***

### decimals

> **decimals**: `number`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:15

The token's own decimals (6 for mUSD/USDC, 8 for the B20 equities, 18 for flETH).

***

### feeEscrow

> **feeEscrow**: `Address`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:17

***

### minDistribute

> **minDistribute**: `bigint`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:19

***

### priceCalculator

> **priceCalculator**: `Address`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:18

***

### tokenType

> **tokenType**: `PairedTokenType`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:13

***

### underlying

> **underlying**: `Address`

Defined in: clients/PairedTokenRegistryV1\_3Client.ts:16


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenFlaunchFee

# Type Alias: PairedTokenFlaunchFee

> **PairedTokenFlaunchFee** = `object`

Defined in: clients/FlaunchZapV1\_3Client.ts:27

## Properties

### ethRequired

> **ethRequired**: `bigint`

Defined in: clients/FlaunchZapV1\_3Client.ts:28

***

### pairedPremineCost

> **pairedPremineCost**: `bigint`

Defined in: clients/FlaunchZapV1\_3Client.ts:29


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenFlaunchParams

# Type Alias: PairedTokenFlaunchParams

> **PairedTokenFlaunchParams** = `object`

Defined in: clients/FlaunchZapV1\_3Client.ts:14

## Properties

### creator

> **creator**: `Address`

Defined in: clients/FlaunchZapV1\_3Client.ts:19

***

### creatorFeeAllocation

> **creatorFeeAllocation**: `number`

Defined in: clients/FlaunchZapV1\_3Client.ts:20

***

### feeCalculatorParams

> **feeCalculatorParams**: `HexString`

Defined in: clients/FlaunchZapV1\_3Client.ts:23

***

### flaunchAt

> **flaunchAt**: `bigint`

Defined in: clients/FlaunchZapV1\_3Client.ts:21

***

### initialPriceParams

> **initialPriceParams**: `HexString`

Defined in: clients/FlaunchZapV1\_3Client.ts:22

***

### name

> **name**: `string`

Defined in: clients/FlaunchZapV1\_3Client.ts:15

***

### pairedToken

> **pairedToken**: `Address`

Defined in: clients/FlaunchZapV1\_3Client.ts:24

***

### premineAmount

> **premineAmount**: `bigint`

Defined in: clients/FlaunchZapV1\_3Client.ts:18

***

### symbol

> **symbol**: `string`

Defined in: clients/FlaunchZapV1\_3Client.ts:16

***

### tokenUri

> **tokenUri**: `string`

Defined in: clients/FlaunchZapV1\_3Client.ts:17


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenSwapParams

# Type Alias: PairedTokenSwapParams

> **PairedTokenSwapParams** = `object`

Defined in: sdk/FlaunchSDK.ts:403

An exact-input swap on a paired-token pool (a coin launched through `flaunchPairedToken`,
paired with mUSD, native ETH, flETH or a B20 equity). Exact-output is deliberately absent — the
spend gate rejects it. Requires a router with on-chain minimum output, deadline and full-fill checks.

## Properties

### amountIn

> **amountIn**: `bigint`

Defined in: sdk/FlaunchSDK.ts:408

Exact input, in the input currency's own decimals (a buy spends the paired token, a sell spends the coin).

***

### approvalAllowance?

> `optional` **approvalAllowance?**: `bigint`

Defined in: sdk/FlaunchSDK.ts:426

When an approve is needed, approve this much instead of `amountIn` (must be ≥ `amountIn`). A
standing allowance — a round's wallet cap, say — turns every later buy into a single swap call.

***

### coinAddress

> **coinAddress**: `Address`

Defined in: sdk/FlaunchSDK.ts:404

***

### deadline?

> `optional` **deadline?**: `bigint`

Defined in: sdk/FlaunchSDK.ts:412

Unix seconds; defaults to now + 10 minutes. Preserved in calldata.

***

### hookData?

> `optional` **hookData?**: `Hex`

Defined in: sdk/FlaunchSDK.ts:414

Bytes for the pool's hook — a spend-gated pool's signed authorisation.

***

### pairedToken?

> `optional` **pairedToken?**: `Address`

Defined in: sdk/FlaunchSDK.ts:406

The pool's paired side; resolved on chain from the PositionManager when omitted. `zeroAddress` = native ETH.

***

### referrer?

> `optional` **referrer?**: `Address`

Defined in: sdk/FlaunchSDK.ts:416

Referral attribution; ignored when `hookData` is given (the gate's payload leads with the referrer).

***

### router?

> `optional` **router?**: `Address`

Defined in: sdk/FlaunchSDK.ts:432

The PoolSwap to route through. Defaults to the router approved on the spend gate of the hook
the coin's pool lives on (`poolSwapForHook`); a host that learned the router from the gate's
own `/config` passes it here.

***

### sender?

> `optional` **sender?**: `Address`

Defined in: sdk/FlaunchSDK.ts:421

The wallet that will send the swap. Used for the ERC20 allowance check; defaults to the
drift signer. A quote-backed swap requires a sender; approval-only planning can omit it.

***

### slippageBps

> **slippageBps**: `number`

Defined in: sdk/FlaunchSDK.ts:410

Haircut below quoted net output, 0..9999 basis points (50 = 0.5%). Enforced on chain.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenType

# Type Alias: PairedTokenType

> **PairedTokenType** = *typeof* `PAIRED_TOKEN_TYPE`\keyof *typeof* [`PAIRED_TOKEN_TYPE`\]

Defined in: types.ts:37


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ParsedSwapData

# Type Alias: ParsedSwapData

> **ParsedSwapData** = `BuySwapData` \| `SellSwapData`

Defined in: utils/parseSwap.ts:39


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PermitDetails

# Type Alias: PermitDetails

> **PermitDetails** = `object`

Defined in: utils/universalRouter.ts:22

## Properties

### amount

> **amount**: `bigint`

Defined in: utils/universalRouter.ts:24

***

### expiration

> **expiration**: `number`

Defined in: utils/universalRouter.ts:25

***

### nonce

> **nonce**: `number`

Defined in: utils/universalRouter.ts:26

***

### token

> **token**: `Address`

Defined in: utils/universalRouter.ts:23


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PermitSingle

# Type Alias: PermitSingle

> **PermitSingle** = `object`

Defined in: utils/universalRouter.ts:29

## Properties

### details

> **details**: `PermitDetails`

Defined in: utils/universalRouter.ts:30

***

### sigDeadline

> **sigDeadline**: `bigint`

Defined in: utils/universalRouter.ts:32

***

### spender

> **spender**: `Address`

Defined in: utils/universalRouter.ts:31


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolCreatedEventData

# Type Alias: PoolCreatedEventData

> **PoolCreatedEventData** = `object`

Defined in: types.ts:293

Parsed data from a PoolCreated event

## Properties

### currencyFlipped

> **currencyFlipped**: `boolean`

Defined in: types.ts:298

***

### flaunchFee

> **flaunchFee**: `bigint`

Defined in: types.ts:299

***

### memecoin

> **memecoin**: `Address`

Defined in: types.ts:295

***

### memecoinTreasury

> **memecoinTreasury**: `Address`

Defined in: types.ts:296

***

### params

> **params**: `object`

Defined in: types.ts:300

creator

> **creator**: `Address`

creatorFeeAllocation

> **creatorFeeAllocation**: `number`

fairLaunchDuration?

> `optional` **fairLaunchDuration?**: `bigint`

feeCalculatorParams

> **feeCalculatorParams**: `Hex`

flaunchAt

> **flaunchAt**: `bigint`

initialPriceParams

> **initialPriceParams**: `Hex`

initialTokenFairLaunch

> **initialTokenFairLaunch**: `bigint`

name

> **name**: `string`

pairedToken?

> `optional` **pairedToken?**: `Address`

Undefined for pre-V1.3 pools; zeroAddress selects native ETH in V1.3.

premineAmount

> **premineAmount**: `bigint`

symbol

> **symbol**: `string`

tokenUri

> **tokenUri**: `string`

***

### poolId

> **poolId**: `Hex`

Defined in: types.ts:294

***

### tokenId

> **tokenId**: `bigint`

Defined in: types.ts:297


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolCreatedLogs

# Type Alias: PoolCreatedLogs

> **PoolCreatedLogs** = `PoolCreatedLog`[]

Defined in: clients/FlaunchPositionManagerClient.ts:27


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolSwapLog

# Type Alias: PoolSwapLog

> **PoolSwapLog** = `BuySwapLog` \| `SellSwapLog` \| `BaseSwapLog`

Defined in: clients/FlaunchPositionManagerClient.ts:68


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolSwapParams

# Type Alias: PoolSwapParams

> **PoolSwapParams** = `object`

Defined in: clients/PoolSwapV1\_3Client.ts:20

Uniswap v4 `SwapParams`: negative `amountSpecified` = exact input.

## Properties

### amountSpecified

> **amountSpecified**: `bigint`

Defined in: clients/PoolSwapV1\_3Client.ts:22

***

### sqrtPriceLimitX96

> **sqrtPriceLimitX96**: `bigint`

Defined in: clients/PoolSwapV1\_3Client.ts:23

***

### zeroForOne

> **zeroForOne**: `boolean`

Defined in: clients/PoolSwapV1\_3Client.ts:21


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolSwapV1\_3SwapParams

# Type Alias: PoolSwapV1\_3SwapParams

> **PoolSwapV1\_3SwapParams** = `object`

Defined in: clients/PoolSwapV1\_3Client.ts:26

## Properties

### hookData?

> `optional` **hookData?**: `HexString`

Defined in: clients/PoolSwapV1\_3Client.ts:34

Arbitrary bytes for the pool's hook — a spend-gated launch's signed authorisation travels here.
When present the `bytes` overload is used and `referrer` is ignored (the gate's payload already
leads with the referrer address).

***

### params

> **params**: `PoolSwapParams`

Defined in: clients/PoolSwapV1\_3Client.ts:28

***

### poolKey

> **poolKey**: `PoolKey`

Defined in: clients/PoolSwapV1\_3Client.ts:27

***

### referrer?

> `optional` **referrer?**: `Address`

Defined in: clients/PoolSwapV1\_3Client.ts:36

Referral attribution via the `address` overload; zero (or absent) means none.

***

### value?

> `optional` **value?**: `bigint`

Defined in: clients/PoolSwapV1\_3Client.ts:38

Native ETH input for a pool whose paired side is `address(0)`; 0 for ERC20 / flETH input.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / RecipientShare

# Type Alias: RecipientShare

> **RecipientShare** = `object`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:16

## Properties

### recipient

> **recipient**: `Address`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:17

***

### share

> **share**: `bigint`

Defined in: clients/DynamicAddressFeeSplitManagerClient.ts:18


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ResolvedPairedPool

# Type Alias: ResolvedPairedPool

> **ResolvedPairedPool** = `object`

Defined in: sdk/FlaunchSDK.ts:492

## Properties

### pairedToken

> **pairedToken**: `Address`

Defined in: sdk/FlaunchSDK.ts:496

The non-coin side of the key; `zeroAddress` is native ETH.

***

### poolId

> **poolId**: `Hex`

Defined in: sdk/FlaunchSDK.ts:494

***

### poolKey

> **poolKey**: `PoolKey`

Defined in: sdk/FlaunchSDK.ts:493


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SellSwapLog

# Type Alias: SellSwapLog

> **SellSwapLog** = `BaseSwapLog` & `object`

Defined in: clients/FlaunchPositionManagerClient.ts:56

## Type Declaration

### delta

> **delta**: `object`

delta.coinsSold

> **coinsSold**: `bigint`

delta.fees

> **fees**: `object`

delta.fees.amount

> **amount**: `bigint`

delta.fees.isInFLETH

> **isInFLETH**: `boolean`

delta.flETHBought

> **flETHBought**: `bigint`

### type

> **type**: `"SELL"`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / StakePositionV1\_3

# Type Alias: StakePositionV1\_3

> **StakePositionV1\_3** = `object`

Defined in: clients/StakingManagerV1\_3Client.ts:18

A user's staked position

## Properties

### amount

> **amount**: `bigint`

Defined in: clients/StakingManagerV1\_3Client.ts:19

***

### timelockedUntil

> **timelockedUntil**: `bigint`

Defined in: clients/StakingManagerV1\_3Client.ts:20


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / StakeRewardsV1\_3

# Type Alias: StakeRewardsV1\_3

> **StakeRewardsV1\_3** = `object`

Defined in: clients/StakingManagerV1\_3Client.ts:24

A user's reward checkpoint in one payout asset

## Properties

### owed

> **owed**: `bigint`

Defined in: clients/StakingManagerV1\_3Client.ts:27

Rewards deferred because the asset refused the transfer on `unstake`; paid on the next claim

***

### rewardsPerTokenSnapshotX128

> **rewardsPerTokenSnapshotX128**: `bigint`

Defined in: clients/StakingManagerV1\_3Client.ts:25


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AddressFeeSplitManagerAddress

# Variable: AddressFeeSplitManagerAddress

> `const` **AddressFeeSplitManagerAddress**: `Addresses`

Defined in: addresses.ts:294


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AddressFeeSplitManagerV1\_3Address

# Variable: AddressFeeSplitManagerV1\_3Address

> `const` **AddressFeeSplitManagerV1\_3Address**: `Addresses`

Defined in: addresses.ts:337


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AnyBidWallAddress

# Variable: AnyBidWallAddress

> `const` **AnyBidWallAddress**: `Addresses`

Defined in: addresses.ts:266


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AnyBidWallV1\_3Address

# Variable: AnyBidWallV1\_3Address

> `const` **AnyBidWallV1\_3Address**: `Addresses`

Defined in: addresses.ts:272


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AnyFlaunchAddress

# Variable: AnyFlaunchAddress

> `const` **AnyFlaunchAddress**: `Addresses`

Defined in: addresses.ts:225


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AnyFlaunchV1\_3Address

# Variable: AnyFlaunchV1\_3Address

> `const` **AnyFlaunchV1\_3Address**: `Addresses`

Defined in: addresses.ts:231


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AnyPositionManagerAddress

# Variable: AnyPositionManagerAddress

> `const` **AnyPositionManagerAddress**: `Addresses`

Defined in: addresses.ts:191


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / AnyPositionManagerV1\_3Address

# Variable: AnyPositionManagerV1\_3Address

> `const` **AnyPositionManagerV1\_3Address**: `Addresses`

Defined in: addresses.ts:197


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / BidWallAddress

# Variable: BidWallAddress

> `const` **BidWallAddress**: `Addresses`

Defined in: addresses.ts:248


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / BidWallV1\_1Address

# Variable: BidWallV1\_1Address

> `const` **BidWallV1\_1Address**: `Addresses`

Defined in: addresses.ts:254


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / BidWallV1\_3Address

# Variable: BidWallV1\_3Address

> `const` **BidWallV1\_3Address**: `Addresses`

Defined in: addresses.ts:260


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / BuyBackManagerAddress

# Variable: BuyBackManagerAddress

> `const` **BuyBackManagerAddress**: `Addresses`

Defined in: addresses.ts:315


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ClankerWorldVerifierAddress

# Variable: ClankerWorldVerifierAddress

> `const` **ClankerWorldVerifierAddress**: `Addresses`

Defined in: addresses.ts:390


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ClosedPermissionsAddress

# Variable: ClosedPermissionsAddress

> `const` **ClosedPermissionsAddress**: `Addresses`

Defined in: addresses.ts:423

Permissions


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / DopplerVerifierAddress

# Variable: DopplerVerifierAddress

> `const` **DopplerVerifierAddress**: `Addresses`

Defined in: addresses.ts:395


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / DynamicAddressFeeSplitManagerAddress

# Variable: DynamicAddressFeeSplitManagerAddress

> `const` **DynamicAddressFeeSplitManagerAddress**: `Addresses`

Defined in: addresses.ts:302


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / DynamicAddressFeeSplitManagerV1\_3Address

# Variable: DynamicAddressFeeSplitManagerV1\_3Address

> `const` **DynamicAddressFeeSplitManagerV1\_3Address**: `Addresses`

Defined in: addresses.ts:343


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ERC721OwnerFeeSplitManagerV1\_3Address

# Variable: ERC721OwnerFeeSplitManagerV1\_3Address

> `const` **ERC721OwnerFeeSplitManagerV1\_3Address**: `Addresses`

Defined in: addresses.ts:349


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FLETHAddress

# Variable: FLETHAddress

> `const` **FLETHAddress**: `Addresses`

Defined in: addresses.ts:479


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FLETHHooksAddress

# Variable: FLETHHooksAddress

> `const` **FLETHHooksAddress**: `Addresses`

Defined in: addresses.ts:548


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FairLaunchAddress

# Variable: FairLaunchAddress

> `const` **FairLaunchAddress**: `Addresses`

Defined in: addresses.ts:237


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FairLaunchV1\_1Address

# Variable: FairLaunchV1\_1Address

> `const` **FairLaunchV1\_1Address**: `Addresses`

Defined in: addresses.ts:243


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FastFlaunchZapAddress

# Variable: FastFlaunchZapAddress

> `const` **FastFlaunchZapAddress**: `Addresses`

Defined in: addresses.ts:555


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FeeEscrowAddress

# Variable: FeeEscrowAddress

> `const` **FeeEscrowAddress**: `Addresses`

Defined in: addresses.ts:449

===========


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FeeEscrowV1\_3Address

# Variable: FeeEscrowV1\_3Address

> `const` **FeeEscrowV1\_3Address**: `Addresses`

Defined in: addresses.ts:461


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchAddress

# Variable: FlaunchAddress

> `const` **FlaunchAddress**: `Addresses`

Defined in: addresses.ts:203


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchManagerZapV1\_3Address

# Variable: FlaunchManagerZapV1\_3Address

> `const` **FlaunchManagerZapV1\_3Address**: `Addresses`

Defined in: addresses.ts:369


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchMultichainAddress

# Variable: FlaunchMultichainAddress

> `const` **FlaunchMultichainAddress**: `Addresses`

Defined in: addresses.ts:40


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchPositionManagerAddress

# Variable: FlaunchPositionManagerAddress

> `const` **FlaunchPositionManagerAddress**: `Addresses`

Defined in: addresses.ts:47


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchPositionManagerMultichainAddress

# Variable: FlaunchPositionManagerMultichainAddress

> `const` **FlaunchPositionManagerMultichainAddress**: `Addresses`

Defined in: addresses.ts:34


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchPositionManagerV1\_1Address

# Variable: FlaunchPositionManagerV1\_1Address

> `const` **FlaunchPositionManagerV1\_1Address**: `Addresses`

Defined in: addresses.ts:52


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchPositionManagerV1\_2Address

# Variable: FlaunchPositionManagerV1\_2Address

> `const` **FlaunchPositionManagerV1\_2Address**: `Addresses`

Defined in: addresses.ts:57


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchPositionManagerV1\_3Address

# Variable: FlaunchPositionManagerV1\_3Address

> `const` **FlaunchPositionManagerV1\_3Address**: `Addresses`

Defined in: addresses.ts:65


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchSDK

# Variable: FlaunchSDK

> `const` **FlaunchSDK**: `object`

Defined in: index.ts:127

## Type Declaration

### ReadFlaunchSDK

> **ReadFlaunchSDK**: *typeof* `ReadFlaunchSDK`

### ReadWriteFlaunchSDK

> **ReadWriteFlaunchSDK**: *typeof* `ReadWriteFlaunchSDK`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchV1\_1Address

# Variable: FlaunchV1\_1Address

> `const` **FlaunchV1\_1Address**: `Addresses`

Defined in: addresses.ts:208


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchV1\_2Address

# Variable: FlaunchV1\_2Address

> `const` **FlaunchV1\_2Address**: `Addresses`

Defined in: addresses.ts:213


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchV1\_3Address

# Variable: FlaunchV1\_3Address

> `const` **FlaunchV1\_3Address**: `Addresses`

Defined in: addresses.ts:219


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchZapAddress

# Variable: FlaunchZapAddress

> `const` **FlaunchZapAddress**: `Addresses`

Defined in: addresses.ts:11


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchZapMultichainAddress

# Variable: FlaunchZapMultichainAddress

> `const` **FlaunchZapMultichainAddress**: `Addresses`

Defined in: addresses.ts:28


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / FlaunchZapV1\_3Address

# Variable: FlaunchZapV1\_3Address

> `const` **FlaunchZapV1\_3Address**: `Addresses`

Defined in: addresses.ts:22


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / GroupMapperV1\_3Address

# Variable: GroupMapperV1\_3Address

> `const` **GroupMapperV1\_3Address**: `Addresses`

Defined in: addresses.ts:361


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / MAX\_SQRT\_PRICE\_LIMIT

# Variable: MAX\_SQRT\_PRICE\_LIMIT

> `const` **MAX\_SQRT\_PRICE\_LIMIT**: `bigint`

Defined in: utils/univ4.ts:448


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / MIN\_SQRT\_PRICE\_LIMIT

# Variable: MIN\_SQRT\_PRICE\_LIMIT

> `const` **MIN\_SQRT\_PRICE\_LIMIT**: `bigint`

Defined in: utils/univ4.ts:447

TickMath sqrt-price bounds, one step inside the absolute limits. These allow the pool to
consume input without a spot-anchored cap. They do not themselves guarantee full consumption
or minimum output; protected PoolSwap execution enforces both from the final delta.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PAIRED\_TOKEN\_TYPE

# Variable: PAIRED\_TOKEN\_TYPE

> `const` **PAIRED\_TOKEN\_TYPE**: `object`

Defined in: types.ts:29

`IPairedTokenRegistry.PairedTokenType`: how a paired token is funded and paid out.
`nativeEth` pools key their paired side as `address(0)`.

## Type Declaration

### erc20

> `readonly` **erc20**: `4` = `4`

### erc20Wrapper

> `readonly` **erc20Wrapper**: `2` = `2`

### nativeEth

> `readonly` **nativeEth**: `3` = `3`

### nativeWrapper

> `readonly` **nativeWrapper**: `1` = `1`

### unset

> `readonly` **unset**: `0` = `0`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PERMIT\_DETAILS

# Variable: PERMIT\_DETAILS

> `const` **PERMIT\_DETAILS**: `object`[]

Defined in: utils/universalRouter.ts:684

## Type Declaration

### name

> **name**: `string` = `"token"`

### type

> **type**: `string` = `"address"`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PERMIT\_TYPES

# Variable: PERMIT\_TYPES

> `const` **PERMIT\_TYPES**: `object`

Defined in: utils/universalRouter.ts:691

## Type Declaration

### PermitDetails

> **PermitDetails**: `object`[] = `PERMIT_DETAILS`

### PermitSingle

> **PermitSingle**: `object`[]


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenAcquisitionDexAddress

# Variable: PairedTokenAcquisitionDexAddress

> `const` **PairedTokenAcquisitionDexAddress**: `Record`\<`number`, `PairedTokenAcquisitionDex`\>

Defined in: addresses.ts:163


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenPositionManagerV1\_3Address

# Variable: PairedTokenPositionManagerV1\_3Address

> `const` **PairedTokenPositionManagerV1\_3Address**: `Addresses`

Defined in: addresses.ts:91


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PairedTokenRegistryV1\_3Address

# Variable: PairedTokenRegistryV1\_3Address

> `const` **PairedTokenRegistryV1\_3Address**: `Addresses`

Defined in: addresses.ts:97


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / Permit2Address

# Variable: Permit2Address

> `const` **Permit2Address**: `Addresses`

Defined in: addresses.ts:588


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolManagerAddress

# Variable: PoolManagerAddress

> `const` **PoolManagerAddress**: `Addresses`

Defined in: addresses.ts:560


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolSwapForHookV1\_3Address

# Variable: PoolSwapForHookV1\_3Address

> `const` **PoolSwapForHookV1\_3Address**: `Record`\<`number`, `Record`\<`string`, `Address`\>\>

Defined in: addresses.ts:120

Hook (lowercase) → the PoolSwap approved on that hook generation's spend gate.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / PoolSwapV1\_3Address

# Variable: PoolSwapV1\_3Address

> `const` **PoolSwapV1\_3Address**: `Addresses`

Defined in: addresses.ts:113


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / Q192

# Variable: Q192

> `const` **Q192**: `bigint`

Defined in: utils/univ4.ts:11


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / Q96

# Variable: Q96

> `const` **Q96**: `bigint`

Defined in: utils/univ4.ts:10


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / QuoterAddress

# Variable: QuoterAddress

> `const` **QuoterAddress**: `Addresses`

Defined in: addresses.ts:574


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReferralEscrowAddress

# Variable: ReferralEscrowAddress

> `const` **ReferralEscrowAddress**: `Addresses`

Defined in: addresses.ts:467


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ReferralEscrowV1\_3Address

# Variable: ReferralEscrowV1\_3Address

> `const` **ReferralEscrowV1\_3Address**: `Addresses`

Defined in: addresses.ts:473


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / RevenueManagerAddress

# Variable: RevenueManagerAddress

> `const` **RevenueManagerAddress**: `Addresses`

Defined in: addresses.ts:286


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / RevenueManagerV1\_3Address

# Variable: RevenueManagerV1\_3Address

> `const` **RevenueManagerV1\_3Address**: `Addresses`

Defined in: addresses.ts:331


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SlipstreamPoolAbi

# Variable: SlipstreamPoolAbi

> `const` **SlipstreamPoolAbi**: readonly \[\{ `inputs`: readonly \[\]; `name`: `"slot0"`; `outputs`: readonly \[\{ `name`: `"sqrtPriceX96"`; `type`: `"uint160"`; \}, \{ `name`: `"tick"`; `type`: `"int24"`; \}, \{ `name`: `"observationIndex"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinality"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinalityNext"`; `type`: `"uint16"`; \}, \{ `name`: `"unlocked"`; `type`: `"bool"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"fee"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"uint24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"tickSpacing"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"int24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"token0"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}\]

Defined in: utils/pairedTokenAcquisition.ts:126

Slipstream-shaped CL pool reads. `slot0` is six fields — V3's `feeProtocol` is dropped.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SlipstreamSwapRouterAbi

# Variable: SlipstreamSwapRouterAbi

> `const` **SlipstreamSwapRouterAbi**: readonly \[\{ `inputs`: readonly \[\{ `components`: readonly \[\{ `name`: `"tokenIn"`; `type`: `"address"`; \}, \{ `name`: `"tokenOut"`; `type`: `"address"`; \}, \{ `name`: `"tickSpacing"`; `type`: `"int24"`; \}, \{ `name`: `"recipient"`; `type`: `"address"`; \}, \{ `name`: `"deadline"`; `type`: `"uint256"`; \}, \{ `name`: `"amountOut"`; `type`: `"uint256"`; \}, \{ `name`: `"amountInMaximum"`; `type`: `"uint256"`; \}, \{ `name`: `"sqrtPriceLimitX96"`; `type`: `"uint160"`; \}\]; `name`: `"params"`; `type`: `"tuple"`; \}\]; `name`: `"exactOutputSingle"`; `outputs`: readonly \[\{ `name`: `"amountIn"`; `type`: `"uint256"`; \}\]; `stateMutability`: `"payable"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\{ `components`: readonly \[\{ `name`: `"path"`; `type`: `"bytes"`; \}, \{ `name`: `"recipient"`; `type`: `"address"`; \}, \{ `name`: `"deadline"`; `type`: `"uint256"`; \}, \{ `name`: `"amountOut"`; `type`: `"uint256"`; \}, \{ `name`: `"amountInMaximum"`; `type`: `"uint256"`; \}\]; `name`: `"params"`; `type`: `"tuple"`; \}\]; `name`: `"exactOutput"`; `outputs`: readonly \[\{ `name`: `"amountIn"`; `type`: `"uint256"`; \}\]; `stateMutability`: `"payable"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\{ `name`: `"data"`; `type`: `"bytes[]"`; \}\]; `name`: `"multicall"`; `outputs`: readonly \[\{ `name`: `"results"`; `type`: `"bytes[]"`; \}\]; `stateMutability`: `"payable"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"refundETH"`; `outputs`: readonly \[\]; `stateMutability`: `"payable"`; `type`: `"function"`; \}\]

Defined in: utils/pairedTokenAcquisition.ts:16

The Slipstream SwapRouter write surface — `tickSpacing`-keyed, `deadline` inside the params.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SolanaVerifierAddress

# Variable: SolanaVerifierAddress

> `const` **SolanaVerifierAddress**: `Addresses`

Defined in: addresses.ts:400


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / StakingManagerAddress

# Variable: StakingManagerAddress

> `const` **StakingManagerAddress**: `Addresses`

Defined in: addresses.ts:310


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / StakingManagerV1\_3Address

# Variable: StakingManagerV1\_3Address

> `const` **StakingManagerV1\_3Address**: `Addresses`

Defined in: addresses.ts:355


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / StateViewAddress

# Variable: StateViewAddress

> `const` **StateViewAddress**: `Addresses`

Defined in: addresses.ts:580


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / SupersededPositionManagerV1\_3Address

# Variable: SupersededPositionManagerV1\_3Address

> `const` **SupersededPositionManagerV1\_3Address**: `Record`\<`number`, `Address`[]\>

Defined in: addresses.ts:77

Hooks that a chain's v1.3 generation has moved OFF but that still serve the pools launched on
them (coins never migrate). Consumers resolving "which hook is this coin on" must consult
these alongside the current maps. Robinhood 2026-08-21 -> 2026-09-xx: the v1.3.1 hooks were
regenerated onto a fixed InternalSwapPool (v1.3.3).


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / TICK\_SPACING

# Variable: TICK\_SPACING

> `const` **TICK\_SPACING**: `60` = `60`

Defined in: utils/univ4.ts:13


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / TickFinder

# Variable: TickFinder

> `const` **TickFinder**: `object`

Defined in: utils/univ4.ts:5

## Type Declaration

### MAX\_TICK

> **MAX\_TICK**: `number` = `887220`

### MIN\_TICK

> **MIN\_TICK**: `number` = `-887220`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / TokenImporterAddress

# Variable: TokenImporterAddress

> `const` **TokenImporterAddress**: `Addresses`

Defined in: addresses.ts:376

Verifiers


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / TokenImporterV1\_3Address

# Variable: TokenImporterV1\_3Address

> `const` **TokenImporterV1\_3Address**: `Addresses`

Defined in: addresses.ts:384


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / TreasuryManagerFactoryAddress

# Variable: TreasuryManagerFactoryAddress

> `const` **TreasuryManagerFactoryAddress**: `Addresses`

Defined in: addresses.ts:278


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / TreasuryManagerFactoryV1\_3Address

# Variable: TreasuryManagerFactoryV1\_3Address

> `const` **TreasuryManagerFactoryV1\_3Address**: `Addresses`

Defined in: addresses.ts:325


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / TwoHopPriceCalculatorAbi

# Variable: TwoHopPriceCalculatorAbi

> `const` **TwoHopPriceCalculatorAbi**: readonly \[\{ `inputs`: readonly \[\]; `name`: `"hubPairedPool"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"hubToken"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}\]

Defined in: utils/pairedTokenAcquisition.ts:168

The two getters read off a paired token's registered two-hop price calculator.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / USDCETHPoolKeys

# Variable: USDCETHPoolKeys

> `const` **USDCETHPoolKeys**: `object`

Defined in: addresses.ts:599

## Index Signature

\[`chainId`: `number`\]: `PoolKey`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / UniV4PositionManagerAddress

# Variable: UniV4PositionManagerAddress

> `const` **UniV4PositionManagerAddress**: `Addresses`

Defined in: addresses.ts:594


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / UniswapV3FactoryAbi

# Variable: UniswapV3FactoryAbi

> `const` **UniswapV3FactoryAbi**: readonly \[\{ `inputs`: readonly \[\{ `name`: `"tokenA"`; `type`: `"address"`; \}, \{ `name`: `"tokenB"`; `type`: `"address"`; \}, \{ `name`: `"fee"`; `type`: `"uint24"`; \}\]; `name`: `"getPool"`; `outputs`: readonly \[\{ `name`: `"pool"`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}\]

Defined in: utils/pairedTokenAcquisition.ts:174

`getPool` on a canonical Uniswap V3 factory.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / UniswapV3PoolAbi

# Variable: UniswapV3PoolAbi

> `const` **UniswapV3PoolAbi**: readonly \[\{ `inputs`: readonly \[\]; `name`: `"slot0"`; `outputs`: readonly \[\{ `name`: `"sqrtPriceX96"`; `type`: `"uint160"`; \}, \{ `name`: `"tick"`; `type`: `"int24"`; \}, \{ `name`: `"observationIndex"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinality"`; `type`: `"uint16"`; \}, \{ `name`: `"observationCardinalityNext"`; `type`: `"uint16"`; \}, \{ `name`: `"feeProtocol"`; `type`: `"uint8"`; \}, \{ `name`: `"unlocked"`; `type`: `"bool"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"fee"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"uint24"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"token0"`; `outputs`: readonly \[\{ `name`: `""`; `type`: `"address"`; \}\]; `stateMutability`: `"view"`; `type`: `"function"`; \}\]

Defined in: utils/pairedTokenAcquisition.ts:147

Canonical Uniswap V3 pool reads. `slot0` carries the seventh `feeProtocol` field.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / UniswapV3QuoterV2Abi

# Variable: UniswapV3QuoterV2Abi

> `const` **UniswapV3QuoterV2Abi**: readonly \[\{ `inputs`: readonly \[\{ `name`: `"path"`; `type`: `"bytes"`; \}, \{ `name`: `"amountIn"`; `type`: `"uint256"`; \}\]; `name`: `"quoteExactInput"`; `outputs`: readonly \[\{ `name`: `"amountOut"`; `type`: `"uint256"`; \}, \{ `name`: `"sqrtPriceX96AfterList"`; `type`: `"uint160[]"`; \}, \{ `name`: `"initializedTicksCrossedList"`; `type`: `"uint32[]"`; \}, \{ `name`: `"gasEstimate"`; `type`: `"uint256"`; \}\]; `stateMutability`: `"nonpayable"`; `type`: `"function"`; \}\]

Defined in: utils/pairedTokenAcquisition.ts:189

Uniswap `QuoterV2`'s depth-aware exact-input quote.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / UniswapV3SwapRouter02Abi

# Variable: UniswapV3SwapRouter02Abi

> `const` **UniswapV3SwapRouter02Abi**: readonly \[\{ `inputs`: readonly \[\{ `components`: readonly \[\{ `name`: `"tokenIn"`; `type`: `"address"`; \}, \{ `name`: `"tokenOut"`; `type`: `"address"`; \}, \{ `name`: `"fee"`; `type`: `"uint24"`; \}, \{ `name`: `"recipient"`; `type`: `"address"`; \}, \{ `name`: `"amountOut"`; `type`: `"uint256"`; \}, \{ `name`: `"amountInMaximum"`; `type`: `"uint256"`; \}, \{ `name`: `"sqrtPriceLimitX96"`; `type`: `"uint160"`; \}\]; `name`: `"params"`; `type`: `"tuple"`; \}\]; `name`: `"exactOutputSingle"`; `outputs`: readonly \[\{ `name`: `"amountIn"`; `type`: `"uint256"`; \}\]; `stateMutability`: `"payable"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\{ `components`: readonly \[\{ `name`: `"path"`; `type`: `"bytes"`; \}, \{ `name`: `"recipient"`; `type`: `"address"`; \}, \{ `name`: `"amountOut"`; `type`: `"uint256"`; \}, \{ `name`: `"amountInMaximum"`; `type`: `"uint256"`; \}\]; `name`: `"params"`; `type`: `"tuple"`; \}\]; `name`: `"exactOutput"`; `outputs`: readonly \[\{ `name`: `"amountIn"`; `type`: `"uint256"`; \}\]; `stateMutability`: `"payable"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\{ `name`: `"deadline"`; `type`: `"uint256"`; \}, \{ `name`: `"data"`; `type`: `"bytes[]"`; \}\]; `name`: `"multicall"`; `outputs`: readonly \[\{ `name`: `"results"`; `type`: `"bytes[]"`; \}\]; `stateMutability`: `"payable"`; `type`: `"function"`; \}, \{ `inputs`: readonly \[\]; `name`: `"refundETH"`; `outputs`: readonly \[\]; `stateMutability`: `"payable"`; `type`: `"function"`; \}\]

Defined in: utils/pairedTokenAcquisition.ts:72

The Uniswap `SwapRouter02` write surface — `fee`-keyed, no `deadline` in the params. The deadline
is enforced by the `multicall(uint256 deadline, bytes[])` overload every routed call is wrapped in.


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / UniversalRouterAddress

# Variable: UniversalRouterAddress

> `const` **UniversalRouterAddress**: `Addresses`

Defined in: addresses.ts:568


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / VirtualsVerifierAddress

# Variable: VirtualsVerifierAddress

> `const` **VirtualsVerifierAddress**: `Addresses`

Defined in: addresses.ts:405


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / WhitelistVerifierAddress

# Variable: WhitelistVerifierAddress

> `const` **WhitelistVerifierAddress**: `Addresses`

Defined in: addresses.ts:410


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / WhitelistedPermissionsAddress

# Variable: WhitelistedPermissionsAddress

> `const` **WhitelistedPermissionsAddress**: `Addresses`

Defined in: addresses.ts:431


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / WhitelistedPermissionsV1\_3Address

# Variable: WhitelistedPermissionsV1\_3Address

> `const` **WhitelistedPermissionsV1\_3Address**: `Addresses`

Defined in: addresses.ts:442


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / ZoraVerifierAddress

# Variable: ZoraVerifierAddress

> `const` **ZoraVerifierAddress**: `Addresses`

Defined in: addresses.ts:415


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / chainIdToChain

# Variable: chainIdToChain

> `const` **chainIdToChain**: `object`

Defined in: helpers/chainIdToChain.ts:10

## Index Signature

\[`key`: `number`\]: `Chain`


---

**@flaunch/sdk v0.13.0**

***

@flaunch/sdk / encodedCallAbi

# Variable: encodedCallAbi

> `const` **encodedCallAbi**: readonly \[\{ \}\]

Defined in: sdk/calldata.ts:16


---

